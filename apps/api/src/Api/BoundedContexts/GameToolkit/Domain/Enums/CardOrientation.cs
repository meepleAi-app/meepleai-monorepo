namespace Api.BoundedContexts.GameToolkit.Domain.Enums;

/// <summary>
/// Card orientation: face-up (visible) or face-down (hidden).
/// </summary>
public enum CardOrientation
{
    FaceDown = 0,
    FaceUp = 1
}
