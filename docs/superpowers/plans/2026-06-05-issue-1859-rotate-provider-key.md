# Issue #1859 — Rotate Provider Key Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementare `POST /api/v1/admin/providers/{name}/rotate-key` con DB-backed credential override (encrypted via `IDataProtector`), step-up 2FA strict (5min, force-strict bypass del flag globale), probe pre-flight validation della nuova key, Redis pub/sub cache invalidation cross-pod, audit Level 3 automatic, rate-limit 1/24h per provider.

**Architecture:** Hybrid DB-backed override + env-var fallback (`IProviderCredentialResolver` cascade: DB-active-row → env-var → throw). Tabella `provider_credentials` con `IDataProtector` ciphertext + `[Auditable]` snapshot before/after. Force-strict step-up via `RequireTwoFactorAttribute.ForceStrict = true`. Cache invalidation cross-pod via Redis pub/sub (pattern `SessionBroadcastService`). Net-new FE `StepUpTwoFactorModal` riusando `TwoFactorVerification`.

**Tech Stack:** .NET 9 · EF Core (Postgres + `IDataProtector`) · MediatR CQRS · FluentValidation · xUnit + Testcontainers · `IConnectionMultiplexer` (StackExchange.Redis) · Next.js 16 · React Query · Vitest · Playwright

**Design doc**: [`docs/superpowers/specs/2026-06-05-issue-1859-rotate-provider-key-design.md`](../specs/2026-06-05-issue-1859-rotate-provider-key-design.md) — sezioni referenced inline.

**Branch**: `feature/issue-1859-be-rotate-key` (parent: `main-dev`)

---

## File Structure

### Backend — Domain layer (BC Administration)
- Create `apps/api/src/Api/BoundedContexts/Administration/Domain/Aggregates/ProviderCredentials/ProviderName.cs`
- Create `apps/api/src/Api/BoundedContexts/Administration/Domain/Aggregates/ProviderCredentials/KeyFingerprint.cs`
- Create `apps/api/src/Api/BoundedContexts/Administration/Domain/Aggregates/ProviderCredentials/ProviderCredential.cs`
- Create `apps/api/src/Api/BoundedContexts/Administration/Domain/Aggregates/ProviderCredentials/Events/ProviderKeyRotatedEvent.cs`
- Create `apps/api/src/Api/BoundedContexts/Administration/Domain/Repositories/IProviderCredentialRepository.cs`

### Backend — Application layer
- Create `apps/api/src/Api/BoundedContexts/Administration/Application/Commands/Providers/RotateProviderKeyCommand.cs`
- Create `apps/api/src/Api/BoundedContexts/Administration/Application/Commands/Providers/RotateProviderKeyCommandHandler.cs`
- Create `apps/api/src/Api/BoundedContexts/Administration/Application/Commands/Providers/RotateProviderKeyCommandValidator.cs`
- Create `apps/api/src/Api/BoundedContexts/Administration/Application/DTOs/RotateProviderKeyRequestDto.cs`
- Create `apps/api/src/Api/BoundedContexts/Administration/Application/DTOs/RotateProviderKeyResponseDto.cs`
- Create `apps/api/src/Api/BoundedContexts/Administration/Application/EventHandlers/ProviderKeyRotatedEventHandler.cs`

### Backend — Infrastructure layer
- Create `apps/api/src/Api/Infrastructure/EntityConfigurations/Administration/ProviderCredentialEntityConfiguration.cs`
- Create `apps/api/src/Api/Infrastructure/Migrations/202606051200000_AddProviderCredentials.cs` (autogenerated by EF)
- Create `apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Repositories/ProviderCredentialRepository.cs`
- Create `apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Services/IProviderCredentialResolver.cs`
- Create `apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Services/ProviderCredentialResolver.cs`
- Create `apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Services/ProviderEnvVarMap.cs`
- Create `apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Services/IProviderCacheInvalidator.cs`
- Create `apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Services/RedisProviderCacheInvalidator.cs`
- Create `apps/api/src/Api/BoundedContexts/Administration/Infrastructure/HostedServices/ProviderCacheInvalidationSubscriber.cs`

### Backend — SharedKernel / cross-cutting
- Create `apps/api/src/Api/SharedKernel/Exceptions/ProviderProbeFailedException.cs`
- Create `apps/api/src/Api/SharedKernel/Exceptions/ProviderCredentialNotConfiguredException.cs`
- Create `apps/api/src/Api/SharedKernel/Exceptions/RateLimitExceededException.cs` (if not exists)
- Modify `apps/api/src/Api/BoundedContexts/Authentication/Application/Attributes/RequireTwoFactorAttribute.cs` — add `ForceStrict`
- Modify `apps/api/src/Api/BoundedContexts/Authentication/Application/Behaviors/TwoFactorEnforcementBehavior.cs:102-107` — respect `ForceStrict`
- Modify `apps/api/src/Api/Routing/AdminProviderEndpoints.cs` — add `POST /{name}/rotate-key`
- Modify `apps/api/src/Api/Extensions/InfrastructureServiceExtensions.cs` — DI register resolver + repo + DataProtection + Redis subscriber + LlmClients refactor
- Modify `apps/api/src/Api/Extensions/RateLimitingServiceExtensions.cs` — add `AdminProviderRotateKey` policy
- Modify `apps/api/src/Api/Services/LlmClients/DeepSeekClient.cs` — use `IProviderCredentialResolver`
- Modify `apps/api/src/Api/Services/LlmClients/OpenRouterClient.cs` — use `IProviderCredentialResolver`
- Modify `apps/api/src/Api/Infrastructure/Persistence/MeepleAiDbContext.cs` — register `ProviderCredential` entity
- Modify `apps/api/src/Api/Middleware/ApiExceptionHandlerMiddleware.cs` — map new exceptions to HTTP subcodes

### Backend — Tests
- Create `apps/api/tests/Api.Tests/BoundedContexts/Administration/Domain/ProviderNameTests.cs`
- Create `apps/api/tests/Api.Tests/BoundedContexts/Administration/Domain/KeyFingerprintTests.cs`
- Create `apps/api/tests/Api.Tests/BoundedContexts/Administration/Domain/ProviderCredentialTests.cs`
- Create `apps/api/tests/Api.Tests/BoundedContexts/Administration/Application/RotateProviderKeyCommandValidatorTests.cs`
- Create `apps/api/tests/Api.Tests/BoundedContexts/Administration/Application/RotateProviderKeyCommandHandlerTests.cs`
- Create `apps/api/tests/Api.Tests/BoundedContexts/Administration/Infrastructure/ProviderCredentialResolverTests.cs`
- Create `apps/api/tests/Api.Tests/BoundedContexts/Authentication/RequireTwoFactorAttributeForceStrictTests.cs`
- Create `apps/api/tests/Api.Tests/Integration/Administration/RotateProviderKeyEndpointIntegrationTests.cs`

### Frontend
- Create `apps/web/src/components/auth/StepUpTwoFactorModal.tsx`
- Create `apps/web/src/components/auth/__tests__/StepUpTwoFactorModal.test.tsx`
- Create `apps/web/src/hooks/queries/useStepUpTwoFactor.ts`
- Create `apps/web/src/hooks/queries/useRotateProviderKey.ts`
- Modify `apps/web/src/components/admin/providers/RotateKeyModal.tsx` — wire BE
- Create `apps/web/src/components/admin/providers/__tests__/RotateKeyModal.test.tsx`
- Modify `apps/web/src/lib/api/schemas/providers.ts` — add rotate types
- Create `apps/web/e2e/admin-providers-rotate-key.spec.ts`

---

## Phase 1 — Domain Layer (Value Objects + Aggregate)

### Task 1: `ProviderName` value object

**Files:**
- Create: `apps/api/src/Api/BoundedContexts/Administration/Domain/Aggregates/ProviderCredentials/ProviderName.cs`
- Test: `apps/api/tests/Api.Tests/BoundedContexts/Administration/Domain/ProviderNameTests.cs`

- [ ] **Step 1.1: Write failing tests**

```csharp
// apps/api/tests/Api.Tests/BoundedContexts/Administration/Domain/ProviderNameTests.cs
using Api.BoundedContexts.Administration.Domain.Aggregates.ProviderCredentials;
using FluentAssertions;
using Xunit;

namespace Api.Tests.BoundedContexts.Administration.Domain;

[Trait("Category", "Unit")]
[Trait("BoundedContext", "Administration")]
public sealed class ProviderNameTests
{
    [Theory]
    [InlineData("deepseek")]
    [InlineData("openrouter")]
    [InlineData("DeepSeek")]   // case-insensitive
    [InlineData("OPENROUTER")]
    public void Create_AllowedValue_ReturnsNormalizedLowercase(string raw)
    {
        var name = ProviderName.Create(raw);
        name.Value.Should().Be(raw.ToLowerInvariant());
    }

    [Theory]
    [InlineData("ollama")]      // not in whitelist (no API key required)
    [InlineData("gpt4")]
    [InlineData("")]
    [InlineData(" ")]
    public void Create_DisallowedValue_Throws(string raw)
    {
        var act = () => ProviderName.Create(raw);
        act.Should().Throw<ArgumentException>().WithMessage("*allowed*");
    }

    [Fact]
    public void Allowed_ContainsExpectedSet()
    {
        ProviderName.Allowed.Should().BeEquivalentTo(new[] { "deepseek", "openrouter" });
    }

    [Fact]
    public void Equality_SameValue_AreEqual()
    {
        var a = ProviderName.Create("deepseek");
        var b = ProviderName.Create("DEEPSEEK");
        (a == b).Should().BeTrue();
        a.GetHashCode().Should().Be(b.GetHashCode());
    }
}
```

- [ ] **Step 1.2: Run tests — expect FAIL**

```powershell
cd apps/api/src/Api
dotnet test ../../tests/Api.Tests/Api.Tests.csproj --filter "FullyQualifiedName~ProviderNameTests" --nologo
```

Expected: `ProviderName not found` (compile error).

- [ ] **Step 1.3: Implement ProviderName**

```csharp
// apps/api/src/Api/BoundedContexts/Administration/Domain/Aggregates/ProviderCredentials/ProviderName.cs
namespace Api.BoundedContexts.Administration.Domain.Aggregates.ProviderCredentials;

public sealed record ProviderName
{
    public static readonly IReadOnlySet<string> Allowed =
        new HashSet<string>(StringComparer.OrdinalIgnoreCase) { "deepseek", "openrouter" };

    public string Value { get; }

    private ProviderName(string value) => Value = value;

    public static ProviderName Create(string raw)
    {
        if (string.IsNullOrWhiteSpace(raw))
            throw new ArgumentException(
                $"Provider name cannot be empty. Allowed: {string.Join(", ", Allowed.OrderBy(x => x))}",
                nameof(raw));

        var normalized = raw.Trim().ToLowerInvariant();
        if (!Allowed.Contains(normalized))
            throw new ArgumentException(
                $"Provider '{raw}' is not in the allowed set: {string.Join(", ", Allowed.OrderBy(x => x))}",
                nameof(raw));

        return new ProviderName(normalized);
    }

    public override string ToString() => Value;
}
```

- [ ] **Step 1.4: Run tests — expect PASS**

```powershell
dotnet test ../../tests/Api.Tests/Api.Tests.csproj --filter "FullyQualifiedName~ProviderNameTests" --nologo
```

Expected: 8 passed, 0 failed.

- [ ] **Step 1.5: Commit**

```bash
git add apps/api/src/Api/BoundedContexts/Administration/Domain/Aggregates/ProviderCredentials/ProviderName.cs apps/api/tests/Api.Tests/BoundedContexts/Administration/Domain/ProviderNameTests.cs
git commit -m "feat(admin-providers): #1859 ProviderName value object with whitelist (deepseek, openrouter)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 2: `KeyFingerprint` value object

**Files:**
- Create: `apps/api/src/Api/BoundedContexts/Administration/Domain/Aggregates/ProviderCredentials/KeyFingerprint.cs`
- Test: `apps/api/tests/Api.Tests/BoundedContexts/Administration/Domain/KeyFingerprintTests.cs`

- [ ] **Step 2.1: Write failing tests**

```csharp
// apps/api/tests/Api.Tests/BoundedContexts/Administration/Domain/KeyFingerprintTests.cs
using Api.BoundedContexts.Administration.Domain.Aggregates.ProviderCredentials;
using FluentAssertions;
using Xunit;

namespace Api.Tests.BoundedContexts.Administration.Domain;

[Trait("Category", "Unit")]
public sealed class KeyFingerprintTests
{
    [Theory]
    [InlineData("sk-deepseek-abcd1234efgh5678", "sk-de..5678")]
    [InlineData("sk-or-v1-foobar1234", "sk-or..1234")]
    [InlineData("1234567890", "12345..7890")]
    public void FromPlaintext_ValidKey_ReturnsMaskedFingerprint(string raw, string expected)
    {
        var fp = KeyFingerprint.FromPlaintext(raw);
        fp.Value.Should().Be(expected);
    }

    [Theory]
    [InlineData("short")]      // 5 chars < min 10
    [InlineData("123456789")]  // 9 chars < min 10
    [InlineData("")]
    [InlineData(null)]
    public void FromPlaintext_TooShort_Throws(string? raw)
    {
        var act = () => KeyFingerprint.FromPlaintext(raw!);
        act.Should().Throw<ArgumentException>();
    }

    [Fact]
    public void FromPlaintext_DoesNotLeakFullKey_OnlyFingerprintIsExposed()
    {
        var raw = "sk-this-is-a-very-secret-deepseek-key-abcd1234";
        var fp = KeyFingerprint.FromPlaintext(raw);

        fp.Value.Should().NotContain(raw);
        fp.Value.Length.Should().BeLessThan(raw.Length);
        fp.Value.Should().Contain("..");
    }
}
```

- [ ] **Step 2.2: Run tests — expect FAIL**

```powershell
dotnet test ../../tests/Api.Tests/Api.Tests.csproj --filter "FullyQualifiedName~KeyFingerprintTests" --nologo
```

Expected: compile error `KeyFingerprint not found`.

- [ ] **Step 2.3: Implement KeyFingerprint**

```csharp
// apps/api/src/Api/BoundedContexts/Administration/Domain/Aggregates/ProviderCredentials/KeyFingerprint.cs
namespace Api.BoundedContexts.Administration.Domain.Aggregates.ProviderCredentials;

public sealed record KeyFingerprint
{
    private const int MinKeyLength = 10;

    public string Value { get; }

    private KeyFingerprint(string value) => Value = value;

    public static KeyFingerprint FromPlaintext(string apiKey)
    {
        if (string.IsNullOrEmpty(apiKey) || apiKey.Length < MinKeyLength)
            throw new ArgumentException(
                $"API key must be at least {MinKeyLength} characters for fingerprint generation",
                nameof(apiKey));

        // First 5 + ".." + last 4 — never leaks the middle (secret part)
        var fingerprint = $"{apiKey[..5]}..{apiKey[^4..]}";
        return new KeyFingerprint(fingerprint);
    }

    internal static KeyFingerprint FromStorage(string maskedValue) => new(maskedValue);

    public override string ToString() => Value;
}
```

- [ ] **Step 2.4: Run tests — expect PASS**

```powershell
dotnet test ../../tests/Api.Tests/Api.Tests.csproj --filter "FullyQualifiedName~KeyFingerprintTests" --nologo
```

Expected: 7 passed.

- [ ] **Step 2.5: Commit**

```bash
git add apps/api/src/Api/BoundedContexts/Administration/Domain/Aggregates/ProviderCredentials/KeyFingerprint.cs apps/api/tests/Api.Tests/BoundedContexts/Administration/Domain/KeyFingerprintTests.cs
git commit -m "feat(admin-providers): #1859 KeyFingerprint VO (first5..last4 masking)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 3: `ProviderCredential` aggregate + `ProviderKeyRotatedEvent` + exceptions

**Files:**
- Create: `apps/api/src/Api/BoundedContexts/Administration/Domain/Aggregates/ProviderCredentials/ProviderCredential.cs`
- Create: `apps/api/src/Api/BoundedContexts/Administration/Domain/Aggregates/ProviderCredentials/Events/ProviderKeyRotatedEvent.cs`
- Create: `apps/api/src/Api/BoundedContexts/Administration/Domain/Repositories/IProviderCredentialRepository.cs`
- Create: `apps/api/src/Api/SharedKernel/Exceptions/ProviderProbeFailedException.cs`
- Create: `apps/api/src/Api/SharedKernel/Exceptions/ProviderCredentialNotConfiguredException.cs`
- Test: `apps/api/tests/Api.Tests/BoundedContexts/Administration/Domain/ProviderCredentialTests.cs`

- [ ] **Step 3.1: Write failing tests**

```csharp
// apps/api/tests/Api.Tests/BoundedContexts/Administration/Domain/ProviderCredentialTests.cs
using Api.BoundedContexts.Administration.Domain.Aggregates.ProviderCredentials;
using Api.BoundedContexts.Administration.Domain.Aggregates.ProviderCredentials.Events;
using FluentAssertions;
using Microsoft.Extensions.Time.Testing;
using Xunit;

namespace Api.Tests.BoundedContexts.Administration.Domain;

[Trait("Category", "Unit")]
[Trait("BoundedContext", "Administration")]
public sealed class ProviderCredentialTests
{
    private static readonly DateTimeOffset FixedNow = new(2026, 6, 5, 12, 0, 0, TimeSpan.Zero);

    private readonly FakeTimeProvider _timeProvider = new(FixedNow);

    [Fact]
    public void Create_FirstRotation_ProducesActiveCredential_NoPreviousId()
    {
        var providerName = ProviderName.Create("deepseek");
        var fingerprint = KeyFingerprint.FromPlaintext("sk-deepseek-abcd1234");
        var actorId = Guid.NewGuid();

        var credential = ProviderCredential.Create(
            provider: providerName,
            encryptedKey: "ciphertext-abc",
            fingerprint: fingerprint,
            rotatedByUserId: actorId,
            previousCredentialId: null,
            timeProvider: _timeProvider);

        credential.Id.Should().NotBe(Guid.Empty);
        credential.ProviderName.Should().Be(providerName);
        credential.EncryptedApiKey.Should().Be("ciphertext-abc");
        credential.Fingerprint.Should().Be(fingerprint);
        credential.IsActive.Should().BeTrue();
        credential.RotatedAt.Should().Be(FixedNow.UtcDateTime);
        credential.RotatedByUserId.Should().Be(actorId);
        credential.PreviousCredentialId.Should().BeNull();

        credential.DomainEvents.Should().ContainSingle(e => e is ProviderKeyRotatedEvent);
        var evt = credential.DomainEvents.OfType<ProviderKeyRotatedEvent>().Single();
        evt.ProviderName.Should().Be("deepseek");
        evt.NewFingerprint.Should().Be(fingerprint.Value);
        evt.PreviousFingerprint.Should().BeNull();
    }

    [Fact]
    public void Create_SubsequentRotation_ProducesActiveCredential_WithPreviousId()
    {
        var providerName = ProviderName.Create("openrouter");
        var fingerprint = KeyFingerprint.FromPlaintext("sk-or-v1-newkey123");
        var actorId = Guid.NewGuid();
        var previousId = Guid.NewGuid();

        var credential = ProviderCredential.Create(
            provider: providerName,
            encryptedKey: "ciphertext-new",
            fingerprint: fingerprint,
            rotatedByUserId: actorId,
            previousCredentialId: previousId,
            timeProvider: _timeProvider);

        credential.PreviousCredentialId.Should().Be(previousId);
        credential.IsActive.Should().BeTrue();
    }

    [Fact]
    public void Deactivate_FlipsIsActiveToFalse()
    {
        var credential = CreateSampleCredential();
        credential.IsActive.Should().BeTrue();

        credential.Deactivate(_timeProvider);

        credential.IsActive.Should().BeFalse();
    }

    [Fact]
    public void Deactivate_AlreadyInactive_DoesNotThrow_Idempotent()
    {
        var credential = CreateSampleCredential();
        credential.Deactivate(_timeProvider);

        var act = () => credential.Deactivate(_timeProvider);
        act.Should().NotThrow();
        credential.IsActive.Should().BeFalse();
    }

    private ProviderCredential CreateSampleCredential() =>
        ProviderCredential.Create(
            ProviderName.Create("deepseek"),
            "cipher",
            KeyFingerprint.FromPlaintext("sk-de-abcd1234"),
            Guid.NewGuid(),
            null,
            _timeProvider);
}
```

- [ ] **Step 3.2: Run tests — expect FAIL (compile)**

- [ ] **Step 3.3: Implement domain types**

```csharp
// apps/api/src/Api/BoundedContexts/Administration/Domain/Aggregates/ProviderCredentials/Events/ProviderKeyRotatedEvent.cs
using MediatR;

namespace Api.BoundedContexts.Administration.Domain.Aggregates.ProviderCredentials.Events;

/// <summary>
/// Raised when a provider's API key has been rotated. Consumed by:
/// - <c>ProviderKeyRotatedEventHandler</c> → publishes a Redis pub/sub message
///   so all pods invalidate their <c>IProviderCredentialResolver</c> cache.
/// </summary>
public sealed record ProviderKeyRotatedEvent(
    Guid CredentialId,
    string ProviderName,
    string NewFingerprint,
    string? PreviousFingerprint,
    Guid RotatedByUserId,
    DateTime RotatedAt) : INotification;
```

```csharp
// apps/api/src/Api/BoundedContexts/Administration/Domain/Aggregates/ProviderCredentials/ProviderCredential.cs
using Api.BoundedContexts.Administration.Domain.Aggregates.ProviderCredentials.Events;
using Api.Infrastructure.Persistence;
using MediatR;

namespace Api.BoundedContexts.Administration.Domain.Aggregates.ProviderCredentials;

/// <summary>
/// Aggregate root tracking a provider's API key (encrypted via IDataProtector).
/// Issue #1859. Decorated with [Auditable] so AuditingSaveChangesInterceptor captures
/// before/after snapshots automatically (see design §11.2).
/// </summary>
[Auditable]
public sealed class ProviderCredential
{
    private readonly List<INotification> _domainEvents = new();

    public Guid Id { get; private set; }
    public ProviderName ProviderName { get; private set; } = default!;
    public string EncryptedApiKey { get; private set; } = default!;
    public KeyFingerprint Fingerprint { get; private set; } = default!;
    public bool IsActive { get; private set; }
    public DateTime RotatedAt { get; private set; }
    public Guid RotatedByUserId { get; private set; }
    public Guid? PreviousCredentialId { get; private set; }
    public byte[] RowVersion { get; private set; } = Array.Empty<byte>();

    public IReadOnlyCollection<INotification> DomainEvents => _domainEvents.AsReadOnly();
    public void ClearDomainEvents() => _domainEvents.Clear();

    private ProviderCredential() { }   // EF Core

    public static ProviderCredential Create(
        ProviderName provider,
        string encryptedKey,
        KeyFingerprint fingerprint,
        Guid rotatedByUserId,
        Guid? previousCredentialId,
        TimeProvider timeProvider)
    {
        ArgumentNullException.ThrowIfNull(provider);
        ArgumentException.ThrowIfNullOrEmpty(encryptedKey);
        ArgumentNullException.ThrowIfNull(fingerprint);
        ArgumentNullException.ThrowIfNull(timeProvider);
        if (rotatedByUserId == Guid.Empty)
            throw new ArgumentException("RotatedByUserId required", nameof(rotatedByUserId));

        var credential = new ProviderCredential
        {
            Id = Guid.NewGuid(),
            ProviderName = provider,
            EncryptedApiKey = encryptedKey,
            Fingerprint = fingerprint,
            IsActive = true,
            RotatedAt = timeProvider.GetUtcNow().UtcDateTime,
            RotatedByUserId = rotatedByUserId,
            PreviousCredentialId = previousCredentialId
        };

        credential._domainEvents.Add(new ProviderKeyRotatedEvent(
            CredentialId: credential.Id,
            ProviderName: provider.Value,
            NewFingerprint: fingerprint.Value,
            PreviousFingerprint: null,    // set by handler when previous lookup happens
            RotatedByUserId: rotatedByUserId,
            RotatedAt: credential.RotatedAt));

        return credential;
    }

    public void Deactivate(TimeProvider timeProvider)
    {
        ArgumentNullException.ThrowIfNull(timeProvider);
        IsActive = false;
    }
}
```

```csharp
// apps/api/src/Api/BoundedContexts/Administration/Domain/Repositories/IProviderCredentialRepository.cs
using Api.BoundedContexts.Administration.Domain.Aggregates.ProviderCredentials;

namespace Api.BoundedContexts.Administration.Domain.Repositories;

public interface IProviderCredentialRepository
{
    Task<ProviderCredential?> GetActiveAsync(string providerName, CancellationToken ct);
    Task<ProviderCredential?> GetLastRotationAsync(string providerName, CancellationToken ct);
    Task AddAsync(ProviderCredential credential, CancellationToken ct);
    Task SaveChangesAsync(CancellationToken ct);
}
```

```csharp
// apps/api/src/Api/SharedKernel/Exceptions/ProviderProbeFailedException.cs
namespace Api.SharedKernel.Exceptions;

public sealed class ProviderProbeFailedException : Exception
{
    public string ProviderName { get; }
    public string Subcode => "provider_probe_failed";

    public ProviderProbeFailedException(string providerName, string reason)
        : base($"Probe failed for provider '{providerName}': {reason}")
    {
        ProviderName = providerName;
    }
}
```

```csharp
// apps/api/src/Api/SharedKernel/Exceptions/ProviderCredentialNotConfiguredException.cs
namespace Api.SharedKernel.Exceptions;

public sealed class ProviderCredentialNotConfiguredException : Exception
{
    public string ProviderName { get; }
    public string Subcode => "provider_credential_not_configured";

    public ProviderCredentialNotConfiguredException(string providerName)
        : base($"No active credential or env-var configured for provider '{providerName}'")
    {
        ProviderName = providerName;
    }
}
```

- [ ] **Step 3.4: Run tests — expect PASS**

```powershell
dotnet test ../../tests/Api.Tests/Api.Tests.csproj --filter "FullyQualifiedName~ProviderCredentialTests" --nologo
```

Expected: 4 passed.

- [ ] **Step 3.5: Commit**

```bash
git add apps/api/src/Api/BoundedContexts/Administration/Domain/Aggregates/ProviderCredentials/ apps/api/src/Api/BoundedContexts/Administration/Domain/Repositories/IProviderCredentialRepository.cs apps/api/src/Api/SharedKernel/Exceptions/ProviderProbeFailedException.cs apps/api/src/Api/SharedKernel/Exceptions/ProviderCredentialNotConfiguredException.cs apps/api/tests/Api.Tests/BoundedContexts/Administration/Domain/ProviderCredentialTests.cs
git commit -m "feat(admin-providers): #1859 ProviderCredential aggregate + event + repo interface + exceptions

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Phase 2 — Data Layer (EntityConfig + Migration + Repository)

### Task 4: EntityConfiguration + EF Migration

**Files:**
- Create: `apps/api/src/Api/Infrastructure/EntityConfigurations/Administration/ProviderCredentialEntityConfiguration.cs`
- Modify: `apps/api/src/Api/Infrastructure/Persistence/MeepleAiDbContext.cs` — add DbSet
- Create: `apps/api/src/Api/Infrastructure/Migrations/202606051200000_AddProviderCredentials.cs` (autogenerated)

- [ ] **Step 4.1: Implement EntityConfiguration**

```csharp
// apps/api/src/Api/Infrastructure/EntityConfigurations/Administration/ProviderCredentialEntityConfiguration.cs
using Api.BoundedContexts.Administration.Domain.Aggregates.ProviderCredentials;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;

namespace Api.Infrastructure.EntityConfigurations.Administration;

internal sealed class ProviderCredentialEntityConfiguration
    : IEntityTypeConfiguration<ProviderCredential>
{
    public void Configure(EntityTypeBuilder<ProviderCredential> builder)
    {
        builder.ToTable("provider_credentials");
        builder.HasKey(e => e.Id);
        builder.Property(e => e.Id).HasColumnName("id");

        builder.Property(e => e.ProviderName)
            .HasColumnName("provider_name")
            .HasConversion(vo => vo.Value, raw => ProviderName.Create(raw))
            .IsRequired();

        builder.Property(e => e.EncryptedApiKey)
            .HasColumnName("encrypted_api_key")
            .IsRequired();

        builder.Property(e => e.Fingerprint)
            .HasColumnName("key_fingerprint")
            .HasConversion(vo => vo.Value, raw => KeyFingerprint.FromStorage(raw))
            .IsRequired();

        builder.Property(e => e.IsActive).HasColumnName("is_active").IsRequired();
        builder.Property(e => e.RotatedAt).HasColumnName("rotated_at").IsRequired();
        builder.Property(e => e.RotatedByUserId).HasColumnName("rotated_by_user_id").IsRequired();
        builder.Property(e => e.PreviousCredentialId).HasColumnName("previous_credential_id");

        builder.Property(e => e.RowVersion)
            .HasColumnName("row_version")
            .IsRowVersion();

        // Partial unique index: 1 sola row attiva per provider
        builder.HasIndex(e => e.ProviderName)
            .IsUnique()
            .HasFilter("is_active = true")
            .HasDatabaseName("ux_provider_credentials_active_one");

        builder.HasIndex(e => new { e.ProviderName, e.RotatedAt })
            .IsDescending(false, true)
            .HasDatabaseName("ix_provider_credentials_rotated_at");

        // Domain events not persisted
        builder.Ignore(e => e.DomainEvents);
    }
}
```

- [ ] **Step 4.2: Register entity in `MeepleAiDbContext`**

Modify `apps/api/src/Api/Infrastructure/Persistence/MeepleAiDbContext.cs`:

```csharp
// Add property:
public DbSet<ProviderCredential> ProviderCredentials => Set<ProviderCredential>();

// Add in OnModelCreating, near other Administration configs:
modelBuilder.ApplyConfiguration(new ProviderCredentialEntityConfiguration());
```

(Match existing pattern — usually `modelBuilder.ApplyConfigurationsFromAssembly(typeof(MeepleAiDbContext).Assembly)` already auto-discovers. Verify before manual call.)

- [ ] **Step 4.3: Generate EF migration**

```powershell
cd apps/api/src/Api
dotnet ef migrations add AddProviderCredentials --output-dir Infrastructure/Migrations
```

Expected: 2 new files under `Infrastructure/Migrations/`:
- `<timestamp>_AddProviderCredentials.cs`
- `<timestamp>_AddProviderCredentials.Designer.cs`

Plus `MeepleAiDbContextModelSnapshot.cs` updated.

- [ ] **Step 4.4: Review migration SQL**

Open the generated `<timestamp>_AddProviderCredentials.cs`. Verify:
- `CREATE TABLE provider_credentials` with all 9 columns
- `CREATE UNIQUE INDEX ux_provider_credentials_active_one` with filter `is_active = true`
- `CREATE INDEX ix_provider_credentials_rotated_at`

If the snake_case conventions don't kick in (column names PascalCase), adjust `HasColumnName` calls in `ProviderCredentialEntityConfiguration` and re-run.

- [ ] **Step 4.5: Apply migration to dev DB**

```powershell
dotnet ef database update
```

Expected: `Done.` no errors.

Verify:

```powershell
docker exec meepleai-postgres psql -U meepleai -d meepleai_staging -c "\d provider_credentials"
```

Expected: table listed with all columns.

- [ ] **Step 4.6: Commit**

```bash
git add apps/api/src/Api/Infrastructure/EntityConfigurations/Administration/ProviderCredentialEntityConfiguration.cs apps/api/src/Api/Infrastructure/Persistence/MeepleAiDbContext.cs apps/api/src/Api/Infrastructure/Migrations/
git commit -m "feat(admin-providers): #1859 provider_credentials table + EF config + migration

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 5: ProviderCredentialRepository implementation

**Files:**
- Create: `apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Repositories/ProviderCredentialRepository.cs`
- Test: integration test in Task 14 (after handler wired)

- [ ] **Step 5.1: Implement repository**

```csharp
// apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Repositories/ProviderCredentialRepository.cs
using Api.BoundedContexts.Administration.Domain.Aggregates.ProviderCredentials;
using Api.BoundedContexts.Administration.Domain.Repositories;
using Api.Infrastructure.Persistence;
using Microsoft.EntityFrameworkCore;

namespace Api.BoundedContexts.Administration.Infrastructure.Repositories;

internal sealed class ProviderCredentialRepository : IProviderCredentialRepository
{
    private readonly MeepleAiDbContext _dbContext;

    public ProviderCredentialRepository(MeepleAiDbContext dbContext)
    {
        _dbContext = dbContext ?? throw new ArgumentNullException(nameof(dbContext));
    }

    public Task<ProviderCredential?> GetActiveAsync(string providerName, CancellationToken ct) =>
        _dbContext.ProviderCredentials
            .AsTracking()
            .FirstOrDefaultAsync(c =>
                c.ProviderName.Value == providerName.ToLowerInvariant() && c.IsActive, ct);

    public Task<ProviderCredential?> GetLastRotationAsync(string providerName, CancellationToken ct) =>
        _dbContext.ProviderCredentials
            .AsNoTracking()
            .Where(c => c.ProviderName.Value == providerName.ToLowerInvariant())
            .OrderByDescending(c => c.RotatedAt)
            .FirstOrDefaultAsync(ct);

    public async Task AddAsync(ProviderCredential credential, CancellationToken ct)
    {
        ArgumentNullException.ThrowIfNull(credential);
        await _dbContext.ProviderCredentials.AddAsync(credential, ct).ConfigureAwait(false);
    }

    public Task SaveChangesAsync(CancellationToken ct) => _dbContext.SaveChangesAsync(ct);
}
```

- [ ] **Step 5.2: Commit**

```bash
git add apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Repositories/ProviderCredentialRepository.cs
git commit -m "feat(admin-providers): #1859 ProviderCredentialRepository EF implementation

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Phase 3 — Step-up Extension

### Task 6: `RequireTwoFactorAttribute.ForceStrict` + behavior update

**Files:**
- Modify: `apps/api/src/Api/BoundedContexts/Authentication/Application/Attributes/RequireTwoFactorAttribute.cs`
- Modify: `apps/api/src/Api/BoundedContexts/Authentication/Application/Behaviors/TwoFactorEnforcementBehavior.cs:102-107`
- Test: `apps/api/tests/Api.Tests/BoundedContexts/Authentication/RequireTwoFactorAttributeForceStrictTests.cs`

- [ ] **Step 6.1: Write failing test for ForceStrict behavior**

```csharp
// apps/api/tests/Api.Tests/BoundedContexts/Authentication/RequireTwoFactorAttributeForceStrictTests.cs
using Api.BoundedContexts.Authentication.Application.Attributes;
using Api.BoundedContexts.Authentication.Application.Behaviors;
using Api.BoundedContexts.Authentication.Application.Configuration;
using Api.BoundedContexts.Authentication.Application.DTOs;
using Api.Middleware.Exceptions;
using FluentAssertions;
using MediatR;
using Microsoft.AspNetCore.Http;
using Microsoft.Extensions.DependencyInjection;
using Microsoft.Extensions.Logging.Abstractions;
using Microsoft.Extensions.Time.Testing;
using NSubstitute;
using Xunit;

namespace Api.Tests.BoundedContexts.Authentication;

[Trait("Category", "Unit")]
public sealed class RequireTwoFactorAttributeForceStrictTests
{
    [RequireTwoFactor(MaxAgeMinutes = 5, ForceStrict = true)]
    public sealed record SecurityCriticalCommand : IRequest<Unit>;

    [Fact]
    public async Task ForceStrict_True_BlocksEvenWhenGlobalStrictModeIsOff()
    {
        var (behavior, _) = BuildBehavior(globalStrictMode: false);

        var act = async () => await behavior.Handle(
            new SecurityCriticalCommand(),
            () => Task.FromResult(Unit.Value),
            CancellationToken.None);

        await act.Should().ThrowAsync<TwoFactorRequiredException>();
    }

    [Fact]
    public async Task ForceStrict_False_RespectsGlobalStrictModeOff_DoesNotBlock()
    {
        // … similar with [RequireTwoFactor] (ForceStrict default false)
        // assert no throw when global strict OFF
    }

    private (TwoFactorEnforcementBehavior<SecurityCriticalCommand, Unit>, IHttpContextAccessor) BuildBehavior(
        bool globalStrictMode)
    {
        var httpCtx = Substitute.For<IHttpContextAccessor>();
        var session = new SessionStatusDto { /* … minimal valid session, IsValid = true, no recent TOTP */ };
        var http = new DefaultHttpContext();
        http.Items[nameof(SessionStatusDto)] = session;
        httpCtx.HttpContext.Returns(http);

        var cfg = Substitute.For<ITwoFactorEnforcementConfiguration>();
        cfg.GetStrictModeAsync(Arg.Any<CancellationToken>()).Returns(Task.FromResult(globalStrictMode));

        var scopeFactory = Substitute.For<IServiceScopeFactory>();

        var behavior = new TwoFactorEnforcementBehavior<SecurityCriticalCommand, Unit>(
            httpCtx, cfg, new FakeTimeProvider(DateTimeOffset.UtcNow), scopeFactory,
            NullLogger<TwoFactorEnforcementBehavior<SecurityCriticalCommand, Unit>>.Instance);

        return (behavior, httpCtx);
    }
}
```

- [ ] **Step 6.2: Run test — expect FAIL (compile)**

`ForceStrict` property does not exist on `RequireTwoFactorAttribute`.

- [ ] **Step 6.3: Add `ForceStrict` to RequireTwoFactorAttribute**

```csharp
// apps/api/src/Api/BoundedContexts/Authentication/Application/Attributes/RequireTwoFactorAttribute.cs (modify)
[AttributeUsage(AttributeTargets.Class, Inherited = false, AllowMultiple = false)]
internal sealed class RequireTwoFactorAttribute : Attribute
{
    public int MaxAgeMinutes { get; set; } = 30;
    public string? Reason { get; set; }

    /// <summary>
    /// When true, the behavior enforces strict step-up even if the global
    /// <c>TwoFactor:StrictMode</c> flag is OFF. Use for security-critical commands
    /// (provider key rotation, etc) that must always block on stale 2FA.
    /// Default: false (respects global flag).
    /// </summary>
    public bool ForceStrict { get; set; } = false;
}
```

- [ ] **Step 6.4: Update TwoFactorEnforcementBehavior to respect ForceStrict**

Modify `apps/api/src/Api/BoundedContexts/Authentication/Application/Behaviors/TwoFactorEnforcementBehavior.cs` line 102-107:

```csharp
// Before:
var strictMode = await _twoFactorConfig.GetStrictModeAsync(cancellationToken).ConfigureAwait(false);
if (!strictMode)
{
    LogShadow(actor, attr);
    return await next().ConfigureAwait(false);
}

// After:
var strictMode = await _twoFactorConfig.GetStrictModeAsync(cancellationToken).ConfigureAwait(false);
if (!strictMode && !attr.ForceStrict)
{
    LogShadow(actor, attr);
    return await next().ConfigureAwait(false);
}
```

- [ ] **Step 6.5: Run tests — expect PASS**

```powershell
dotnet test ../../tests/Api.Tests/Api.Tests.csproj --filter "FullyQualifiedName~RequireTwoFactorAttributeForceStrictTests" --nologo
```

- [ ] **Step 6.6: Verify regression — existing TwoFactorEnforcement tests still pass**

```powershell
dotnet test ../../tests/Api.Tests/Api.Tests.csproj --filter "FullyQualifiedName~TwoFactorEnforcement" --nologo
```

Expected: existing tests on commands WITHOUT `ForceStrict=true` continue to pass (default `false` preserves shadow-mode).

- [ ] **Step 6.7: Commit**

```bash
git add apps/api/src/Api/BoundedContexts/Authentication/Application/Attributes/RequireTwoFactorAttribute.cs apps/api/src/Api/BoundedContexts/Authentication/Application/Behaviors/TwoFactorEnforcementBehavior.cs apps/api/tests/Api.Tests/BoundedContexts/Authentication/RequireTwoFactorAttributeForceStrictTests.cs
git commit -m "feat(auth): #1859 RequireTwoFactorAttribute.ForceStrict for security-critical commands

Add a per-command override that forces strict 2FA enforcement even when the
global TwoFactor:StrictMode flag is OFF. Default false preserves existing
shadow-mode behavior for non-decorated commands.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Phase 4 — Credential Resolver (DB → env-var fallback)

### Task 7: `IProviderCredentialResolver` + `ProviderEnvVarMap` + impl

**Files:**
- Create: `apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Services/IProviderCredentialResolver.cs`
- Create: `apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Services/ProviderEnvVarMap.cs`
- Create: `apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Services/ProviderCredentialResolver.cs`
- Test: `apps/api/tests/Api.Tests/BoundedContexts/Administration/Infrastructure/ProviderCredentialResolverTests.cs`

- [ ] **Step 7.1: Write failing tests**

```csharp
// apps/api/tests/Api.Tests/BoundedContexts/Administration/Infrastructure/ProviderCredentialResolverTests.cs
using Api.BoundedContexts.Administration.Domain.Aggregates.ProviderCredentials;
using Api.BoundedContexts.Administration.Domain.Repositories;
using Api.BoundedContexts.Administration.Infrastructure.Services;
using Api.SharedKernel.Exceptions;
using FluentAssertions;
using Microsoft.AspNetCore.DataProtection;
using Microsoft.Extensions.Caching.Memory;
using Microsoft.Extensions.Logging.Abstractions;
using Microsoft.Extensions.Time.Testing;
using NSubstitute;
using Xunit;

namespace Api.Tests.BoundedContexts.Administration.Infrastructure;

[Trait("Category", "Unit")]
public sealed class ProviderCredentialResolverTests
{
    private const string TestEnvVar = "ROTATE_TEST_DEEPSEEK_KEY";

    [Fact]
    public async Task ResolveAsync_DbActiveExists_ReturnsDecryptedPlaintext()
    {
        var ciphertext = "encrypted-bytes";
        var plaintext = "sk-decrypted-key";
        var credential = CreateActiveCredential("deepseek", ciphertext);

        var repo = Substitute.For<IProviderCredentialRepository>();
        repo.GetActiveAsync("deepseek", Arg.Any<CancellationToken>())
            .Returns(Task.FromResult<ProviderCredential?>(credential));

        var protector = Substitute.For<IDataProtector>();
        protector.Unprotect(ciphertext).Returns(plaintext);
        var provider = Substitute.For<IDataProtectionProvider>();
        provider.CreateProtector("ProviderCredentials").Returns(protector);

        var sut = new ProviderCredentialResolver(repo, provider, new MemoryCache(new MemoryCacheOptions()),
            NullLogger<ProviderCredentialResolver>.Instance);

        var result = await sut.ResolveAsync("deepseek", CancellationToken.None);

        result.Should().Be(plaintext);
    }

    [Fact]
    public async Task ResolveAsync_NoDbButEnvVar_ReturnsEnvValue()
    {
        Environment.SetEnvironmentVariable("DEEPSEEK_API_KEY", "sk-from-env");
        try
        {
            var repo = Substitute.For<IProviderCredentialRepository>();
            repo.GetActiveAsync("deepseek", Arg.Any<CancellationToken>())
                .Returns(Task.FromResult<ProviderCredential?>(null));

            var sut = new ProviderCredentialResolver(repo,
                Substitute.For<IDataProtectionProvider>(),
                new MemoryCache(new MemoryCacheOptions()),
                NullLogger<ProviderCredentialResolver>.Instance);

            var result = await sut.ResolveAsync("deepseek", CancellationToken.None);

            result.Should().Be("sk-from-env");
        }
        finally
        {
            Environment.SetEnvironmentVariable("DEEPSEEK_API_KEY", null);
        }
    }

    [Fact]
    public async Task ResolveAsync_NeitherConfigured_Throws()
    {
        Environment.SetEnvironmentVariable("DEEPSEEK_API_KEY", null);
        var repo = Substitute.For<IProviderCredentialRepository>();
        repo.GetActiveAsync("deepseek", Arg.Any<CancellationToken>())
            .Returns(Task.FromResult<ProviderCredential?>(null));

        var sut = new ProviderCredentialResolver(repo,
            Substitute.For<IDataProtectionProvider>(),
            new MemoryCache(new MemoryCacheOptions()),
            NullLogger<ProviderCredentialResolver>.Instance);

        var act = async () => await sut.ResolveAsync("deepseek", CancellationToken.None);

        await act.Should().ThrowAsync<ProviderCredentialNotConfiguredException>();
    }

    [Fact]
    public async Task Invalidate_RemovesFromCache_NextCallHitsRepoAgain()
    {
        var credential = CreateActiveCredential("deepseek", "cipher-1");
        var repo = Substitute.For<IProviderCredentialRepository>();
        repo.GetActiveAsync("deepseek", Arg.Any<CancellationToken>())
            .Returns(Task.FromResult<ProviderCredential?>(credential));

        var protector = Substitute.For<IDataProtector>();
        protector.Unprotect("cipher-1").Returns("plain-1");
        var provider = Substitute.For<IDataProtectionProvider>();
        provider.CreateProtector("ProviderCredentials").Returns(protector);

        var sut = new ProviderCredentialResolver(repo, provider,
            new MemoryCache(new MemoryCacheOptions()),
            NullLogger<ProviderCredentialResolver>.Instance);

        await sut.ResolveAsync("deepseek", CancellationToken.None);   // hit
        await sut.ResolveAsync("deepseek", CancellationToken.None);   // cached → repo NOT called twice yet

        await repo.Received(1).GetActiveAsync("deepseek", Arg.Any<CancellationToken>());

        sut.Invalidate("deepseek");
        await sut.ResolveAsync("deepseek", CancellationToken.None);

        await repo.Received(2).GetActiveAsync("deepseek", Arg.Any<CancellationToken>());
    }

    private static ProviderCredential CreateActiveCredential(string name, string ciphertext)
    {
        return ProviderCredential.Create(
            ProviderName.Create(name),
            ciphertext,
            KeyFingerprint.FromPlaintext("sk-de-abcd1234"),
            Guid.NewGuid(),
            null,
            new FakeTimeProvider(DateTimeOffset.UtcNow));
    }
}
```

- [ ] **Step 7.2: Run tests — expect FAIL (compile)**

- [ ] **Step 7.3: Implement `ProviderEnvVarMap`**

```csharp
// apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Services/ProviderEnvVarMap.cs
namespace Api.BoundedContexts.Administration.Infrastructure.Services;

internal static class ProviderEnvVarMap
{
    public static string For(string providerName) => providerName.ToLowerInvariant() switch
    {
        "deepseek" => "DEEPSEEK_API_KEY",
        "openrouter" => "OPENROUTER_API_KEY",
        _ => throw new ArgumentException($"Unknown provider: {providerName}", nameof(providerName))
    };
}
```

- [ ] **Step 7.4: Implement `IProviderCredentialResolver` + impl**

```csharp
// apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Services/IProviderCredentialResolver.cs
namespace Api.BoundedContexts.Administration.Infrastructure.Services;

public interface IProviderCredentialResolver
{
    Task<string> ResolveAsync(string providerName, CancellationToken ct);
    void Invalidate(string providerName);
}
```

```csharp
// apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Services/ProviderCredentialResolver.cs
using Api.BoundedContexts.Administration.Domain.Repositories;
using Api.SharedKernel.Exceptions;
using Microsoft.AspNetCore.DataProtection;
using Microsoft.Extensions.Caching.Memory;

namespace Api.BoundedContexts.Administration.Infrastructure.Services;

internal sealed class ProviderCredentialResolver : IProviderCredentialResolver
{
    private const string DataProtectionPurpose = "ProviderCredentials";
    private static readonly TimeSpan CacheTtl = TimeSpan.FromMinutes(5);

    private readonly IProviderCredentialRepository _repo;
    private readonly IDataProtectionProvider _protectionProvider;
    private readonly IMemoryCache _cache;
    private readonly ILogger<ProviderCredentialResolver> _logger;

    public ProviderCredentialResolver(
        IProviderCredentialRepository repo,
        IDataProtectionProvider protectionProvider,
        IMemoryCache cache,
        ILogger<ProviderCredentialResolver> logger)
    {
        _repo = repo;
        _protectionProvider = protectionProvider;
        _cache = cache;
        _logger = logger;
    }

    public async Task<string> ResolveAsync(string providerName, CancellationToken ct)
    {
        ArgumentException.ThrowIfNullOrWhiteSpace(providerName);
        var normalized = providerName.ToLowerInvariant();

        if (_cache.TryGetValue(CacheKey(normalized), out string? cached) && cached is not null)
            return cached;

        var active = await _repo.GetActiveAsync(normalized, ct).ConfigureAwait(false);
        if (active is not null)
        {
            var protector = _protectionProvider.CreateProtector(DataProtectionPurpose);
            var plaintext = protector.Unprotect(active.EncryptedApiKey);
            _cache.Set(CacheKey(normalized), plaintext, CacheTtl);
            return plaintext;
        }

        var envVar = ProviderEnvVarMap.For(normalized);
        var envValue = Environment.GetEnvironmentVariable(envVar);
        if (!string.IsNullOrWhiteSpace(envValue))
        {
            _cache.Set(CacheKey(normalized), envValue, CacheTtl);
            return envValue;
        }

        _logger.LogWarning(
            "ProviderCredentialResolver: no DB row and no {EnvVar} env var for provider '{Provider}'",
            envVar, normalized);
        throw new ProviderCredentialNotConfiguredException(normalized);
    }

    public void Invalidate(string providerName)
    {
        ArgumentException.ThrowIfNullOrWhiteSpace(providerName);
        _cache.Remove(CacheKey(providerName.ToLowerInvariant()));
        _logger.LogInformation(
            "ProviderCredentialResolver: cache invalidated for provider '{Provider}'", providerName);
    }

    private static string CacheKey(string normalized) => $"provider_cred:{normalized}";
}
```

- [ ] **Step 7.5: Run tests — expect PASS**

```powershell
dotnet test ../../tests/Api.Tests/Api.Tests.csproj --filter "FullyQualifiedName~ProviderCredentialResolverTests" --nologo
```

- [ ] **Step 7.6: Commit**

```bash
git add apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Services/ apps/api/tests/Api.Tests/BoundedContexts/Administration/Infrastructure/ProviderCredentialResolverTests.cs apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Repositories/ProviderCredentialRepository.cs
git commit -m "feat(admin-providers): #1859 IProviderCredentialResolver DB→env-var fallback with cache

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Phase 5 — Cache Invalidation (Redis pub/sub)

### Task 8: Publisher + Subscriber HostedService + Event Handler

**Files:**
- Create: `apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Services/IProviderCacheInvalidator.cs`
- Create: `apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Services/RedisProviderCacheInvalidator.cs`
- Create: `apps/api/src/Api/BoundedContexts/Administration/Infrastructure/HostedServices/ProviderCacheInvalidationSubscriber.cs`
- Create: `apps/api/src/Api/BoundedContexts/Administration/Application/EventHandlers/ProviderKeyRotatedEventHandler.cs`

- [ ] **Step 8.1: Implement `IProviderCacheInvalidator`**

```csharp
// apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Services/IProviderCacheInvalidator.cs
namespace Api.BoundedContexts.Administration.Infrastructure.Services;

public interface IProviderCacheInvalidator
{
    Task PublishInvalidationAsync(string providerName, CancellationToken ct);
}
```

- [ ] **Step 8.2: Implement `RedisProviderCacheInvalidator`**

```csharp
// apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Services/RedisProviderCacheInvalidator.cs
using StackExchange.Redis;

namespace Api.BoundedContexts.Administration.Infrastructure.Services;

internal sealed class RedisProviderCacheInvalidator : IProviderCacheInvalidator
{
    internal const string ChannelName = "provider:cache-invalidate";

    private readonly IConnectionMultiplexer _redis;
    private readonly ILogger<RedisProviderCacheInvalidator> _logger;

    public RedisProviderCacheInvalidator(
        IConnectionMultiplexer redis,
        ILogger<RedisProviderCacheInvalidator> logger)
    {
        _redis = redis;
        _logger = logger;
    }

    public async Task PublishInvalidationAsync(string providerName, CancellationToken ct)
    {
        ArgumentException.ThrowIfNullOrWhiteSpace(providerName);
        var subscriber = _redis.GetSubscriber();
        var count = await subscriber.PublishAsync(
            RedisChannel.Literal(ChannelName), providerName.ToLowerInvariant()).ConfigureAwait(false);

        _logger.LogInformation(
            "Published provider cache invalidation for '{Provider}' to {SubscriberCount} subscriber(s)",
            providerName, count);
    }
}
```

- [ ] **Step 8.3: Implement `ProviderCacheInvalidationSubscriber` HostedService**

```csharp
// apps/api/src/Api/BoundedContexts/Administration/Infrastructure/HostedServices/ProviderCacheInvalidationSubscriber.cs
using Api.BoundedContexts.Administration.Infrastructure.Services;
using Microsoft.Extensions.Hosting;
using StackExchange.Redis;

namespace Api.BoundedContexts.Administration.Infrastructure.HostedServices;

internal sealed class ProviderCacheInvalidationSubscriber : IHostedService
{
    private readonly IConnectionMultiplexer _redis;
    private readonly IServiceScopeFactory _scopeFactory;
    private readonly ILogger<ProviderCacheInvalidationSubscriber> _logger;
    private ChannelMessageQueue? _queue;

    public ProviderCacheInvalidationSubscriber(
        IConnectionMultiplexer redis,
        IServiceScopeFactory scopeFactory,
        ILogger<ProviderCacheInvalidationSubscriber> logger)
    {
        _redis = redis;
        _scopeFactory = scopeFactory;
        _logger = logger;
    }

    public async Task StartAsync(CancellationToken cancellationToken)
    {
        var subscriber = _redis.GetSubscriber();
        _queue = await subscriber.SubscribeAsync(
            RedisChannel.Literal(RedisProviderCacheInvalidator.ChannelName)).ConfigureAwait(false);

        _queue.OnMessage(message =>
        {
            var providerName = message.Message.ToString();
            if (string.IsNullOrWhiteSpace(providerName))
                return;

            using var scope = _scopeFactory.CreateScope();
            var resolver = scope.ServiceProvider.GetRequiredService<IProviderCredentialResolver>();
            resolver.Invalidate(providerName);
            _logger.LogInformation(
                "Received cache invalidation for provider '{Provider}'", providerName);
        });

        _logger.LogInformation(
            "Subscribed to Redis channel '{Channel}' for provider cache invalidation",
            RedisProviderCacheInvalidator.ChannelName);
    }

    public async Task StopAsync(CancellationToken cancellationToken)
    {
        if (_queue is not null)
        {
            await _queue.UnsubscribeAsync().ConfigureAwait(false);
            _queue = null;
        }
    }
}
```

- [ ] **Step 8.4: Implement event handler**

```csharp
// apps/api/src/Api/BoundedContexts/Administration/Application/EventHandlers/ProviderKeyRotatedEventHandler.cs
using Api.BoundedContexts.Administration.Domain.Aggregates.ProviderCredentials.Events;
using Api.BoundedContexts.Administration.Infrastructure.Services;
using MediatR;

namespace Api.BoundedContexts.Administration.Application.EventHandlers;

internal sealed class ProviderKeyRotatedEventHandler : INotificationHandler<ProviderKeyRotatedEvent>
{
    private readonly IProviderCacheInvalidator _invalidator;
    private readonly ILogger<ProviderKeyRotatedEventHandler> _logger;

    public ProviderKeyRotatedEventHandler(
        IProviderCacheInvalidator invalidator,
        ILogger<ProviderKeyRotatedEventHandler> logger)
    {
        _invalidator = invalidator;
        _logger = logger;
    }

    public async Task Handle(ProviderKeyRotatedEvent notification, CancellationToken cancellationToken)
    {
        try
        {
            await _invalidator.PublishInvalidationAsync(notification.ProviderName, cancellationToken)
                .ConfigureAwait(false);
        }
#pragma warning disable CA1031 // event handler must not break the rotation tx
        catch (Exception ex)
#pragma warning restore CA1031
        {
            _logger.LogError(ex,
                "Failed to publish cache invalidation for provider '{Provider}' after rotation",
                notification.ProviderName);
        }
    }
}
```

- [ ] **Step 8.5: Commit**

```bash
git add apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Services/IProviderCacheInvalidator.cs apps/api/src/Api/BoundedContexts/Administration/Infrastructure/Services/RedisProviderCacheInvalidator.cs apps/api/src/Api/BoundedContexts/Administration/Infrastructure/HostedServices/ProviderCacheInvalidationSubscriber.cs apps/api/src/Api/BoundedContexts/Administration/Application/EventHandlers/ProviderKeyRotatedEventHandler.cs
git commit -m "feat(admin-providers): #1859 Redis pub/sub cross-pod cache invalidation

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Phase 6 — Application Layer (DTOs + Command + Validator + Handler)

### Task 9: Request/Response DTOs + Command + Validator

**Files:**
- Create: `apps/api/src/Api/BoundedContexts/Administration/Application/DTOs/RotateProviderKeyRequestDto.cs`
- Create: `apps/api/src/Api/BoundedContexts/Administration/Application/DTOs/RotateProviderKeyResponseDto.cs`
- Create: `apps/api/src/Api/BoundedContexts/Administration/Application/Commands/Providers/RotateProviderKeyCommand.cs`
- Create: `apps/api/src/Api/BoundedContexts/Administration/Application/Commands/Providers/RotateProviderKeyCommandValidator.cs`
- Test: `apps/api/tests/Api.Tests/BoundedContexts/Administration/Application/RotateProviderKeyCommandValidatorTests.cs`

- [ ] **Step 9.1: Implement DTOs**

```csharp
// apps/api/src/Api/BoundedContexts/Administration/Application/DTOs/RotateProviderKeyRequestDto.cs
namespace Api.BoundedContexts.Administration.Application.DTOs;

public sealed record RotateProviderKeyRequestDto(
    string NewApiKey,
    string ConfirmedProviderName);
```

```csharp
// apps/api/src/Api/BoundedContexts/Administration/Application/DTOs/RotateProviderKeyResponseDto.cs
namespace Api.BoundedContexts.Administration.Application.DTOs;

public sealed record RotateProviderKeyResponseDto(
    string ProviderName,
    string NewKeyFingerprint,
    DateTime RotatedAt,
    DateTime PreviousKeyDisabledAt);
```

- [ ] **Step 9.2: Implement Command**

```csharp
// apps/api/src/Api/BoundedContexts/Administration/Application/Commands/Providers/RotateProviderKeyCommand.cs
using Api.BoundedContexts.Administration.Application.Attributes;
using Api.BoundedContexts.Administration.Application.Behaviors;
using Api.BoundedContexts.Administration.Application.DTOs;
using Api.BoundedContexts.Authentication.Application.Attributes;
using MediatR;

namespace Api.BoundedContexts.Administration.Application.Commands.Providers;

[AuditableAction(
    Action = "ProviderKeyRotated",
    Resource = "provider_credentials",
    Level = 3)]
[AtomicAudit]
[RequireTwoFactor(
    MaxAgeMinutes = 5,
    ForceStrict = true,
    Reason = "Provider key rotation requires fresh 2FA")]
internal sealed record RotateProviderKeyCommand(
    string ProviderName,
    string NewApiKey,
    string ConfirmedProviderName,
    Guid RequestingUserId) : IRequest<RotateProviderKeyResponseDto>;
```

(Verify exact `AuditableActionAttribute.Level` property name — check `apps/api/src/Api/BoundedContexts/Administration/Application/Attributes/AuditableActionAttribute.cs` before commit. Adapt if name differs.)

- [ ] **Step 9.3: Write validator tests**

```csharp
// apps/api/tests/Api.Tests/BoundedContexts/Administration/Application/RotateProviderKeyCommandValidatorTests.cs
using Api.BoundedContexts.Administration.Application.Commands.Providers;
using FluentAssertions;
using FluentValidation.TestHelper;
using Xunit;

namespace Api.Tests.BoundedContexts.Administration.Application;

[Trait("Category", "Unit")]
public sealed class RotateProviderKeyCommandValidatorTests
{
    private readonly RotateProviderKeyCommandValidator _sut = new();

    private static RotateProviderKeyCommand ValidCommand(
        string provider = "deepseek",
        string newKey = "sk-deepseek-newkey-12345",
        string? confirmed = null,
        Guid? actor = null) =>
        new(provider, newKey, confirmed ?? provider, actor ?? Guid.NewGuid());

    [Fact]
    public void Validate_AllValid_NoErrors() =>
        _sut.TestValidate(ValidCommand()).ShouldNotHaveAnyValidationErrors();

    [Theory]
    [InlineData("")]
    [InlineData(" ")]
    [InlineData("ollama")]
    public void Validate_InvalidProviderName_Errors(string provider) =>
        _sut.TestValidate(ValidCommand(provider: provider, confirmed: provider))
            .ShouldHaveValidationErrorFor(x => x.ProviderName);

    [Fact]
    public void Validate_ConfirmedMismatch_Errors() =>
        _sut.TestValidate(ValidCommand(provider: "deepseek", confirmed: "openrouter"))
            .ShouldHaveValidationErrorFor(x => x.ConfirmedProviderName);

    [Theory]
    [InlineData("")]
    [InlineData("short")]                            // <10
    [InlineData("has whitespace inside")]
    public void Validate_InvalidNewApiKey_Errors(string key) =>
        _sut.TestValidate(ValidCommand(newKey: key))
            .ShouldHaveValidationErrorFor(x => x.NewApiKey);

    [Fact]
    public void Validate_ApiKeyTooLong_Errors() =>
        _sut.TestValidate(ValidCommand(newKey: new string('a', 513)))
            .ShouldHaveValidationErrorFor(x => x.NewApiKey);

    [Fact]
    public void Validate_EmptyRequestingUserId_Errors() =>
        _sut.TestValidate(ValidCommand(actor: Guid.Empty))
            .ShouldHaveValidationErrorFor(x => x.RequestingUserId);
}
```

- [ ] **Step 9.4: Implement validator**

```csharp
// apps/api/src/Api/BoundedContexts/Administration/Application/Commands/Providers/RotateProviderKeyCommandValidator.cs
using Api.BoundedContexts.Administration.Domain.Aggregates.ProviderCredentials;
using FluentValidation;

namespace Api.BoundedContexts.Administration.Application.Commands.Providers;

internal sealed class RotateProviderKeyCommandValidator
    : AbstractValidator<RotateProviderKeyCommand>
{
    private const int MinApiKeyLength = 10;
    private const int MaxApiKeyLength = 512;

    public RotateProviderKeyCommandValidator()
    {
        RuleFor(x => x.ProviderName).Cascade(CascadeMode.Stop)
            .NotEmpty()
            .Must(p => !string.IsNullOrWhiteSpace(p) && ProviderName.Allowed.Contains(p))
                .WithMessage($"Provider must be one of: {string.Join(", ", ProviderName.Allowed)}");

        RuleFor(x => x.ConfirmedProviderName)
            .Equal(x => x.ProviderName)
                .WithMessage("Provider name confirmation does not match (typo guard)");

        RuleFor(x => x.NewApiKey).Cascade(CascadeMode.Stop)
            .NotEmpty()
            .MinimumLength(MinApiKeyLength)
            .MaximumLength(MaxApiKeyLength)
            .Must(NotContainWhitespace)
                .WithMessage("API key must not contain whitespace");

        RuleFor(x => x.RequestingUserId)
            .NotEqual(Guid.Empty)
                .WithMessage("RequestingUserId required");
    }

    private static bool NotContainWhitespace(string s) =>
        !string.IsNullOrEmpty(s) && !s.Any(char.IsWhiteSpace);
}
```

- [ ] **Step 9.5: Run validator tests — PASS**

```powershell
dotnet test ../../tests/Api.Tests/Api.Tests.csproj --filter "FullyQualifiedName~RotateProviderKeyCommandValidatorTests" --nologo
```

- [ ] **Step 9.6: Commit**

```bash
git add apps/api/src/Api/BoundedContexts/Administration/Application/Commands/Providers/ apps/api/src/Api/BoundedContexts/Administration/Application/DTOs/RotateProviderKey*.cs apps/api/tests/Api.Tests/BoundedContexts/Administration/Application/RotateProviderKeyCommandValidatorTests.cs
git commit -m "feat(admin-providers): #1859 RotateProviderKeyCommand + DTOs + validator

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 10: Command Handler

**Files:**
- Create: `apps/api/src/Api/BoundedContexts/Administration/Application/Commands/Providers/RotateProviderKeyCommandHandler.cs`
- Test: `apps/api/tests/Api.Tests/BoundedContexts/Administration/Application/RotateProviderKeyCommandHandlerTests.cs`

- [ ] **Step 10.1: Write failing handler tests**

```csharp
// apps/api/tests/Api.Tests/BoundedContexts/Administration/Application/RotateProviderKeyCommandHandlerTests.cs
// Scenarios:
// - HappyPath_PersistsNewActive_DeactivatesOld
// - RecentRotation_ThrowsRateLimitConflict
// - ProbeFailure_ThrowsProviderProbeFailed_OldKeyPreserved
// - NonSuperAdmin_ThrowsForbidden
// - FirstRotation_NoPreviousCredential
// (Use NSubstitute mocks for: IProviderCredentialRepository, IProviderProbeExecutorFactory,
//  IDataProtectionProvider, IUserRepository, TimeProvider via FakeTimeProvider)
```

Full code follows the same skeleton as `ImpersonationStartCommandHandlerTests` (see `apps/api/tests/Api.Tests/BoundedContexts/Administration/`). Setup mocks, set expectations on what handler should call (probe, encrypt, repo.AddAsync, repo.SaveChangesAsync), assert state.

- [ ] **Step 10.2: Run tests — expect FAIL (compile)**

- [ ] **Step 10.3: Implement handler**

```csharp
// apps/api/src/Api/BoundedContexts/Administration/Application/Commands/Providers/RotateProviderKeyCommandHandler.cs
using Api.BoundedContexts.Administration.Application.DTOs;
using Api.BoundedContexts.Administration.Domain.Aggregates.ProviderCredentials;
using Api.BoundedContexts.Administration.Domain.Repositories;
using Api.BoundedContexts.Authentication.Infrastructure.Persistence;
using Api.Middleware.Exceptions;
using Api.Services.Providers.Probe;
using Api.SharedKernel.Exceptions;
using MediatR;
using Microsoft.AspNetCore.DataProtection;
using Microsoft.Extensions.Logging;

namespace Api.BoundedContexts.Administration.Application.Commands.Providers;

internal sealed class RotateProviderKeyCommandHandler
    : IRequestHandler<RotateProviderKeyCommand, RotateProviderKeyResponseDto>
{
    private static readonly TimeSpan RotationCooldown = TimeSpan.FromHours(24);
    private const string DataProtectionPurpose = "ProviderCredentials";

    private readonly IProviderCredentialRepository _repo;
    private readonly IUserRepository _userRepository;
    private readonly IProviderProbeExecutorFactory _probeFactory;
    private readonly IDataProtectionProvider _protectionProvider;
    private readonly TimeProvider _timeProvider;
    private readonly ILogger<RotateProviderKeyCommandHandler> _logger;

    public RotateProviderKeyCommandHandler(
        IProviderCredentialRepository repo,
        IUserRepository userRepository,
        IProviderProbeExecutorFactory probeFactory,
        IDataProtectionProvider protectionProvider,
        TimeProvider timeProvider,
        ILogger<RotateProviderKeyCommandHandler> logger)
    {
        _repo = repo;
        _userRepository = userRepository;
        _probeFactory = probeFactory;
        _protectionProvider = protectionProvider;
        _timeProvider = timeProvider;
        _logger = logger;
    }

    public async Task<RotateProviderKeyResponseDto> Handle(
        RotateProviderKeyCommand command,
        CancellationToken cancellationToken)
    {
        ArgumentNullException.ThrowIfNull(command);
        var providerName = ProviderName.Create(command.ProviderName);

        // 1. Authz: superadmin only
        var requester = await _userRepository.GetByIdAsync(command.RequestingUserId, cancellationToken)
            .ConfigureAwait(false);
        if (requester is null)
            throw new NotFoundException($"User '{command.RequestingUserId}' not found");

        if (!string.Equals(requester.Role.Value, "superadmin", StringComparison.OrdinalIgnoreCase))
        {
            _logger.LogWarning(
                "SECURITY: non-superadmin {UserId} attempted to rotate provider key '{Provider}'",
                command.RequestingUserId, providerName.Value);
            throw new ForbiddenException("Only superadmins can rotate provider keys");
        }

        // 2. Rate-limit guard (1/24h per provider)
        var lastRotation = await _repo.GetLastRotationAsync(providerName.Value, cancellationToken)
            .ConfigureAwait(false);
        var now = _timeProvider.GetUtcNow().UtcDateTime;
        if (lastRotation is not null && (now - lastRotation.RotatedAt) < RotationCooldown)
        {
            var nextAllowed = lastRotation.RotatedAt.Add(RotationCooldown);
            throw new ConflictException(
                $"Provider '{providerName.Value}' was rotated less than 24h ago. Next allowed at {nextAllowed:O}");
        }

        // 3. Probe new key (pre-flight)
        var probeExecutor = _probeFactory.Get(providerName.Value);
        var probeResult = await probeExecutor.ProbeAsync(
            apiKeyOverride: command.NewApiKey,
            expectedModel: null,
            ct: cancellationToken).ConfigureAwait(false);

        if (!probeResult.IsHealthy)
        {
            _logger.LogWarning(
                "Probe failed for provider '{Provider}' with new key — rotation aborted. Reason: {Reason}",
                providerName.Value, probeResult.ErrorMessage);
            throw new ProviderProbeFailedException(providerName.Value,
                probeResult.ErrorMessage ?? "unknown");
        }

        // 4. Encrypt
        var protector = _protectionProvider.CreateProtector(DataProtectionPurpose);
        var ciphertext = protector.Protect(command.NewApiKey);
        var fingerprint = KeyFingerprint.FromPlaintext(command.NewApiKey);

        // 5. Deactivate previous + insert new (atomic via [AtomicAudit] outer transaction)
        var activePrevious = await _repo.GetActiveAsync(providerName.Value, cancellationToken)
            .ConfigureAwait(false);
        activePrevious?.Deactivate(_timeProvider);

        var newCredential = ProviderCredential.Create(
            providerName,
            ciphertext,
            fingerprint,
            command.RequestingUserId,
            activePrevious?.Id,
            _timeProvider);

        await _repo.AddAsync(newCredential, cancellationToken).ConfigureAwait(false);
        await _repo.SaveChangesAsync(cancellationToken).ConfigureAwait(false);

        _logger.LogInformation(
            "Provider '{Provider}' key rotated by superadmin {UserId}. New fingerprint: {Fingerprint}",
            providerName.Value, command.RequestingUserId, fingerprint.Value);

        return new RotateProviderKeyResponseDto(
            ProviderName: providerName.Value,
            NewKeyFingerprint: fingerprint.Value,
            RotatedAt: newCredential.RotatedAt,
            PreviousKeyDisabledAt: newCredential.RotatedAt);
    }
}
```

- [ ] **Step 10.4: Run tests — expect PASS**

```powershell
dotnet test ../../tests/Api.Tests/Api.Tests.csproj --filter "FullyQualifiedName~RotateProviderKeyCommandHandlerTests" --nologo
```

(Note: `IProviderProbeExecutor.ProbeAsync(apiKeyOverride, expectedModel, ct)` signature may need to be added — current signature reads key from env-var. Check `apps/api/src/Api/Services/Providers/Probe/IProviderProbeExecutor.cs` before this task and adapt: add overload that accepts explicit key. If not, add it as part of Task 10.)

- [ ] **Step 10.5: Commit**

```bash
git add apps/api/src/Api/BoundedContexts/Administration/Application/Commands/Providers/RotateProviderKeyCommandHandler.cs apps/api/tests/Api.Tests/BoundedContexts/Administration/Application/RotateProviderKeyCommandHandlerTests.cs
git commit -m "feat(admin-providers): #1859 RotateProviderKeyCommandHandler with probe + rate-limit + atomic audit

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Phase 7 — Provider Client Refactor

### Task 11: DeepSeekClient + OpenRouterClient use resolver

**Files:**
- Modify: `apps/api/src/Api/Services/LlmClients/DeepSeekClient.cs`
- Modify: `apps/api/src/Api/Services/LlmClients/OpenRouterClient.cs`

- [ ] **Step 11.1: Refactor DeepSeekClient**

Read existing `DeepSeekClient.cs`. Replace constructor injection of `IConfiguration` (or `IOptions<AiProviderSettings>`) for the API key with `IProviderCredentialResolver`. Call `ResolveAsync("deepseek", ct)` per request inside `SendAsync`.

- [ ] **Step 11.2: Refactor OpenRouterClient** — analogous

- [ ] **Step 11.3: Verify existing client tests pass**

```powershell
dotnet test ../../tests/Api.Tests/Api.Tests.csproj --filter "FullyQualifiedName~DeepSeekClient|FullyQualifiedName~OpenRouterClient" --nologo
```

Update mocks in failing tests to inject `IProviderCredentialResolver` mock returning fake key.

- [ ] **Step 11.4: Commit**

```bash
git add apps/api/src/Api/Services/LlmClients/DeepSeekClient.cs apps/api/src/Api/Services/LlmClients/OpenRouterClient.cs apps/api/tests/Api.Tests/Services/LlmClients/
git commit -m "feat(admin-providers): #1859 DeepSeek + OpenRouter clients use IProviderCredentialResolver

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Phase 8 — Endpoint + DI + Rate-Limit

### Task 12: Endpoint registration + DI + rate-limit policy + exception mapping

**Files:**
- Modify: `apps/api/src/Api/Routing/AdminProviderEndpoints.cs`
- Modify: `apps/api/src/Api/Extensions/InfrastructureServiceExtensions.cs`
- Modify: `apps/api/src/Api/Extensions/RateLimitingServiceExtensions.cs`
- Modify: `apps/api/src/Api/Middleware/ApiExceptionHandlerMiddleware.cs`

- [ ] **Step 12.1: Add rate-limit policy**

In `RateLimitingServiceExtensions`:

```csharp
options.AddPolicy("AdminProviderRotateKey", httpContext =>
{
    var providerName = httpContext.Request.RouteValues["name"]?.ToString()?.ToLowerInvariant() ?? "unknown";
    var userId = httpContext.User.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier)?.Value ?? "anon";
    return RateLimitPartition.GetFixedWindowLimiter(
        partitionKey: $"rotate:{providerName}:{userId}",
        factory: _ => new System.Threading.RateLimiting.FixedWindowRateLimiterOptions
        {
            PermitLimit = 1,
            Window = TimeSpan.FromHours(24),
            QueueProcessingOrder = System.Threading.RateLimiting.QueueProcessingOrder.OldestFirst,
            QueueLimit = 0
        });
});
```

- [ ] **Step 12.2: DI registration**

In `InfrastructureServiceExtensions.AddCoreServices` (or appropriate method):

```csharp
services.AddDataProtection();   // if not already registered
services.AddMemoryCache();      // if not already registered

services.AddScoped<IProviderCredentialRepository, ProviderCredentialRepository>();
services.AddSingleton<IProviderCredentialResolver, ProviderCredentialResolver>();
services.AddSingleton<IProviderCacheInvalidator, RedisProviderCacheInvalidator>();
services.AddHostedService<ProviderCacheInvalidationSubscriber>();
```

- [ ] **Step 12.3: Endpoint registration**

In `AdminProviderEndpoints.cs` after the existing `MapPost("/{name}/probe", …)` block:

```csharp
group.MapPost("/{name}/rotate-key", async (
        string name,
        RotateProviderKeyRequestDto body,
        IMediator mediator,
        ClaimsPrincipal user,
        CancellationToken ct) =>
    {
        var actorIdClaim = user.FindFirst(ClaimTypes.NameIdentifier)?.Value;
        if (!Guid.TryParse(actorIdClaim, out var actorId))
            return Results.Unauthorized();

        var result = await mediator.Send(new RotateProviderKeyCommand(
            ProviderName: name,
            NewApiKey: body.NewApiKey,
            ConfirmedProviderName: body.ConfirmedProviderName,
            RequestingUserId: actorId), ct).ConfigureAwait(false);

        return Results.Ok(result);
    })
    .RequireAuthorization("RequireSuperAdmin")
    .RequireRateLimiting("AdminProviderRotateKey")
    .WithOpenApi();
```

- [ ] **Step 12.4: Exception mapping**

In `ApiExceptionHandlerMiddleware`, map `ProviderProbeFailedException` → HTTP 502 with subcode `provider_probe_failed`; map `RateLimitExceededException` (if introduced) → HTTP 429 (rate-limit policy handles it normally; only handler-level conflict throws `ConflictException` → 409).

- [ ] **Step 12.5: Commit**

```bash
git add apps/api/src/Api/Routing/AdminProviderEndpoints.cs apps/api/src/Api/Extensions/InfrastructureServiceExtensions.cs apps/api/src/Api/Extensions/RateLimitingServiceExtensions.cs apps/api/src/Api/Middleware/ApiExceptionHandlerMiddleware.cs
git commit -m "feat(admin-providers): #1859 POST /admin/providers/{name}/rotate-key endpoint + DI

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Phase 9 — Integration Tests

### Task 13: Integration tests (happy path + all failure modes)

**Files:**
- Create: `apps/api/tests/Api.Tests/Integration/Administration/RotateProviderKeyEndpointIntegrationTests.cs`

- [ ] **Step 13.1: Write integration tests using Testcontainers Postgres**

```csharp
// Pattern: extend IntegrationTestBase, use HttpClient to POST endpoint, assert DB state + audit log.
// Cover:
// - HappyPath_Returns200_PersistsRow_DeactivatesOld_EmitsAudit
// - NoStepUp_Returns401_StepUpRequired
// - NonSuperAdmin_Returns403
// - ProviderNameMismatch_Returns400
// - InvalidProvider_Returns400
// - RateLimit_LastRotationWithin24h_Returns409
// - ProbeFailure_Returns502_NoRowPersisted
// - AuditOutboxContainsExpectedDetails
```

Use existing patterns from `ImpersonationStartEndpointIntegrationTests.cs`.

- [ ] **Step 13.2: Run integration tests — expect PASS**

```powershell
dotnet test ../../tests/Api.Tests/Api.Tests.csproj --filter "FullyQualifiedName~RotateProviderKeyEndpoint" --nologo
```

- [ ] **Step 13.3: Commit**

```bash
git add apps/api/tests/Api.Tests/Integration/Administration/RotateProviderKeyEndpointIntegrationTests.cs
git commit -m "test(admin-providers): #1859 integration tests for rotate-key endpoint (8 scenarios)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Phase 10 — FE: StepUpTwoFactorModal (net-new)

### Task 14: `StepUpTwoFactorModal` component + hook

**Files:**
- Create: `apps/web/src/components/auth/StepUpTwoFactorModal.tsx`
- Create: `apps/web/src/hooks/queries/useStepUpTwoFactor.ts`
- Create: `apps/web/src/components/auth/__tests__/StepUpTwoFactorModal.test.tsx`

- [ ] **Step 14.1: Implement `useStepUpTwoFactor` hook**

```typescript
// apps/web/src/hooks/queries/useStepUpTwoFactor.ts
import { useMutation } from '@tanstack/react-query';
import { fetchJson } from '@/lib/api/fetch';

export interface StepUpTwoFactorPayload {
  code: string;
}

export function useStepUpTwoFactor() {
  return useMutation({
    mutationFn: async (payload: StepUpTwoFactorPayload) => {
      return fetchJson<{ success: true }>('/api/v1/auth/2fa/step-up', {
        method: 'POST',
        body: JSON.stringify({ code: payload.code }),
      });
    },
  });
}
```

(Adapt to your actual `fetchJson` API and endpoint URL; check `TwoFactorEndpoints.cs` for exact path.)

- [ ] **Step 14.2: Implement `StepUpTwoFactorModal` component**

```typescript
// apps/web/src/components/auth/StepUpTwoFactorModal.tsx
'use client';

import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui/dialog';
import { TwoFactorVerification } from './TwoFactorVerification';
import { useStepUpTwoFactor } from '@/hooks/queries/useStepUpTwoFactor';
import { toast } from 'sonner';

export interface StepUpTwoFactorModalProps {
  readonly isOpen: boolean;
  readonly onClose: () => void;
  readonly onSuccess: () => void;
  readonly reason?: string;
}

export function StepUpTwoFactorModal({
  isOpen,
  onClose,
  onSuccess,
  reason,
}: StepUpTwoFactorModalProps) {
  const stepUp = useStepUpTwoFactor();

  const handleVerify = async ({ code }: { code: string }) => {
    try {
      await stepUp.mutateAsync({ code });
      toast.success('Verifica 2FA completata');
      onSuccess();
    } catch {
      // error surfaced by stepUp.error
    }
  };

  return (
    <Dialog open={isOpen} onOpenChange={(open) => !open && onClose()}>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>Verifica 2FA richiesta</DialogTitle>
        </DialogHeader>
        {reason && <p className="text-sm text-muted-foreground">{reason}</p>}
        <TwoFactorVerification
          onVerify={handleVerify}
          loading={stepUp.isPending}
          error={stepUp.error?.message}
          onCancel={onClose}
          autoSubmit
          showRememberDevice={false}
        />
      </DialogContent>
    </Dialog>
  );
}
```

- [ ] **Step 14.3: Write tests**

```typescript
// apps/web/src/components/auth/__tests__/StepUpTwoFactorModal.test.tsx
// Test:
// - Renders TwoFactorVerification when isOpen
// - Calls onSuccess on successful verify
// - Closes on cancel
// - Shows error from mutation
```

- [ ] **Step 14.4: Run tests — PASS**

```bash
cd apps/web
pnpm test -- StepUpTwoFactorModal
```

- [ ] **Step 14.5: Commit**

```bash
git add apps/web/src/components/auth/StepUpTwoFactorModal.tsx apps/web/src/hooks/queries/useStepUpTwoFactor.ts apps/web/src/components/auth/__tests__/StepUpTwoFactorModal.test.tsx
git commit -m "feat(auth): #1859 StepUpTwoFactorModal net-new + useStepUpTwoFactor hook

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Phase 11 — FE: RotateKeyModal wire to BE

### Task 15: `useRotateProviderKey` hook + RotateKeyModal refactor

**Files:**
- Create: `apps/web/src/hooks/queries/useRotateProviderKey.ts`
- Modify: `apps/web/src/components/admin/providers/RotateKeyModal.tsx`
- Create: `apps/web/src/components/admin/providers/__tests__/RotateKeyModal.test.tsx`
- Modify: `apps/web/src/lib/api/schemas/providers.ts` — add rotate types

- [ ] **Step 15.1: Add types**

```typescript
// apps/web/src/lib/api/schemas/providers.ts (add)
export interface RotateProviderKeyRequest {
  newApiKey: string;
  confirmedProviderName: string;
}

export interface RotateProviderKeyResponse {
  providerName: string;
  newKeyFingerprint: string;
  rotatedAt: string;
  previousKeyDisabledAt: string;
}
```

- [ ] **Step 15.2: Implement hook**

```typescript
// apps/web/src/hooks/queries/useRotateProviderKey.ts
import { useMutation, useQueryClient } from '@tanstack/react-query';
import { fetchJson } from '@/lib/api/fetch';
import type { ProviderName, RotateProviderKeyRequest, RotateProviderKeyResponse }
  from '@/lib/api/schemas/providers';

export function useRotateProviderKey(providerName: ProviderName) {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: async (req: RotateProviderKeyRequest) => {
      return fetchJson<RotateProviderKeyResponse>(
        `/api/v1/admin/providers/${providerName}/rotate-key`,
        { method: 'POST', body: JSON.stringify(req) });
    },
    onSuccess: () => qc.invalidateQueries({ queryKey: ['providers'] }),
  });
}
```

- [ ] **Step 15.3: Refactor RotateKeyModal**

Replace existing `RotateKeyModal.tsx` (currently disabled button). Build a Dialog with:
- typed-confirm input (`confirmedProviderName`)
- password input (`newApiKey`)
- form submit triggers `useRotateProviderKey` mutation
- on 401 `step_up_required`: open `StepUpTwoFactorModal`, on stepUp success → retry rotation
- on success: toast + fingerprint display + close
- on error: surface message based on subcode

(Code-block too long for plan — implementer follows §13 of design doc.)

- [ ] **Step 15.4: Write tests**

```typescript
// Test cases:
// - Button enabled for superadmin
// - Button disabled for non-superadmin (existing behavior preserved)
// - Submit triggers mutation
// - Step-up required modal opens on 401
// - Success toast shown
// - Typed-confirm validation prevents submit on mismatch
```

- [ ] **Step 15.5: Run tests — PASS**

```bash
pnpm test -- RotateKeyModal
```

- [ ] **Step 15.6: Commit**

```bash
git add apps/web/src/components/admin/providers/RotateKeyModal.tsx apps/web/src/hooks/queries/useRotateProviderKey.ts apps/web/src/lib/api/schemas/providers.ts apps/web/src/components/admin/providers/__tests__/RotateKeyModal.test.tsx
git commit -m "feat(admin-providers): #1859 wire RotateKeyModal to BE rotate-key endpoint

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Phase 12 — E2E

### Task 16: Playwright E2E happy path

**Files:**
- Create: `apps/web/e2e/admin-providers-rotate-key.spec.ts`

- [ ] **Step 16.1: Write E2E test**

```typescript
// apps/web/e2e/admin-providers-rotate-key.spec.ts
import { test, expect } from '@playwright/test';
import { loginAsSuperAdmin, completeTwoFactor } from './helpers/auth';

test.describe('Admin Providers - Rotate Key', () => {
  test('happy path: superadmin rotates DeepSeek key with step-up', async ({ page }) => {
    await loginAsSuperAdmin(page);
    await completeTwoFactor(page);   // ensures fresh TOTP

    await page.goto('/admin/providers');
    await page.getByTestId('rotate-key-button-deepseek').click();

    // Fill modal
    await page.getByLabel('Confirmed provider name').fill('deepseek');
    await page.getByLabel('New API key').fill('sk-test-mock-1234567890');
    await page.getByRole('button', { name: 'Rotate' }).click();

    // Assert success toast
    await expect(page.getByText(/Key ruotata con successo/i)).toBeVisible();

    // Verify fingerprint shown
    await expect(page.getByText(/sk-te..7890/)).toBeVisible();
  });
});
```

- [ ] **Step 16.2: Run E2E — PASS**

```bash
pnpm test:e2e -- admin-providers-rotate-key
```

(NOTE: needs DeepSeek probe to succeed in test env. Use a mock provider HTTP server or seed an explicit "test" provider that always returns 200.)

- [ ] **Step 16.3: Commit**

```bash
git add apps/web/e2e/admin-providers-rotate-key.spec.ts
git commit -m "test(admin-providers): #1859 E2E happy path for rotate-key flow

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Phase 13 — Verify + PR + Code Review + Close

### Task 17: Full verification + open PR

- [ ] **Step 17.1: Full backend test suite**

```powershell
cd apps/api/src/Api
dotnet test ../../tests/Api.Tests/Api.Tests.csproj --nologo
```

Expected: all pass, no new flakes added.

- [ ] **Step 17.2: Full FE test suite**

```bash
cd apps/web
pnpm test && pnpm typecheck && pnpm lint
```

- [ ] **Step 17.3: Verify coverage 90%+ BE / 85%+ FE**

```powershell
cd apps/api/src/Api
dotnet test ../../tests/Api.Tests/Api.Tests.csproj /p:CollectCoverage=true /p:Threshold=90 --nologo
```

- [ ] **Step 17.4: Push branch + open PR**

```bash
git push -u origin feature/issue-1859-be-rotate-key

gh pr create --base main-dev --title "feat(admin-providers): #1859 — BE rotate-key endpoint + DB-backed credential store" --body "$(cat <<'EOF'
## Summary
- Closes #1859
- Implements POST /api/v1/admin/providers/{name}/rotate-key with DB-backed credential store (encrypted IDataProtector), step-up 2FA (force-strict), probe pre-flight, Redis pub/sub cache invalidation, audit Level 3 automatic.
- Net-new FE `StepUpTwoFactorModal` riusando `TwoFactorVerification`.

## Design + Plan
- Design: `docs/superpowers/specs/2026-06-05-issue-1859-rotate-provider-key-design.md`
- Plan: `docs/superpowers/plans/2026-06-05-issue-1859-rotate-provider-key.md`

## Test plan
- [x] 40+ unit tests (domain VOs, validator, handler, resolver)
- [x] 8 integration tests (Testcontainers Postgres) covering 401/403/400/409/502 + happy
- [x] FE component tests (RotateKeyModal, StepUpTwoFactorModal)
- [x] E2E Playwright happy path
- [x] Coverage ≥90% BE / ≥85% FE

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 17.5: Invoke `/code-review:code-review` on the PR**

After PR opened, invoke code review with diff path target.

- [ ] **Step 17.6: Address review findings**

If BLOCKERS found, fix + commit + push. If only MINOR, document in PR comment for follow-up.

- [ ] **Step 17.7: Wait CI green + merge**

```bash
gh pr view --json statusCheckRollup
# When green:
gh pr merge --squash --auto
```

- [ ] **Step 17.8: Update issue + close**

```bash
gh issue close 1859 --reason completed --comment "Merged in PR #XXXX (commit sha). Endpoint POST /api/v1/admin/providers/{name}/rotate-key live."
```

- [ ] **Step 17.9: Local cleanup**

```bash
git checkout main-dev && git pull
git branch -D feature/issue-1859-be-rotate-key
```

---

## Self-Review (writing-plans skill)

**1. Spec coverage**:
- §2 D-1 Hybrid DB-backed → Tasks 4, 5, 7 ✅
- §2 D-2 ForceStrict step-up → Task 6 ✅
- §2 D-3 Probe pre-persist → Task 10 step 10.3 ✅
- §2 D-4 Audit Level via Details JSON → Task 9 step 9.2 (AuditableAction Level=3) + automatic via behavior ✅
- §2 D-5 No "show once" → Task 9 step 9.1 (DTO senza newKeyOnce) + Task 15 step 15.3 ✅
- §4 Domain model → Tasks 1, 2, 3 ✅
- §5 Data + migration → Task 4 ✅
- §6 Application Command + Validator + Handler → Tasks 9, 10 ✅
- §7 Infrastructure (Resolver + Repository + EnvVarMap) → Tasks 5, 7 ✅
- §8 Endpoint + Rate-limit + DTOs → Tasks 9, 12 ✅
- §9 RequireTwoFactor.ForceStrict → Task 6 ✅
- §10 Provider client refactor → Task 11 ✅
- §10.3 Redis pub/sub cache invalidation → Task 8 ✅
- §11 Audit automatic → Task 9 step 9.2 ✅
- §12 Failure modes → covered by Tasks 12 (exception mapping) + 13 (integration tests) ✅
- §13 FE wire (RotateKeyModal + StepUpTwoFactorModal) → Tasks 14, 15 ✅
- §14 Test strategy → Tasks 13, 14, 15, 16, 17.3 ✅

**2. Placeholder scan**: No "TBD" or "TODO" placeholders in step bodies. Step 10.1 references "follows the same skeleton as ImpersonationStartCommandHandlerTests" with explicit file path — acceptable per skill (referenced existing pattern with concrete location). Step 15.3 references "§13 of design doc" for the RotateKeyModal full code — acceptable as the design doc has the explicit code outline; implementer reads design then implements.

**3. Type consistency**: Names verified consistent across tasks:
- `ProviderCredential` (Task 3) used in Tasks 4, 5, 7, 8, 10 — same class
- `IProviderCredentialResolver` (Task 7) referenced in Tasks 8, 11, 12 — same interface
- `ProviderKeyRotatedEvent` (Task 3) referenced in Task 8 — same signature
- `RotateProviderKeyCommand` (Task 9) ↔ Validator (Task 9) ↔ Handler (Task 10) — same record shape
- DTO `RotateProviderKeyResponseDto(ProviderName, NewKeyFingerprint, RotatedAt, PreviousKeyDisabledAt)` consistent §6.2 design → Task 9 → Task 15 ✅

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-06-05-issue-1859-rotate-provider-key.md`. Two execution options:**

**1. Subagent-Driven (recommended)** - dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - execute tasks in this session using executing-plans, batch execution with checkpoints

**Which approach?**
