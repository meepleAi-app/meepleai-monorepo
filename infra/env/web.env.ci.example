# Continuous integration environment for MeepleAI web app.
# Configure these values via CI variables rather than committing secrets.
NEXT_PUBLIC_API_BASE=https://api-ci.example.com
NEXT_PUBLIC_TENANT_ID=ci
