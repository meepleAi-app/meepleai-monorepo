# Happy Path — Report di esecuzione

> Compilato in **Fase B** (esecuzione browser). Ordine esecuzione (spec §6): U1 → U2 → U3 → U4 → U5 → U6 → U7 → U8 → A3 → A2 → A1 → A4 → A5.
> Legenda: ✅ pass · ❌ fail · ⚠️ blocked-env · ⏭️ pending.
>
> 🔴 **Regola di verifica scritture** (dal 2026-07-12): per OGNI operazione che modifica un dato (create/edit/delete/toggle/publish/RSVP/salva-preferenze/…) verificare SEMPRE **(1)** che l'UI produca l'effetto atteso (toast / dialog-sheet chiuso / elemento aggiornato-rimosso a schermo / redirect) **E (2)** che il dato sia **realmente persistito** via **reload della pagina o GET API** — il codice `2xx` da solo **NON è prova di persistenza** (cfr. **#T** `PUT` 204 ma GET ritorna il valore originale; **#B/#D** `emailSent:true` falso positivo). Prompt di ripresa completo: [`_RESUME.md`](./_RESUME.md).

## Ambiente

| Campo | Locale | Staging |
|-------|--------|---------|
| URL | `http://localhost:3000` | `https://meepleai.app` |
| API | `http://localhost:8080` | — |
| Seed | `make seed-sp4` | `make seed-sp4-staging` |
| Stato | ⏭️ non avviato | ⏭️ non avviato |
| Data run | — | — |

## Riepilogo

| Area | Totale | ✅ | ❌ | ⚠️ | ⏭️ |
|------|--------|----|----|----|----|
| U1 Accesso | 28 | 9 | 2 | 0 | 17 |
| U2 Catalogo | 14 | 13 | 1 | 0 | 0 |
| U3 Library/KB | 25 | 14 | 8 | 1 | 2 |
| U4 Chat RAG | 17 | 10 | 3 | 4 | 0 |
| U5 Game Night | 15 | 4 | 1 | 0 | 10 |
| U6 Sessioni | 29 | 14 | 6 | 6 | 3 |
| U7 Toolkit | 18 | 15 | 0 | 3 | 0 |
| U8 Profilo | 12 | 10 | 1 | 1 | 0 |
| A1 Agenti | 29 | 0 | 0 | 0 | 29 |
| A2 KB admin | 20 | 0 | 0 | 0 | 20 |
| A3 Catalogo | 20 | 5 | 4 | 0 | 11 |
| A4 Config | 13 | 0 | 0 | 0 | 13 |
| A5 Monitoraggio | 28 | 0 | 0 | 0 | 28 |
| **Totale** | **268** | **94** | **26** | **15** | **133** |

## Issue aperte (fallimenti / finding)

> **Issue GitHub aperte 2026-07-12b** (prompt di fix in [`_FIX-PROMPTS.md`](./_FIX-PROMPTS.md)): **[#2845](https://github.com/meepleAi-app/meepleai-monorepo/issues/2845)** cluster CRUD admin (#FF/#GG/#HH) · **[#2846](https://github.com/meepleAi-app/meepleai-monorepo/issues/2846)** proxy prefix-collision (#G/#DD/#EE) · **[#2847](https://github.com/meepleAi-app/meepleai-monorepo/issues/2847)** play-record create data-loss (#BB) · **[#2848](https://github.com/meepleAi-app/meepleai-monorepo/issues/2848)** sessions/history schema (#Z) · **[#2849](https://github.com/meepleAi-app/meepleai-monorepo/issues/2849)** notification prefs no-persist (#T) · **[#2850](https://github.com/meepleAi-app/meepleai-monorepo/issues/2850)** gamebooks 500 (#M) · **[#2851](https://github.com/meepleAi-app/meepleai-monorepo/issues/2851)** toolkit private 422 (#Q). **Cleanup HP-TEST: FATTO** (2 shared-game + agente + campagna + sessione live + token, via DB — vedi `_FIX-PROMPTS.md` § c). Fix già shipped in sessioni precedenti: #R (#2838), #S (#2834), #V/#W (#2833), #F (#2809), invito (#2805).

| # | Scenario | Ambiente | Sintesi | Priorità |
|---|----------|----------|---------|----------|
| **A** | area admin (A1-A5) + role-gated (upload/editor) | dev locale | **Bypass auth dev**: `web.env.dev` con `PLAYWRIGHT_AUTH_BYPASS=true` + `NEXT_PUBLIC_VISUAL_TEST_FIXTURE_ENABLED=1` (#992 CI-smoke repro) → `proxy.ts` salta `/auth/me` → cookie ruolo V1 in sunset (>13/05/26) → ogni utente declassato a `'user'` → `/admin` rimbalza a `/library`. **Non è bug di prod** (flag assenti in prod). Fix: rebuild web senza i flag. | 🟡 ambiente (blocca testing admin) |
| **B** | U1-28 (approvazione access-request) | dev locale/codice | Approvazione richiesta accesso: crea **2 `invitation_tokens` duplicati**, ma `access_requests.status` resta **Pending** + email **non accodata** in `email_outbox`. Causa probabile: invio email **SMTP sincrono** fallisce (vedi #D/SMTP) e interrompe il flusso → **stato incoerente/non-transazionale** con token orfani. Da ri-verificare con mailpit. | 🔴 funzionalità (da confermare) |
| **C** | Invito utente — link email errato | codice (confermato) | `POST /admin/users/invite` (`SendInvitation` base, overload **senza** setup-URL) genera link **`{frontendBaseUrl}/invites/{token}`** (`EmailService.Auth.cs:327`). Ma `/invites/[token]` è la landing **RSVP game-night** (`game_night_invitations`), non gestisce `invitation_tokens` → **"Invito non trovato"**. Il link corretto è `/setup-account?token=` (overload enhanced, riga 364). **Percorso UI reale**: admin "Invita Utente" (`InviteUserDialog`) → `api.invitations.sendInvitation` → questo endpoint. Verificato via mailpit (mail ricevuta con link `/invites/`). | 🔴 funzionalità |
| **D** | Invito utente — `emailSent:true` falso positivo | dev locale (confermato) | La risposta di `/admin/users/invite` ritorna **200 + `emailSent:true`** anche quando l'invio SMTP **fallisce** (log server: *"Failed to send invitation email"*, 15:46:07). L'utente crede l'invito partito; nessuna mail arriva. Manca propagazione dell'errore d'invio al chiamante (o accodamento su outbox con retry). | 🔴 funzionalità |
| **E** | Invito utente — token irriscattabile (no pending user) | codice+DB (confermato) | `/admin/users/invite` (base) crea **solo** l'`invitation_token`, **senza** *pending user*. Il flusso `/setup-account` → `activate-account` cerca il pending user e non lo trova → **"not_found"** (DB: account **non creato**, token resta **Pending**). L'endpoint enhanced `/admin/invitations` (`ProvisionAndInvite`) crea il pending user + usa il link corretto. **Conseguenza**: un invito creato dalla UI admin è **irriscattabile** sia via `/invites` (#C) sia via `/setup-account` (#E). | 🔴 funzionalità |
| **SMTP** | Ambiente locale — email non partono | dev locale | Porta **587 in uscita bloccata** (rete/ISP): `ConnectAndHandshake` timeout ~3.6s verso `smtp.gmail.com` → nessuna email. **Fix testing (reversibile)**: `email.secret` → mailpit (`mailpit:1025`, no SSL/auth). Gmail commentato con nota. Non è bug di prodotto. | 🟡 ambiente |
| **F** | U1-06 (conferma reset password) | codice (confermato) | **Reset password rotto per tutti gli utenti**: il form "Imposta nuova password" si apre (verify OK), ma la conferma ritorna **HTTP 422**. `ResetPasswordCommandValidator` esige un token **GUID** (`Guid.TryParse`), mentre `PasswordResetService` genera un token **base64url** random 32-byte (`R6fJNEsX7k4…`, 43 char, non GUID) → ogni token reale rifiutato. Verify (GET) passa perché fa solo hash+DB lookup. Introdotto da #1449. | 🔴 funzionalità |
| **G** | U2-14 (/library-public da ospite) | codice (confermato) | Landing community **pubblica** `/library-public` **rediretta a /login** per gli ospiti. `proxy.ts:457` decide la protezione con `PROTECTED_ROUTES.some(r => pathname.startsWith(r))`; poiché `'/library-public'.startsWith('/library')` è `true`, la route viene scambiata per la dashboard protetta `/library`. La landing di acquisizione (CTA "Inizia gratis") è irraggiungibile dal suo pubblico. Fix: match boundary-aware (`=== route` o `route + '/'`). ⚠️ **Riconfermato ANCORA PRESENTE nel build 2026-07-12b** (giro ospite: `/library-public` → `/login?from=/library-public`) — il fix #2812 non è nel build corrente / non copre il caso ospite. Vedi anche **#DD** (`/library/shared`) e **#EE** (`/games/{id}`), stessa classe di proxy prefix-collision. | 🟠 funzionalità (acquisizione) |
| **H1** | U2-03 (dettaglio gioco, pannello Info) | codice (confermato) | **Chiavi i18n mancanti**: il pannello Info di `/games/[id]` (GameDetailView) referenzia `pages.gameDetail.houseRules.{title,addCta,empty}` ma queste chiavi **non esistono** in `apps/web/src/locales/*.json` → l'i18n rende il **path raw** all'utente (es. `pages.gameDetail.houseRules.title`). Visibile su **ogni** gioco. Fix candidato: mini-PR i18n a fine blocco U2. | 🟠 funzionalità/i18n (media) |
| **H2** | U2-03 (dettaglio gioco, FAQ preview) | codice (confermato) | **Interpolazione i18n mancante**: la chiave `…faqs.subtitle` = "Le domande più frequenti su {title}." esiste (`apps/web/src/locales/it.json:2883`) ma il componente non passa il param `title` → placeholder `{title}` reso letterale invece del nome gioco. Stesso pannello di #H1. | 🟡 i18n (bassa/media) |
| **I** | U3-03 (/library/wishlist da marco) | codice (confermato) | **Wishlist irraggiungibile**: `next.config.js:229` ha un redirect **301 `permanent`** legacy `/library/wishlist` → `/library?tab=wishlist`, residuo di quando la wishlist era un tab del hub. Il tab `wishlist` è stato **rimosso** (`_content.tsx:8`) ma il redirect no → l'utente atterra sul hub tab "Tutti", nessuna wishlist. La **pagina standalone** `wishlist/page.tsx` (heading "My Wishlist" + `AddToWishlistDialog` + griglia) esiste completa ma è **oscurata** dal redirect (Next redirects > page routing). Contraddizioni: `_content.tsx:9-10` dichiara "reachable as standalone route"; `contextual-tabs.ts:14` link nav "Wishlist"→`/library/wishlist` (rotto); `gap-analysis-critical.spec.ts:133` asserisce `toHaveURL('/library/wishlist')`. **Stesso identico bug su `/library/private` (riga 230 → `?tab=private`)** → PrivateGamesClient oscurato. Bloccava U3-03/04/06/23. ✅ **FIXATO + verificato**: rimosse righe 229+230 (branch `fix/library-wishlist-standalone-route`, commit `58dcf326c`); post-rebuild web `/library/wishlist`→"My Wishlist", `/library/private`→PrivateGamesClient. PR [#2818](https://github.com/meepleAi-app/meepleai-monorepo/pull/2818). | 🟠 funzionalità (media) |
| **J** | U3-15 (/knowledge-base/[id] chunk list) | codice (confermato) | **Split-view KB detail non mostra MAI i chunk**: `page.tsx:48` chiama `useKbChunks(documentId, { limit: 200 })`, ma `GET /api/v1/kb-docs/{id}/chunks` impone `limit ∈ [1,100]` → `?limit=200` ritorna **HTTP 400 "limit must be between 1 and 100"** → `chunksQuery` in errore → `chunks = data?.items ?? []` = `[]` → empty-state "Nessun chunk disponibile" nonostante 46 chunk indicizzati (verificato: `?limit=100`→200 `{items:[...]}`). Colpisce **ogni** doc KB (alta visibilità). **Fix**: `page.tsx:48` `limit:200`→`100` (o paginazione cursor per doc >100 chunk). | 🟠 funzionalità (media) |
| **K** | U3-17 (/kb/[id] shorthand redirect) | codice (confermato) | **Shorthand `/kb/[id]` → 404**: `kb/[id]/page.tsx` è un redirect thin (`redirect('/knowledge-base/${encodeURIComponent(id)}')`) ma dichiara `export const dynamic = 'force-static'` (riga 13). Su route dinamica `[id]` senza `generateStaticParams`, `force-static` + `redirect()` genera un `Location` **duplicato** (`/knowledge-base/{id}, /knowledge-base/{id}` unito da `, `) → URL malformato → **404 "Pagina non trovata"**. **Fix**: rimuovere `export const dynamic = 'force-static'` (o `'force-dynamic'`). | 🟡 funzionalità (bassa/media) |
| **L** | U3-21/U3-25 (private game edit/delete UI) | codice (confermato) | **Menu azioni card = placeholder no-op → edit/delete private game irraggiungibili**: il "⋯" (aria-label "Azioni") su ogni card `MeepleCard` è `MenuPlaceholder.tsx`, **placeholder visivo dichiarato senza handler** (`onClick={e=>e.stopPropagation()}`, `tabIndex={-1}`, commento *"No functional handler… Future issue may wire… #1856 DEC-4"*). `PrivateGamesClient.tsx` HA la logica completa (state dialog, `handleEditGame`/`handleDeleteGame`, `openEdit`/`openDelete`, passa `onEdit`/`onDelete` a `PrivateGameCard`, dialog montati) ma **mai raggiunta** perché l'unico trigger visibile (⋯) è no-op. Confermato: click manuale utente non apre nulla, console senza errori app; backend `DELETE /private-games/{id}` via API → **204 OK**. Colpisce edit/delete su tutte le ~72 card-surface con `MeepleCard`. **NON quick-fix**: richiede cablare un menu reale (feature #1856 DEC-4 pending). | 🟠 funzionalità (media) |
| **M** | U3-02 / dettaglio gioco (console) | codice+runtime (confermato) | **`GET /api/v1/gamebooks` → 500**: la lista gamebook globale lancia un errore server EF Core (`SelectExpression.ApplySetOperation`, query con set-operation malformata; `correlationId` server). Chiamata dal dettaglio di **ogni** gioco (library/games) → `[API Error]` in console ad ogni load (in aggiunta a `gamebook/books?gameRefId=…`=200, che invece funziona). Il catalogo U3-19 dichiarava l'endpoint "non ancora esposto", ma è **esposto e 500**. Companion: probe `GET /private-games/{id}` → **404** by-design (risoluzione library-vs-private su un id catalogo) logga anch'esso `[API Error]` (rumore console). Candidato **batch-fix BE** (distinto dal batch FE). | 🟠 funzionalità (media) |
| **N** | U3-22 (remove library entry via bulk-select) | codice+runtime (confermato) | **Rimozione library entry via bulk-select irraggiungibile dall'UI**: il tab "games" rende `GamesResultsGrid` (soli `<Link>`, **senza** `selectionMode`/`onLongPressEnter`); l'unico `onLongPressEnter={handleEnterSelectMode}` è cablato su `LibraryHybridGrid`, reso **solo nei tab ≠ games**, e `handleEnterSelectMode` ha il guard `if (tab !== 'games') return` (`LibraryHub.tsx:427`) → **no-op** proprio lì. `selectionMode='select'` non è attivabile da **nessun** tab → `BulkSelectionBar` + `handleBulkDelete` (già dichiarato `unreachable`, commento `#1566` a `LibraryHub.tsx:444`) mai raggiungibili. Confermato a runtime: 0 trigger di selezione, 0 checkbox, 0 BulkSelectionBar sul tab games. Backend `DELETE /api/v1/library/games/{gameId}` → **204** OK (via API). Gemello di #L. **NON quick-fix** (serve ricablare l'enter-select-mode). | 🟠 funzionalità (media) |
| **O** | U3-04/U3-23 (titolo card wishlist) | codice+runtime (confermato) | **Wishlist card non risolve il titolo del gioco** → mostra "🎲 Game {gameId[0..8]}..." invece del nome. `MeepleWishlistCard` risolve `title = gameName || item.gameName || 'Game {id}...'`; `gameName` arriva **solo** da `gameNameMap` costruita dalla **library** dell'utente (`wishlist/page.tsx:77-85`), e `WishlistItemDto` (`GET /api/v1/wishlist`, **array diretto** non paginato) **non** espone `gameTitle`/`gameImageUrl`. Un gioco in wishlist ma non in library (o rimosso) resta senza titolo. Fix: arricchire il DTO wishlist (join shared-games) o lookup catalogo lato FE. | 🟠 funzionalità/UX (media) |
| **P** | U3-23 (remove/edit wishlist) | codice+runtime (confermato) | **Remove (ed edit) wishlist irraggiungibili dall'UI**: `MeepleWishlistCard` costruisce l'azione 🗑️ Remove e `wishlist/page.tsx:135` passa `onRemove={handleRemove}` (`useRemoveFromWishlist`), MA le passa a `MeepleCard variant="list"` → `ListCard.tsx` **non estrae né rende la prop `actions`** (nessun `QuickActions`, nessun `MenuPlaceholder`; solo le variant `grid`/`featured` rendono `QuickActions`, e solo con `showQuickActions && actions.length>0`). Confermato runtime: card senza alcun button, 0 controlli remove nel DOM. Backend `DELETE /api/v1/wishlist/{id}` → **204** OK (via API). **Terzo gemello di #L (private) e #N (library)**: CRUD backend OK, superficie UI scollegata. **NON quick-fix**. | 🟠 funzionalità (media) |
| **Q** | U3-09 (crea toolkit private game) | codice+runtime (confermato) | **Creazione toolkit per private game da UI rotta (contratto FE↔BE)**: `usePrivateToolkitEditor.createToolkit` (`POST /api/v1/game-toolkits/`) invia `{privateGameId, name, overrides…}` ma il BE valida **`GameId is required`** → **422**. Aggiungere `gameId=privateGameId` → **400** (`GameToolkit..ctor` rifiuta, gameId dev'essere uno shared-game reale). Il BE `game-toolkits` è modellato su `GameId` e non gestisce il caso private-game come lo invia il FE. La UI mostra "HTTP 422: Unprocessable Entity" e il toolkit non viene creato. **Deterministico** (non ambientale). Configuratore + form nome/Crea OK; è l'azione di create a fallire. Diverso da #L/#N/#P (superficie mancante) — qui la superficie c'è ma il **contratto** è rotto. | 🟠 funzionalità (media) |

| **R** | U5-04 / U5-15 (pubblica game night) | codice+runtime (**CONFERMATO bug**) | **Pubblicazione game night → HTTP 500**: `POST /api/v1/game-nights/{id}/publish` fallisce con `internal_server_error`; stack = **EF Core `Multigraph.ThrowCycle` in `CommandBatchPreparer.BatchCommands`/`TopologicalSort`** durante il `SaveChanges` → **dipendenza circolare (cycle)** nel batch di entità salvate dal publish (RSVP/invitation/notifiche/outbox con FK reciproche). ✅ **CONFERMATO bug strutturale (2026-07-11)**: riverificato in ambiente pulito — outbox **svuotato** (`TRUNCATE domain_event_outbox, audit_outbox, domain_event_logs`) + API riavviata → **stesso identico 500** (`Multigraph.ThrowCycle`). **NON ambientale, NON legato all'outbox** (che aveva solo 10 righe). Bug del publish handler: le entità salvate formano un dependency cycle FK che EF non può ordinare topologicamente. Publish game night rotto → **blocca l'intero flusso game-night live** (U5-04/05/06/07/08/15 + a cascata U6 sessioni). Le game night seed risultano Published ma con `gameIds:0`/`totalInvited:0` (dati parziali, coerente con publish rotto) → causa #S. **🔬 Riverificato 2026-07-12 (full-stack AI + ambiente pulito): #R NON è ambientale + condizione ISOLATA.** Publish di GN **senza pre-invitati** → **204 OK** (anche con `gameIds`). Publish di GN con **≥1 pre-invitato** (`invitedUserIds:[sara]` nel create → RSVP pending, `totalInvited:1`) → **500 `Multigraph.ThrowCycle`** (stack `TopologicalSortCore` → `CommandBatchPreparer.BatchCommands`). Il cycle FK si forma SOLO quando il publish batch persiste le entità RSVP/invitation/notifiche del pre-invitato. NB flusso: `POST /game-nights/{id}/invite` richiede GN già **Published** (409 "Can only invite to published game nights" su Draft) → i pre-invitati entrano dal **create payload** `invitedUserIds` (crea l'RSVP pending). Fix candidato BE: spezzare il SaveChanges del publish (2 fasi) o rimuovere la FK reciproca RSVP↔invitation↔notifica. | 🔴 funzionalità · issue [#2835](https://github.com/meepleAi-app/meepleai-monorepo/issues/2835) · **root cause 2026-07-12b: RSVP duplicati in `Publish()` (manca dedup guard), NON ciclo FK cross-tabella** · fix delegato (branch `fix/issue-2835-game-night-publish-duplicate-rsvp`) |

| **S** | U5-07 (detail game night Published) | runtime (confermato 2 GN) | **Detail game night Published crasha → error boundary**: il componente di dettaglio va in "Errore serate di gioco" con `TypeError: s is not iterable` in un `useMemo` (chunk `_next`), **pur essendo tutte le API 200** (game-night/rsvps/diary). Riprodotto su **2** GN Published di marco ("Game Night Club" `14f38bf9` + "Serata da Marco" `105d4496`), entrambe con dati incompleti (`gameIds:0`, `totalInvited:0`). Il componente itera su un campo null/undefined (non `[]`) invece di degradare a empty-state → **bug di robustezza FE**. ⚠️ **Concausa dati**: le GN Published del seed hanno gameIds/invitati **vuoti** (`90-events.sh` non li ha popolati, o persi al publish-time — possibile correlazione con #R). La Draft di U5-02/03 (con 1 invitato) NON crashava. Blocca U5-05/06/07/08 (partono dal detail Published). Da riverificare con seed corretto in ambiente pulito. **✅ FIXATO + VERIFICATO E2E 2026-07-12b** (PR [#2834](https://github.com/meepleAi-app/meepleai-monorepo/pull/2834) MERGED in main-dev; guard `if (!Array.isArray(entries)) return []` in `GameNightDiaryPanel.tsx`; rebuild web → detail Published-vuota mostra empty-state, no error boundary, no `TypeError`). | 🟢 FIXATO + verificato E2E |

| **T** | U8-09 (salva preferenze notifiche) | codice+runtime (confermato via API) | **Le preferenze notifiche non si salvano**: `PUT /api/v1/notifications/preferences` → **204** ma la modifica **non persiste**. Toggle switch "Email/Documento pronto" (`emailOnDocumentReady` true→false) + Salva → 204 → GET successivo **e post-reload** ritornano `true` (valore originale). Riprodotto **via API diretta** con payload completo simmetrico al GET (`{emailOnDocumentReady:false, …9 flag}`) → **204 ma GET ritorna ancora `true`**. Sia il form FE sia il PUT API danno 204-no-op → **bug BE**: il PUT accetta ma non persiste (o cache `HybridCache` non invalidata). Da confermare in triage con query DB `notification_preferences`. | 🔴 funzionalità (confermato) |

| **U** | U4-11 (riattiva agente archiviato) | codice+runtime (confermato) | **Riattivazione agente archiviato → 404**: il pulsante "Riattiva" (`ctaUnarchive`) in `AgentDetailView.tsx:572` fa `router.push('/agents/{id}/unarchive')` verso una **route pagina inesistente** → **404 "Pagina non trovata"**, invece di chiamare un endpoint di riattivazione (cfr. game-nights che fa correttamente `POST /game-nights/:id/unarchive` via handler). Gli agenti archiviati sono **irriattivabili dall'UI**. Confermato runtime (navigazione a `/agents/{id}/unarchive` → 404) + codice. Blocca U4-11 (nessun agente attivo: l'unico agente seed "Rules Expert" è archiviato e non riattivabile). | 🟠 funzionalità (media) |
| **V** | U4-07/U4-08 (chat-in-game: citazione → PDF) | codice+runtime (**CONFERMATO**, full-stack AI) | **Le citazioni RAG non raggiungono la UI → PDF sorgente NON apribile** (core value rotto). Con stack AI completo + Azul KB indicizzato (doc `513e4041` Ready, 46 chunk, pagine 1-7), la chat-in-game (`/library/{azulId}?tab=aiChat`, hook `useGameChat.ts`) produce una risposta RAG **grounded** (bolla non-vuota, `confidence-badge` 0.80) MA rende **0 `CitationChip`**. Causa = **mismatch contratto SSE**: il backend **emette le citazioni** nell'evento `type=1` (Citations early) con `{text, source:"PDF:{docId}"}`, ma `useGameChat.ts:209` **ignora esplicitamente il `type=1`** (commento *"Ignora type=1 (Citations early)"*) e legge le citazioni **solo dal payload COMPLETE `type=4`** (righe 176, 193), dove il campo `citations` è **`null`** → `citations=[] → agentMsg.citations=undefined → nessun chip`. **Verifica conclusiva su SSE grezzo** (`POST /api/v1/agents/qa/stream`, 200): `{"type":1,"data":{"citations":[{"text":"…","source":"PDF:853f6…"}]}}` presente + `{"type":4,…,"citations":null}`. Triangolazione: DOM (0 chip) + codice (ignora type=1) + SSE reale. **Fix candidato FE**: accumulare le citazioni dall'evento `type=1` durante lo stream e passarle all'`agentMsg` nel COMPLETE (oppure lato BE popolare `citations` nel payload COMPLETE). Contorno: stesso file di #2712 (fix empty-bubble/gating rank-based). Da verificare se il thread `/chat` (`chat-unified/ChatThreadView`) ha lo stesso difetto (consumer `useThreadMessages.ts` è WIP non implementato). **✅ FIXATO 2026-07-12** (branch `fix/chat-citations-sse-type1`, commit `96a4f2ee2`, TDD 18/18): il loop accumula le citazioni dal `type=1` mappandole (`{text, source:"PDF:{docId}", page, score}` → `Citation`) e le usa come fallback quando il COMPLETE non le ha. Verificato E2E post-rebuild: **5 CitationChip** + CitationModal + tab PDF alla pagina citata. Push/PR manuale pendente (remote HTTPS non-interattivo). | 🟢 FIXATO (core RAG) |
| **W** | U4-08 (citazione → PDF sorgente, download) | codice+dati (**CONFERMATO**, deterministico) | **Il PDF sorgente della citazione dà 404 → fonte non leggibile** (bug distinto da #V, emerso DOPO il fix #V). Cliccando un CitationChip la CitationModal apre il tab "PDF originale" **alla pagina corretta** (es. p.6) ma il file non carica: `GET /api/v1/pdfs/{id}/download` → **404**. Root cause dati: la citazione SSE espone come `source` il **`vector_documents.Id`** (es. `853f6dcc`), mentre l'endpoint download risolve per **`pdf_documents.Id`** (es. `513e4041`). I due id sono per costruzione diversi (tabelle distinte; join `vector_documents.PdfDocumentId → pdf_documents.Id` verificato allineato, 0 orfani) → **download 404 SEMPRE, in qualsiasi ambiente** (NON ambientale). **Fix candidato BE**: il costruttore della citazione (`Snippet.source`) deve esporre il `PdfDocumentId` (o l'endpoint/FE risolvere `vector_documents.Id → PdfDocumentId`). Verificato: doc `513e4041` Ready + 46 chunk (pagg.1-7). **✅ FIXATO 2026-07-12** (branch `fix/chat-citations-sse-type1`, commit `0375e287d`, `dotnet build` 0 err): `StreamQaQueryHandler.PerformSearchAndBuildCitationsAsync` risolve `VectorDocumentId → PdfDocumentId` via `IVectorDocumentRepository.GetByGameIdAsync` (dipendenza **già iniettata** → no DI/ctor/catena-search changes, fallback al vector id). **Verificato E2E** (rebuild api): la richiesta download post-fix usa `/api/v1/pdfs/513e4041/download` (PdfDocumentId corretto) invece di `853f6dcc`. Il download resta 404 perché il manuale Azul è **copyright-protected** (i PDF di catalogo non si servono direttamente) → il FE mostra il **gate ownership legittimo** "🔒 carica la tua copia personale / anti-duplicazione" (by-design, ADR copyright; è l'esito valido previsto da U4-08). Nota UX minore: l'endpoint ritorna 404 anche per doc esistente-ma-protetto (un 403 sarebbe semanticamente più corretto). Caveat cache: risposte QA cachate PRIMA del fix (Redis) servono ancora il vecchio id finché non scadono/invalidate. | 🟢 FIXATO (core RAG, BE) |

| **X** | U6-02/05 (sessione live: id duality + start) | runtime (confermato, full-stack) | **Dualità GameSession ↔ LiveGameSession + /start 503**. (a) Il detail `/sessions/{id}` (`useSessionDetail`) interroga l'endpoint **legacy** `/api/v1/sessions/{id}` → **404** per una live-session valida (`/api/v1/live-sessions/{id}` → 200) → mostra `session-summary-not-found` ("Nessuna sessione attiva") anche per una sessione appena creata dal wizard. (b) Le sessioni **seed** sono GameSession status "Setup" **senza** LiveGameSession → `/sessions/{id}/live` → `session-live-not-found`; il banner "Torna alla partita" ci punta → dead-end. (c) `POST /live-sessions/{id}/start` → **503** (chat agent live "Impossibile avviare l'assistente"). La shell live (U6-05) funziona per una live-session valida; è il detail/summary + lo start-agent a rompersi. | 🟠 funzionalità (media) |
| **Y** | U6-11 (chiusura live → play-record) | runtime (confermato) | **Nessuna CTA "Termina sessione" nella live shell G1**: la LiveTopBar espone solo "Esci dalla sessione" (exit). Il flusso happy-path complete→play-record (endgame dialog → "Salva partita" → `POST /live-sessions/{id}/complete` → `/play-records/{id}`) non è raggiungibile dalla topbar del layout G1 (#2374). Da verificare se la CTA è stata spostata in un menu overflow / bottom-sheet mobile. | 🟠 funzionalità (media) |
| **Z** | U6-21/U6-22 (giocatore: sessions + stats) | runtime (confermato) | **`GET /api/v1/sessions/history?limit=50` fallisce la validazione schema FE**: `/players/{id}/sessions` mostra "Schema validation failed: Response validation failed for /api/v1/sessions/history?limit=50" al posto della lista; `/players/{id}/stats` renderizza solo l'header senza KPI card né empty-state (contenuto vuoto, deriva dalla stessa history). Mismatch di contratto FE↔BE sull'endpoint history. | 🟠 funzionalità (media) |
| **AA** | U6-01/18 (card sessione/giocatore) | runtime (minore) | **Id non risolti a nome**: le card sessione in `/sessions` mostrano il **gameId grezzo** come titolo (invece del nome gioco); il player detail `/players/{id}` mostra lo **userId grezzo** come nome giocatore (e nei back-link "Back to {userId}"). Pattern di lookup nome mancante lato FE. UX minore, non blocca. | 🟡 UX/i18n (bassa) |
| **BB** | U6-13/28 (crea play-record) | runtime+API (**CONFERMATO** via API) | **Play-record create persiste un record VUOTO**: il wizard `/play-records/new` (Gioco→Quando→Punteggi, giocatore+score aggiunti) → "✓ Salva partita" → redirect + toast "Partita registrata", MA `GET /play-records/{id}` ritorna status **"Planned"**, `players:[]`, `location:null` — i dati inseriti (giocatore registrato dal form con playerCount 1 + input score, location, punteggio) **non sono persistiti**. Il 2xx+toast è un falso positivo. Sospetto: l'auto-save "BOZZA SALVATA" salva un draft precoce (0 player) e "Salva partita" lo promuove senza risincronizzare i player aggiunti dopo. Da confermare in triage (draft-sync vs mapping submit). | 🔴 funzionalità (data-loss) |
| **CC** | U6-15/28 (edit play-record) | runtime (confermato) | **i18n leak nella pagina edit play-record**: `/play-records/{id}/edit` rende **chiavi i18n grezze** all'utente: `playRecords.edit.step1Label`/`step2Label`/`step3Label`, `playRecords.edit.step1.title`/`.subtitle`/`.searchPlaceholder`, `playRecords.edit.actions.next`. Chiavi mancanti in `locales/*.json`. Rende la form di modifica illeggibile (banner K5 "Modifica limitata" + "Cancella partita" sono invece tradotti). Gemello di #H1. | 🟠 funzionalità/i18n (media) |
| **DD** | U3-18 (libreria condivisa da ospite) | runtime (confermato, giro ospite) | **Il link di condivisione libreria non è accessibile agli ospiti**: `/library/shared/{token}` (token pubblico generato via `POST /library/share`, `privacyLevel:public`) **redirige a `/login?from=/library/shared/{token}`** per un utente non autenticato. Il proxy protegge il prefisso `/library` e `'/library/shared/...'.startsWith('/library')` → la route condivisa (pensata per essere pubblica via possesso del token) è trattata come dashboard protetta. Stessa classe di **#G** (`/library-public`). La funzione "condividi libreria" è rotta per il suo pubblico target. Fix: match boundary-aware nel proxy (whitelist `/library/shared/*` + `/library-public`). Nota: con marco loggato la stessa route funziona (U3-18 giro 1) — il difetto è solo lato ospite. | 🟠 funzionalità (acquisizione/condivisione) |
| **EE** | U2-13 (dettaglio shared-game da ospite) | runtime (confermato, giro ospite) | **Il dettaglio catalogo `/games/{id}` redirige gli ospiti a /login**: `/games/{sharedGameId}` → `/login?from=/games/{id}` per non autenticati. Il catalogo lo definiva "dettaglio pubblico shared-game", ma è auth-gated → la StickyCta "Sign in" (osservabile ospite-specifico di U2-13) non è testabile perché la pagina stessa redirige. Da chiarire se il gating è intenzionale (catalogo richiede login) o un bug di acquisizione. Il catalogo community list era stato letto come ospite (U2-12) su una route diversa. | 🟡 funzionalità (da chiarire) |
| **FF** | A3-19 (edit categoria admin) | runtime+API (**CONFERMATO**) | **L'edit di una categoria crea un duplicato invece di aggiornare**: in `/admin/shared-games/categories`, click matita "Edit {cat}" apre un dialog il cui pulsante di submit è etichettato **"Add category"** e usa la semantica **create (POST /admin/categories)** invece di update (PUT /admin/categories/{id}). Risultato: rinominare "HP-TEST Cat" → "HP-TEST Cat Edit" **crea una seconda categoria** ("Cat Edit") lasciando l'originale invariata (API: 2 categorie). Il dialog edit non è wirato al PUT / non passa l'id. Verificato via API (2 righe). | 🟠 funzionalità (media) |
| **GG** | U4-14..17 (editor RuleSpec da admin) | runtime (confermato) | **L'EditorAuthGuard nega il superadmin**: `/editor` con **admin@meepleai.app (role `superadmin`)** mostra "Non hai i permessi necessari per utilizzare l'editor.". Il guard `RequireRole={['Admin','Editor']}` è una lista piatta che **non include 'superadmin'** → un superadmin (che dovrebbe avere permessi ≥ Admin) viene negato. Role-hierarchy non rispettata. Blocca l'intero flusso editor (U4-14/15) + probabilmente proposte agente (U4-16/17). Fix: includere 'superadmin' nel guard o implementare gerarchia ruoli. Da riverificare con un utente role Admin/Editor puro. | 🟠 funzionalità (media) |
| **HH** | A3-02/03/18 (CRUD admin shared game) | runtime+API (**CONFERMATO**) | **Il CRUD admin degli shared game è rotto su tutte e 3 le gambe**: (CREATE) `/admin/shared-games/new` → "Create Game" → il BE crea il gioco (verificato API) ma la **risposta POST fallisce la schema-validation FE** ("Schema validation failed: Response validation fail") → nessun redirect, errore mostrato, l'admin ritenta → **duplicati**. (DETAIL) `/admin/shared-games/{id}` → **"Failed to load game details."** per OGNI gioco; `GET /api/v1/admin/shared-games/{id}` → **405 Method Not Allowed** → i tab Details/Documents/Agent + "Edit Game" irraggiungibili. (DELETE) `DELETE /api/v1/admin/shared-games/{id}` → **202** ma il gioco resta nella lista (no-op/async non riflesso). Blocca A3-02/03/03b/04/18. Cluster schema-validation FE↔BE (gemello di #Z/#HH) + endpoint detail mancante (405). | 🔴 funzionalità (CRUD catalogo admin) |

### 🔧 Stato fix (branch `fix/invite-flow-end-to-end`, da `main-dev`)

I finding **#C, #D, #B** corretti; **#E** è un falso allarme (non un bug). Commit `7f13eb1d6` (#C+#D) e `64aad57bc` (#B). **PR [#2805](https://github.com/meepleAi-app/meepleai-monorepo/pull/2805)** → `main-dev`. Problema di DbContext concurrency residuo (preesistente) tracciato in **issue [#2804](https://github.com/meepleAi-app/meepleai-monorepo/issues/2804)**.

- **#C** ✅ **FIXED + verificato e2e** — `EmailService.Auth.cs:327`: link email base `/invites/{token}` → `/accept-invite?token=`. #1629 aveva ripuntato per errore a `/invites/` (landing RSVP game-night); la pagina `/accept-invite` esiste e funziona (`→ POST /auth/accept-invitation`). **Verifica e2e** (mailpit): invito reale → email con `http://localhost:3000/accept-invite?token=…` → aperto nel browser → account creato + login + `/onboarding`; DB: user **Active**, token **Accepted**. Test: `EmailServiceInvitationLinkTests` (asserzione invertita).
- **#D** ✅ **FIXED + verificato e2e** — `SendInvitationCommandHandler`: `emailSent` ora riflette l'esito SMTP reale (`… with { EmailSent = emailSent }`) invece di `true` fisso. Test: `SendInvitationCommandHandlerTests` (nuovo — invio ok→true, fallito→false ma invito persistito).
- **#E** ❌ **NON è un bug** — il `/setup-account` "not_found" era corretto: quella route è il redeem *enhanced* (con pending user); il flusso base si riscatta su `/accept-invite`. Nessuna modifica di codice.
- **#B** ✅ **FIXED last-writer-wins** + ⚠️ **concurrency residua separata** — l'approvazione lasciava `status=Pending` + `invitation_id=null` (token comunque creato). **Root cause**: i due event handler async (`AccessRequestCreatedEventHandler` guard Slack, `AccessRequestApprovedEventHandler` InvitationId) ri-scrivevano l'**intero aggregato** via `UpdateAsync` per persistere un solo campo → in race con l'approvazione lo snapshot stale sovrascriveva lo status (**last-writer-wins**). **Fix**: metodi repository di partial-update (`MarkNotifiedAsync`/`SetInvitationIdAsync`) via `ExecuteUpdateAsync` (UPDATE SQL su 1 colonna, no aggregate load/ChangeTracker/SaveChanges). Test: `AccessRequestApprovalIntegrationTests` (4, Testcontainers) + **191** regressione. **Verifica e2e**: `invitation_id` + `last_notified_event_id` ora persistono **senza** clobbering. ⚠️ **MA** lo status resta `Pending` per un **problema separato di DbContext concurrency** nell'ambiente Docker dev (ConcurrencyDetector su request-access + status non committato dall'approve — **preesistente, non questo fix, non riproducibile negli integration test**), tracciato in **[#2804](https://github.com/meepleAi-app/meepleai-monorepo/issues/2804)**. Il primo tentativo (reentrant `SaveChanges` → ConcurrencyDetector) è stato revertito.

### 🔧 Stato fix — U1-06 reset password (#2806, PR #2809)

Finding **#F** ✅ **FIXED + test** (branch `feature/issue-2806-reset-password-token-validator`, da `main-dev`). Rimosso `.Must(BeValidGuid)` da `ResetPasswordCommandValidator` (il token è secure-random base64url, non GUID; esistenza / uso-singolo / scadenza già validati in `PasswordResetService`). Test regressione `Should_Pass_When_Token_Is_Base64Url_From_Service` (fallisce su `main-dev`, passa col fix); rimosso `Should_Fail_When_Token_Is_Not_A_Valid_Guid` (codificava il requisito buggato). **19/19** `ResetPasswordCommandValidatorTests` verdi. Commit `ee29fa992`. Issue **[#2806](https://github.com/meepleAi-app/meepleai-monorepo/issues/2806)** → **PR [#2809](https://github.com/meepleAi-app/meepleai-monorepo/pull/2809)**.

---

## U1 — Accesso

> Eseguito 2026-07-10 (locale, browser reale). ⚠️ Ambiente inizialmente con bypass auth dev attivo (vedi Finding #A) — fixato durante l'esecuzione.

| ID | Tipo | Locale | Staging | Screenshot | Note/Issue |
|----|------|--------|---------|-----------|------------|
| U1-02 Logout | Flow | ✅ | ⏭️ | sì | "Esci" → /login con form. OK |
| U1-01 Login | Flow | ✅ | ⏭️ | sì | marco@meepleai.test → /library, top-bar auth. Console: solo rumore estensioni Chrome (benigno). OK |
| U1-03 Register invite-only | Flow | ✅ | ⏭️ | sì | /register → RequestAccessForm → submit → banner "Request submitted!". `access_requests` row creato (Pending). OK |
| U1-05 Richiesta reset password | Flow | ✅ | ⏭️ | sì | /reset-password → richiesta inviata; email recapitata in **mailpit** (non Gmail: SMTP dev, vedi #SMTP). `password_reset_tokens` row creato. OK |
| U1-06 Conferma reset password (token valido) | Flow | ❌→🔧 | ⏭️ | sì | **BUG P1** (finding **#F**): verify apre il form, ma confirm → **HTTP 422** (`ResetPasswordCommandValidator` esige GUID; token è base64url). Reset **rotto per tutti**. → **Fixato** (#2806 / PR #2809), vedi § Stato fix. |
| U1-20 Legal → redirect /terms | Smoke | ✅ | ⏭️ | sì | `/legal` → dopo ~2s redirect a `/terms` (LegalPageLayout: heading, "ultimo agg. 9 mar 2026", IT/EN, 12 sezioni, "Accettazione" aperta, prev/next). OK |
| U1-21 FAQ ricerca + tab | Smoke | ✅ | ⏭️ | sì | Hero + "Risposte rapide" (4 QuickAnswerCard) + CategoryTabs con conteggi (Tutte 14 / 5 categorie) + 14 FAQ + CTA Contattaci. OK (ricerca interattiva non forzata) |
| U1-22 How It Works | Smoke | ✅ | ⏭️ | sì | Hero + CTA "Inizia Ora" + 3 step (01/02/03) + 4 feature (RAG/Multilingua/PDF/Libreria) + CTA about/faq. OK |
| U1-23 Legali (privacy/terms/cookies) | Smoke | ✅ | ⏭️ | sì | 3 route con LegalPageLayout: sezione default aperta (privacy=Introduzione, terms=Accettazione, cookies=Cosa sono i Cookie), toggle IT/EN, prev/next. OK |
| U1-24 Salva preferenze cookie | Flow | ✅ | ⏭️ | sì | 3 ToggleSwitch (essential locked/analytics/functional) + Salva → persistenza `localStorage["meepleai-cookie-consent"]` (analytics:true). Toast non catturato (effimero). OK |
| U1-28 (nuova) Request→approva→invito→setup e2e | Flow | ❌ | ⏭️ | sì | **Fallisce**. Ambiente fixato (rebuild senza bypass #A; mailpit per SMTP). Catena di 4 bug reali sul flusso invito utente: **#C** link email = `/invites/` (rotto) · **#D** `emailSent:true` falso positivo · **#E** `/setup-account` activate → "not_found" (no pending user) · **#B** approvazione non-transazionale. Invito creato da UI admin = **irriscattabile end-to-end**. → **Fixato** (vedi § Stato fix): #C/#D/#E verificati e2e (invito→`/accept-invite`→account Active), #B unit+regressione. |

## U2 — Catalogo & Discover

> Eseguito 2026-07-11 (locale, browser reale, sessione ospite per le route pubbliche). Blocco authenticated (U2-01…11, utente marco) da completare dopo login.

| ID | Tipo | Locale | Staging | Screenshot | Note/Issue |
|----|------|--------|---------|-----------|------------|
| U2-12 Catalogo community pubblico | Smoke | ✅ | ⏭️ | sì | Ospite: hero "Catalogo community" + filtri (generi/sort/chip) + griglia 163 giochi (rating/anno/contatori toolkit·agente) + cover placeholder emoji 🎲 (nessun asset BGG) + Top contributor + stats community. OK (ricerca interattiva non forzata) |
| U2-14 Landing community pubblica | Smoke | ❌→🔧 | ⏭️ | sì | **BUG** (finding **#G**): `/library-public` (pubblica) **redirige a /login** per gli ospiti — `proxy.ts` prefix-collision (`/library-public`.startsWith(`/library`)). → **Fixato** (branch `feature/proxy-library-public-boundary-match`, PR a fine sessione): boundary-aware `matchesRoutePrefix` + 6 test. |
| U2-01 Sfoglio Discover (default tab) | Flow | ✅ | ⏭️ | sì | Hub montato, Discover default (`data-active-tab="discover"`) + pill-bar 7 filtri esatti (all/games/agents/toolkits/kbs/people/events) + 7 horizontal-row (32 card reali: games/agents/kbs=10, people=2; placeholder emoji) + **0** richieste BGG / 98 risorse + console pulita. Trending vuoto = empty-state legittimo (no play-records seed nella finestra). **Nota grafica (bassa)**: row `events` (0 card) senza empty-state, mentre trending/toolkits lo mostrano. |
| U2-02 Switch tab → Trending (live) | Flow | ✅ | ⏭️ | sì | Click tab Trending → URL `?tab=trending`, `data-active-tab="trending"`, pannello `games-tab-trending` montato + pill-bar Discover smontata. Corpo = empty-state legittimo (`row-empty`, no play-records seed). Empty-state renderizzato correttamente (a differenza row `events` di U2-01). |
| U2-03 Dettaglio gioco + tab interni (Azul) | Flow | ✅ | ⏭️ | sì | `game-detail-view` + hero "Azul" + KPI (7.7/1.8/2–4/45min) + tablist 7 tab (Info attiva). Click Partite → `game-detail-panel-sessions` hidden=false (altri 6 hidden=true) + CTA "Nuova partita" + empty-state legittimo. **azulId=`d625ebe1-f1fb-4a28-b461-89e5c3e2689e`**. 🟠 **Finding i18n** (pannello Info): **#H1** chiavi `houseRules.*` mancanti (path raw) · **#H2** FAQ `{title}` non interpolato. |
| U2-04 Pagina FAQ gioco (Azul) | Smoke | ✅ | ⏭️ | sì | Heading "FAQ" + back-link "← Gioco" → `/library/{azulId}` + empty-state legittimo "No FAQs available for this game yet." API `GET /api/v1/games/{id}/faqs` → **200**. Nessun i18n leak. Console: solo rumore estensioni Chrome ("message channel closed", source 0:0 — benigno, baseline). |
| U2-05 Pagina Regolamento gioco (Azul) | Smoke | ✅ | ⏭️ | sì | Heading "Regolamento" + back-link → `/library/{azulId}` + empty-state legittimo "No rules have been published for this game yet." (KB PDF indicizzato ≠ regole pubblicate strutturate). Nessun i18n leak. |
| U2-06 Pagina Sessioni gioco (Azul) | Smoke | ✅ | ⏭️ | sì | Heading "Sessions" + back-link "← Back to Game" → `/library/{azulId}` + **contenuto reale**: card "1 Session" (5 lug 2026, badge **Setup**, 1 player, 8371 min, CTA View) = sessione live attiva di marco. Nessun i18n leak. Nota minore: durata 8371 min per sessione in Setup = artefatto seed (live mai chiusa, durata ≈ now−createdAt), non UI bug. |
| U2-07 Discover standalone (backward-compat) | Flow | ✅ | ⏭️ | sì | `/discover` monta DiscoverHub **senza** hub multi-tab (`gamesHubPresent:false`, `miniNavTabLinks:0`) + hero + search + pill-bar 7 filtri (all pressed) + 7 horizontal-row incl. trending. Click pill "Giochi" → `aria-pressed=true` + URL `?entity=games` + filtro **funzionante** (solo trending+games `display:flex`; agents/toolkits/kbs/people/events → `display:none`). |
| U2-08 Tab Catalogo → ComingSoon | Smoke | ✅ | ⏭️ | sì | `?tab=catalogo` → `active-tab="catalogo"` + `games-tab-catalogo-coming-soon` (heading "Catalogo") + link fallback `?tab=discover` e `/library` presenti. (screenshot a render parziale, DOM autorevole) |
| U2-09 Tab invalido → fallback Discover | Smoke | ✅ | ⏭️ | sì | `?tab=inesistente` → `active-tab="discover"` (parseTab fallback, invariante #20) + pill-bar presente + **nessun** ComingSoon. |
| U2-10 /hub → redirect Discover | Smoke | ✅ | ⏭️ | sì | `/hub` → URL finale `/games?tab=discover` + `games-hub` montato con `active-tab="discover"` + pill-bar. |
| U2-11 /hub/games/{id} → redirect dettaglio | Smoke | ✅ | ⏭️ | sì | `/hub/games/{azulId}` → URL finale `/games/{azulId}` + `game-detail-view` con hero "Azul". |
| U2-13 Dettaglio pubblico shared-game (Azul) | Flow | ✅* | ⏭️ | sì | Eseguito **con marco loggato** (scelta utente). `shared-game-detail-page` + `active-tab` "overview"→"toolkits" + hero "Azul" (rating 3.9, 2017·2-4·45min·compl.1.8) + contatori (Toolkit 0/Agenti 1/Knowledge 1) + tablist **5 voci** (Panoramica attiva) + descrizione reale. Click Toolkit → pannello visibile con empty-state `shared-game-detail-empty-state` ("Nessun toolkit ancora"). Nessun i18n leak. ⚠️ **`*`PASS parziale**: StickyCta "Sign in" è ospite-specifica → `signInLinkHref:null` con utente loggato (corretto), **da riconfermare in giro ospite** (vedi § Giro ospite pendente). |

> **§ Giro ospite pendente** — osservabili ospite-specifici verificati con utente loggato (quindi assenti = corretti) ma da **riconfermare in sessione ospite** in un giro dedicato: **U2-13** StickyCta "Sign in" (link `/login`). Raggruppabile con gli U1 email-gated o a fine U8.

## U3 — Library & Knowledge Base

> Eseguito 2026-07-11 (locale, browser reale, utente marco). Giro 1 (smoke read-only) in corso.

| ID | Tipo | Locale | Staging | Screenshot | Note/Issue |
|----|------|--------|---------|-----------|------------|
| U3-01 Library hub | Smoke | ✅ | ⏭️ | sì | Hero "La tua libreria" + CTA "+ Aggiungi gioco" + 6 tab con badge (Tutti 27/Giochi 13/Agenti 12/KB 0/Sessioni 2/Chat 0) + 13 giochi (≥11). Switch tab Giochi → 54→13 card (verificato via `element.click()`; il click CDP non ingaggia gli handler dei `button[role=tab]` del hub — limite tooling, non bug). Nessun i18n leak. |
| U3-02 Aggiungi gioco da catalogo | Flow | ✅ | ⏭️ | DOM | Add "Ticket to Ride" (`665b7b7a-…`) via drawer `?action=add` → scelta "Da Catalogo" (`add-game-choice-catalog`) → ricerca "Ticket" (`catalog-search-input`, "2 giochi trovati") → "Seleziona" (`catalog-card-{id}-select-btn`). Osservabili: redirect `/library/{ticketId}` + heading "Ticket to Ride" + persistenza (library **13→14**, POST verificato via re-fetch `/api/v1/library`). Typing su input React via native value-setter + evento `input`; click via `element.click()`. 🟠 Console: 2 `[API Error]` **pre-esistenti/generici** (NON causati da U3-02): #M `gamebooks` 500 + probe `private-games` 404. |
| U3-22 Ciclo library entry: add→remove | Flow | ❌ | ⏭️ | DOM | Add ✅ (vedi U3-02) + persistenza 13→14. **Remove ❌ BLOCKED da #N**: bulk-select irraggiungibile (tab games = `GamesResultsGrid` senza long-press; `onLongPressEnter` solo su `LibraryHybridGrid` nei tab≠games + guard `tab!=='games'` no-op; `handleBulkDelete` `unreachable` #1566). Backend `DELETE /api/v1/library/games/{gameId}` → **204** OK (via API). Dato HP-TEST pulito: library **14→13**, Ticket assente (persistito, confermato post-reload UI: "Giochi 13", 13 card, Ticket non presente). |
| U3-04 Aggiungi gioco a wishlist | Flow | ✅ | ⏭️ | DOM | Dialog "Add to Wishlist" (campo UUID placeholder `xxxx-…`, `<select>` priorità low/medium/high, prezzo/note opz.) → UUID Ticket `665b7b7a-…` + priorità **High** (native value-setter) → conferma. `POST /api/v1/wishlist` → **201**, entry persistita (`id=ddad03ae-…`, `priority=HIGH`, `visibility=PRIVATE`), card renderizzata con badge "ALTA" + "1 game". 🟠 **#O**: card mostra "Game 665b7b7a..." invece di "Ticket to Ride" (titolo non risolto: solo via library). Nota: `/api/v1/wishlist` ritorna un **array diretto**, non paginato. |
| U3-23 Ciclo wishlist: add→remove | Flow | ❌ | ⏭️ | DOM | Add ✅ (vedi U3-04) + persistenza. **Remove ❌ BLOCKED da #P**: `MeepleCard variant="list"` (`ListCard`) ignora la prop `actions` → 🗑️ Remove mai renderizzato (0 button sulla card). Backend `DELETE /api/v1/wishlist/{id}` → **204** OK (via API). Dato HP-TEST pulito: wishlist **1→0**, empty-state "Your wishlist is empty" confermato post-reload UI. |
| U3-03 Wishlist list | Smoke | ❌→✅ | ⏭️ | sì | **BUG #I FIXATO + verificato**: `/library/wishlist` rediriva a `/library?tab=wishlist` (redirect 301 legacy `next.config.js:229`) → wishlist irraggiungibile. Fix: rimosso il redirect (branch `fix/library-wishlist-standalone-route`, commit `58dcf326c`, cherry-pick su testing). Post-rebuild web: `/library/wishlist` renderizza la pagina standalone **"My Wishlist"** (empty-state "Your wishlist is empty" + "Add to Wishlist"). PR [#2818](https://github.com/meepleAi-app/meepleai-monorepo/pull/2818). |
| U3-06 Private games list | Smoke | ✅ | ⏭️ | sì | (Sbloccato dal fix #I, che copriva anche `/library/private`, riga 230.) Pagina standalone **PrivateGamesClient**: stepper "AI Coach" (Crea gioco→Carica PDF→KB→Agente→Chat) + "Gestisci la tua collezione personale (0 giochi)" + ricerca + CTA "+ Aggiungi Gioco" (`add-private-game-btn`) + empty-state "La tua collezione ti aspetta" (Esplora Catalogo / Crea Gioco Custom). 0 giochi privati = empty-state legittimo. |
| U3-04, U3-23 | Flow | ⏭️ | ⏭️ | | Sbloccati dal fix #I (wishlist ora raggiungibile). Da eseguire nel Giro 2 (CRUD). |
| U3-10 Dettaglio gioco library (Azul) | Smoke | ✅ | ⏭️ | DOM | `/library/{azulId}` (GameDetailDesktop) → h1 "Azul" + tablist **5 tab** (Info/Agente/Toolkit/House Rules/Partite, Info attiva). Click House Rules (via `element.click`) → `?tab=houseRules` in URL + aria-selected su House Rules. No i18n leak. (screenshot: glitch tool deserialize → DOM autorevole) |
| U3-11 KB hub gioco (Azul) | Smoke | ✅ | ⏭️ | DOM | heading "Azul · KB" + 1 documento `azul_rulebook` stato **READY** (2.1MB, 12/06) + CTA Carica PDF/Re-index. Reseed non ha toccato Azul (blob present, skipped). docId=`37d96ee8-0761-45e9-b523-b7e42099b43e`. Nota: "COPERTURA: NESSUNA" nonostante 1 doc Ready — coverage calcolata a parte. |
| U3-13 /knowledge-base redirect | Smoke | ✅ | ⏭️ | DOM | `/knowledge-base` → redirect `/library`, h1 "La tua libreria". |
| U3-14 Ricerca KB globale | Flow | ⚠️ | ⏭️ | DOM | **blocked-env**. Osservabili UI OK: HeroSearch (placeholder "Cerca nella knowledge base…"), "MODALITÀ Semantica", submit "punteggio" → URL `?q=punteggio` + branch risultati con header "0 risultati per «punteggio»" + empty-state; `POST /api/v1/knowledge-base/search/global` → **200**. MA ritorna **0 risultati per OGNI query e modalità** (azul/punti/game/player · default+keyword+hybrid) benché i chunk esistano (46 per Azul, cfr #J). Confermato: **nessun servizio AI/embedding** in `make dev-core` (solo web/api/postgres/redis/mailpit) → retrieval non verificabile. Da ri-verificare con `make dev` completo + `make seed-index` (o staging). 🔎 **Sospetto secondario**: keyword mode → 0 su termini certi potrebbe nascondere un bug di **scope** (ricerca globale che non include i chunk dei giochi library) oltre al blocked-env — verificare in ambiente AI-completo. Nota: `422` al mount = POST con query vuota (validazione `query` min 1). |
| U3-15 Dettaglio documento KB | Smoke | ❌ | ⏭️ | DOM | **BUG #J**: split-view ok (KbHeader "azul_rulebook", "Torna alla Libreria", metadata "Chunk 46/Pagine 12") ma lista chunk = "Nessun chunk disponibile" nonostante 46 chunk indicizzati. Root cause: `useKbChunks(id,{limit:200})` → endpoint max 100 → HTTP 400. Anteprima chunk non renderizzata (nessun chunk selezionabile). |
| U3-16 /knowledge-base/[id]/pdf stub | Smoke | ✅ | ⏭️ | DOM | Redirect: `/pdf` → atterra su `/knowledge-base/{docId}` (split-view, h1 "azul_rulebook"). "Coming Soon" intermedio non catturato (redirect ~2s già passato). Osservabile chiave (atterraggio dettaglio) soddisfatto. |
| U3-17 /kb/[id] redirect | Smoke | ❌ | ⏭️ | DOM | **BUG #K**: `/kb/{docId}` → URL malformato `/knowledge-base/{docId}, /knowledge-base/{docId}` (duplicato) → **404 "Pagina non trovata"**. Root cause `force-static` + `redirect()` su route dinamica. |
| U3-18 Libreria condivisa via token | Smoke | ✅ | ⏭️ | DOM | (share link creato come marco: `POST /api/v1/library/share` `{privacyLevel:'public',includeNotes:false}` → token 32-char, poi revocato). `/library/shared/{token}` (route pubblica per-token; eseguito con marco loggato): h1 "**Libreria di Marco R.**" + sottotitolo "condivisa pubblicamente" + badge "**13 giochi**" + "Condiviso il 11 luglio 2026" + griglia **13 card** + CTA footer "**Inizia Ora**". No 404. Il ⋯ sulle card = placeholder #L (irrilevante in sola-lettura). Revoke: `DELETE /api/v1/library/share/{token}` → 204, poi `/library/shared/{token}` → 404. ⚠️ StickyCta/login ospite-specifica da riconfermare in giro ospite (come U2-13). |
| U3-19 Gamebook landing | Smoke | ✅ | ⏭️ | DOM | Hero "I tuoi manuali" + "Manuali fotografati pronti per il tuo tavolo" + CTA "+ Aggiungi manuale" + stats widget (MANUALI 0/QUESTO MESE 0/TRADUZIONI 0). No 404, no i18n leak. (v1 carryover fixture.) |
| U3-20 Gamebook upload | Smoke | ✅ | ⏭️ | DOM | GamebookUploadView FSM 3-step visibile ("1 Gioco / Passo 1 di 3 / 2 Foto / 3 Indice") + step 1 selezione gioco dal catalogo. No 404, no i18n leak. (Stub FSM, upload reale non cablato.) |
| U3-07 Crea gioco privato (wizard) | Flow | ✅ | ⏭️ | DOM | Wizard step 1 "Crea il Gioco": campo `game-name` compilato (via native value-setter + `input` event; il `type` CDP non scrive su input React) + "Crea e Continua →" (element.click) → wizard avanza a step 2 "Carica PDF (Opzionale)", `POST /private-games` 200, gioco creato (id `7a74755e-…`), card in griglia + "collezione personale (1 gioco)". |
| U3-21 Ciclo CRUD gioco privato | Flow | ❌ | ⏭️ | — | Create ✅ (vedi U3-07). **Edit/Delete ❌ BLOCCATI da #L**: il menu "⋯" della card è `MenuPlaceholder` no-op → dialog edit/delete irraggiungibili (confermato anche con click manuale utente). Backend DELETE verificato via API (204). Cleanup dato HP-TEST via API. |
| U3-25 Elimina gioco privato da wizard | Flow | ❌ | ⏭️ | — | Blocked da #L (delete via menu "⋯" placeholder irraggiungibile). Backend DELETE OK (204 via API). |
| U3-08 Private game detail hub | Smoke | ✅ | ⏭️ | DOM | (private game HP-TEST ricreato via API `POST /private-games` con `{source:'manual',title,minPlayers,maxPlayers}`, poi eliminato). `/library/private/{id}` → PrivateGameHub: h1 titolo + "2–4 giocatori" + h2 "Attiva l'Assistente AI" + stepper (step-game-added ✓ / step-pdf / step-agent) + testid nav-hub/nav-toolkit/bottom-tab-hub/bottom-tab-chat. Header + sezioni hub renderizzati, no 404. |
| U3-12 Dettaglio /private-games/[id] | Smoke | ✅ | ⏭️ | DOM | `/private-games/{id}` → `private-game-detail` + h1 titolo + `private-game-pdf-section` (heading "Manuale di gioco", form "Seleziona PDF / fino a 50 MB", pdf-upload-form/button/file-input). Header + sezione PDF renderizzati, no 404. |
| U3-09 Configura toolkit private game | Flow | ❌ | ⏭️ | DOM | Configuratore renderizzato ✅ (h1 "Toolkit Configurator", `CreateToolkitPanel`: input "Nome toolkit" abilita il button "Crea", da `disabled` a enabled). **MA azione "Crea" → BUG #Q**: `POST /api/v1/game-toolkits/` con `{privateGameId,name,overrides}` → **422 `GameId is required`** (contratto FE↔BE disallineato; +gameId → 400 ctor). UI mostra "HTTP 422: Unprocessable Entity", toolkit non creato. Non blocked-env (deterministico). |

## U4 — Chat RAG & Agenti

> Eseguito 2026-07-11/12 (locale dev-core, browser reale, utente marco). ⚠️ **dev-core senza stack AI** (embedding/reranker assenti) + **seed parziale** (1 agente "Rules Expert" archiviato, 0 thread). Gli scenari di chat/streaming/citazioni richiedono `make dev` completo → **blocked-env**. Gli scenari editor richiedono **admin** → pending (giro admin).
>
> **2026-07-12 — ambiente ricreato full-stack AI** (`down -v` + `make dev` + seed KB in corso, embedding CPU-bound ~20-30s/batch). Azul KB indicizzato (doc `513e4041` Ready, 46 chunk). U4-07/08 (chat-in-game citata) **eseguiti**. **#V FIXATO** (branch `fix/chat-citations-sse-type1`, TDD 18/18 + E2E): U4-07 ✅ (5 CitationChip renderizzati). Emerso **#W** (PDF download 404: la citazione usa `vector_documents.Id` invece di `PdfDocumentId`) → U4-08 ⚠️ parziale (CitationModal + tab PDF alla pagina 6 corretta, ma file 404). **U4-03/04** (thread `/chat`) e **U4-05** (wizard crea agente) ancora da eseguire in questo ambiente.

| ID | Tipo | Locale | Staging | Screenshot | Note/Issue |
|----|------|--------|---------|-----------|------------|
| U4-01 Lista thread chat | Flow | ✅ | ⏭️ | DOM | `/chat`: h1 "Chat" + "Le tue Chat / Tutte le conversazioni con gli agenti AI" + empty-state "Nessuna chat trovata. Inizia una nuova conversazione!" (0 thread — seed parziale) + CTA "Nuova chat". Struttura OK; click-thread non esercitabile (0 thread). |
| U4-02 Nuova chat (picker gioco/agente) | Flow | ✅ | ⏭️ | DOM | `/chat/new`: "Inizia una nuova conversazione" + picker "Seleziona un gioco" (tab I miei giochi / Libreria condivisa) + "Continua senza gioco (chat generica)" + "Seleziona un agente (Auto)". Struttura picker OK. "Nessun gioco trovato" (i giochi di marco non hanno KB indicizzato in dev-core → non qualificano). Ramo "avvia conversazione" = blocked-env AI. |
| U4-03 Risposta citata (SSE + citazioni) | Flow | ⚠️ | ⏭️ | — | **blocked-env**: QA-stream `POST /agents/qa/stream` richiede embedding/reranker/LLM-retrieval assenti in dev-core. Non verificabile senza `make dev` completo. |
| U4-04 Citazione → PDF sorgente | Flow | ⚠️ | ⏭️ | — | **blocked-env**: dipende da U4-03 (stack AI). |
| U4-05 Wizard crea agente utente | Flow | ✅ | ⏭️ | DOM | **PASS** (2026-07-12b, full-stack AI). Wizard `/chat/agents/create` 4-step: step1 `game-card-Azul` (selezione `border-amber`) → Avanti → step2 `agent-type-Tutor` → step3 `agent-name-input`="HP-TEST-2026-07-12 Azul Tutor" + selezione PDF `azul_rulebook` (button `border-amber`) → step4 "Riepilogo e conferma" (Azul/Tutor/nome/1 PDF) → "Crea Agent" → `POST /api/v1/agents/user` **201** → redirect `/chat/new?game={azulId}`. **Persistenza confermata** via `GET /games/{azulId}/agents`: agente `b883b2af-…` presente (type RAG). Nota minore: la proiezione lista mostra `kbDocs:0` (linkatura KB async o campo diverso) — osservabile U4-05 comunque soddisfatto. Agente HP-TEST da pulire a fine programma. |
| U4-06 Redirect agent → tab AI Chat | Flow | ✅ | ⏭️ | DOM | `/library/{gameId}/agent` → redirect a `/library/{gameId}?tab=aiChat` (URL finale + title "7 Wonders"); pannello `aiChat` (tabpanel) renderizzato. Chat input non abilitata (7 Wonders senza KB → "Carica un PDF delle regole", coerente dev-core). |
| U4-07 Chat-in-game inline citata | Flow | ✅ | ⏭️ | DOM | **PASS post-fix #V** (2026-07-12, full-stack AI). Azul in library marco (KB `513e4041` Ready, 46 chunk). Pre-fix: risposta grounded (confidence 0.80) ma **0 CitationChip** (#V: citazioni nel SSE `type=1` ignorate dal FE). **Post-fix** (`fix/chat-citations-sse-type1` + rebuild web): stessa domanda → **5 CitationChip** ("📖 p.6/5/5/1/4 — snippet") + `confidence-badge` 0.81 + bolla non-vuota. Osservabili U4-07 soddisfatti. |
| U4-08 Anteprima citazione → PDF | Flow | ✅ | ⏭️ | DOM | **PASS post-fix #V+#W** (2026-07-12, full-stack AI). Click CitationChip → **CitationModal** ("📖 p. 6", tab "📝 Snippet" col testo citazione + tab "📄 PDF originale") → tab PDF monta `pdf-inline-viewer` **alla Pagina 6** con il **documento corretto**: post-fix #W la richiesta è `/api/v1/pdfs/513e4041/download` (PdfDocumentId reale), non più il phantom `853f6dcc` (verificato via network). Esito = **gate ownership legittimo** "🔒 carica la tua copia personale" (manuale copyright-protected — by-design, esito valido previsto dal catalogo U4-08). La verifica utente "risposta citata → leggi il PDF sorgente alla pagina" è **soddisfatta end-to-end**: citazione → chip → modal → snippet → PDF corretto + pagina + gate copyright. |
| U4-09 Catalogo agenti | Smoke | ✅ | ⏭️ | DOM | `/agents`: h1 "Studio agenti" + hero + stats (ATTIVI 0/IN SETUP 0/ARCHIVIATI 1) + barra filtri (Tutti/Attivi/In setup/Archiviati) + "1 agente" (Rules Expert) + `agents-library-view` + search. Filtro "zzz" → 0 risultati (Rules Expert sparisce → filtro funziona). |
| U4-10 Dettaglio agente | Smoke | ✅ | ⏭️ | DOM | `/agents/{id}` (Rules Expert): `agent-detail-view` + nome + tablist 5 tab (Identità/Conoscenza/Prestazioni/Storico/Impostazioni) + click "Conoscenza" (aria-selected) → pannello "Documenti Knowledge Base / Agente standalone". Agente ARCHIVIATO (banner + Riattiva). |
| U4-11 CTA "Gioca" → nuova chat | Flow | ❌ | ⏭️ | DOM | **BLOCKED da #U**: l'unico agente ("Rules Expert") è **archiviato** → niente CTA "Gioca" (mostra "Riattiva"). Il tentativo di riattivare → **404** (#U: `router.push('/agents/{id}/unarchive')` a route inesistente). Nessun agente attivo per esercitare la CTA Gioca. |
| U4-12 Redirect /hub/agents → /agents | Smoke | ✅ | ⏭️ | DOM | `/hub/agents` → redirect a `/agents` (URL finale confermato) + catalogo montato. |
| U4-13 Pipeline Builder canvas | Smoke | ✅ | ⏭️ | DOM | `/pipeline-builder`: title "Pipeline Builder" + "New Pipeline / 0 nodes / 0 edges" (canvas) + pannello Plugins (Routing/Cache/Retrieval/Evaluation/Generation/Validation/Transform + nodi LLM Router/Rules Router). Layout renderizzato, no skeleton. |
| U4-14 Editor RuleSpec landing | Smoke | ❌ | ⏭️ | DOM | **FAIL #GG** (2026-07-12b, admin loggato): `/editor` come **admin@meepleai.app (role superadmin)** → "Editor RuleSpec / **Non hai i permessi necessari per utilizzare l'editor.**" Il guard `RequireRole={['Admin','Editor']}` **non include 'superadmin'** → il superadmin (che dovrebbe essere ≥ Admin) è negato. Role-hierarchy non rispettata nell'EditorAuthGuard. |
| U4-15 Editor RuleSpec con gameId | Flow | ❌ | ⏭️ | — | **blocked #GG**: stesso guard che nega superadmin (U4-14). Non raggiungibile. |
| U4-16 Lista proposte agente | Smoke | ⚠️ | ⏭️ | — | **blocked #GG** probabile (stesso EditorAuthGuard). Da riverificare con un utente role **Admin** o **Editor** (non superadmin). |
| U4-17 Pagina "Feature Removed" | Smoke | ⚠️ | ⏭️ | — | **blocked #GG** probabile (stesso guard). Da riverificare con role Admin/Editor. |

## U5 — Game Night

> Eseguito 2026-07-11 (locale, browser reale, utente marco). Giro marco.

| ID | Tipo | Locale | Staging | Screenshot | Note/Issue |
|----|------|--------|---------|-----------|------------|
| U5-01 Lista game night (calendario/lista/filtri) | Smoke | ✅ | ⏭️ | DOM | h1 "Serate di gioco" + CTA "Nuova serata" + header "4 serate questo mese" + `game-nights-header`/`game-nights-filter-pill-bar`. Vista Calendario default (`game-nights-calendar-month-grid` con Serata da Marco/Game Night Club/Capodanno Ludico). Toggle "Lista" → `?view=list` + `game-nights-list` (4 card, calendario smontato). Filtro "Organizzo" → `?filter=organizing` + lista ristretta 4→**2 organizzate** (Serata da Marco + Game Night Club). Console pulita (solo rumore estensioni; gli `[API Error]` nel buffer erano i 422 pre-esistenti di /knowledge-base/global). |
| U5-02 Crea game night (wizard 4-step) | Flow | ✅ | ⏭️ | DOM | Wizard `/game-nights/new`: step1 titolo "HP-TEST-2026-07-10 Serata Azul" (`game-night-create-title-input`) + datetime-local 2026-07-20 → step2 "Casa mia" (`step2-kind-home` radio) → step3 search "sara" → tag Sara T. (`step3-results` button; "1 invitato") → step4 "Azul" (`step4-game-d625ebe1-…`, "1 gioco selezionato") → submit "Crea evento" (`nav-next`). Redirect `/game-nights/23808313-…` + h1 titolo HP-TEST + stato **Bozza**. API GET 200: status Draft, `totalInvited=1`/`pendingCount=1`, gameIds [azul], scheduledAt 2026-07-20. POST 201 (risorsa creata). Nota minore: `location` null con kind "Casa mia" (indirizzo testuale opzionale, non bug). |
| U5-03 Dettaglio Draft — vista host | Flow | ✅ | ⏭️ | DOM | Sulla Draft di U5-02: h1 titolo + badge **Bozza** + pulsante "Pubblica" (`publish-game-night`) + trigger "Modifica" + planning layout + "organizzato da marco". Roster RSVP: sara **Pending** (`/rsvps` count 1, hasSara). |
| U5-04 Pubblica game night | Flow | ❌ | ⏭️ | DOM | **FAIL #R (bug confermato, issue [#2835](https://github.com/meepleAi-app/meepleai-monorepo/issues/2835))**: `POST /game-nights/{id}/publish` → **500** EF `Multigraph.ThrowCycle` con **≥1 pre-invitato**. **Root cause identificata 2026-07-12b via stack trace reale**: NON è un ciclo FK cross-tabella (RSVP/invitation/notifiche) come ipotizzato prima, ma **RSVP DUPLICATI** — `PreInvite` (al Create) crea l'RSVP, poi `Publish()` ne crea un secondo per lo stesso `(event_id,user_id)` perché manca il guard di dedup che hanno già `PreInvite`/`AddInvitees` → due `GameNightRsvpEntity` con lo stesso unique index → ciclo non ordinabile. Fix = aggiungere il dedup guard in `Publish()`. **Fix delegato a subagent** (branch `fix/issue-2835-game-night-publish-duplicate-rsvp`, TDD). Blocca U5-05/06/08/15 (publish-con-invitati). |
| U5-07 Dettaglio game night Published | Smoke | ✅ | ⏭️ | DOM | **PASS post-fix #S** (2026-07-12b, PR [#2834](https://github.com/meepleAi-app/meepleai-monorepo/pull/2834) MERGED in main-dev + rebuild web). Detail di "Serata da Marco" (`a1e03551`, Published-vuota `gameIds:[]`/`totalInvited:0`) ora renderizza correttamente: **nessun error boundary** (`errorBoundary:false`), h1 "Serata da Marco" + controlli (Voting/Aggiungi partita/Concludi serata) + empty-state "Nessuna partita ancora" + **"Diario serata / Nessun evento registrato"** (il branch empty del `GameNightDiaryPanel` guardato con `Array.isArray`). Console senza `TypeError`. Pre-fix crashava con `TypeError: s is not iterable`. Sblocca U5-05/06/08 lato-detail (restano bloccati lato-publish da #R). |

## U6 — Sessioni & Scoring

> Eseguito 2026-07-12b (full-stack AI, browser reale, utente marco). ⚠️ Seed reale diverso dalle assunzioni catalogo: marco ha **2 sessioni "Setup"** (GameSession, no LiveGameSession → /live 404) + **20 play-record tutti solo** (1 giocatore each → nessun compagno in /players). Scenari live-dipendenti degradati; letti/play-record/players read-only OK. ⚠️ CDP freeze intermittenti su alcune pagine (recuperati via navigazione — glitch tool, non bug).

| ID | Tipo | Locale | Staging | Screenshot | Note/Issue |
|----|------|--------|---------|-----------|------------|
| U6-01 Lista sessioni + filtro stato | Smoke | ✅ | ⏭️ | DOM | `sessions-library-view` + hero "Le tue partite" + "2 partite registrate" + CTA "Registra partita" + filtri (Tutti 2/In corso 2/Completate 0/Abbandonate 0). Click "Completate" → `?status=completed` + 0 card + empty-state "Nessuna partita per questi filtri" + "Reset filtri". Il filtro cambia il set. 🟠 Nota: le card sessione mostrano il **gameId grezzo** come titolo (id-non-risolto), non il nome gioco. |
| U6-02 Crea sessione via wizard | Flow | ✅ | ⏭️ | Net | Wizard `/sessions/new` (SessionCreationWizard): Gioco Azul (picker richiede eventi **keydown/keyup**, non solo input) → Configura Punteggi (Punti/PV default) → Aggiungi Giocatori (HP-TEST-2026-07-12 Marco + Sara, free-text) → Riepilogo → "Crea Sessione". `POST /api/v1/live-sessions` **201** + 2× `/players` 201 → redirect `/sessions/{id}` (id non vuoto). Osservabile create+navigate soddisfatto. 🔴 **2 finding**: (a) `POST /live-sessions/{id}/start` → **503** (chat agent live "Impossibile avviare l'assistente"); (b) il detail `/sessions/{id}` usa endpoint **legacy** `/api/v1/sessions/{id}` → **404** su live-session valida → `session-summary-not-found` ("Nessuna sessione attiva") pur essendo `/live-sessions/{id}` → 200. Sessione HP-TEST `7cda5c11` creata (da pulire — no delete UI). |
| U6-05 Shell sessione live (G1 2 col) | Flow | ✅ | ⏭️ | DOM | Su `/sessions/7cda5c11/live` (live-session creata, "In corso"): **nessun error boundary**, `session-live-top-bar` (LiveTopBar: timer, started-at, exit) + colonna SX `chat-agent-panel` + `action-log-timeline` + colonna DX tab polimorfici **Score/Turni/Widget/Note/Foto/Arbitro**. Click "Turni" → contenuto DX cambia (`turnPanelChanged`). Osservabile G1 soddisfatto. Nota: chat agent live degradata (503 su /start). |
| U6-06 Host edita punteggi polimorfici | Flow | ⚠️ | ⏭️ | DOM | **blocked-env** (by-design): tab Score = `data-slot="scoring-panel-empty"` ("Caricamento punteggi…"). L'evento SignalR `ScoringConfigured` non è emesso (nessuna UI lo emette, ADR-083 Fase 1) → né editor né renderer. Come da § Nota tecnica catalogo. |
| U6-07 Viewer non-host renderer read-only | Flow | ⚠️ | ⏭️ | DOM | **blocked-env**: stesso gate `ScoringConfigured` di U6-06 (`scoring-panel-empty`). |
| U6-11 Chiusura live → play-record | Flow | ❌ | ⏭️ | DOM | **FAIL/finding**: nella live shell (G1) **nessuna CTA "Termina sessione"** — solo "Esci dalla sessione" (exit). Il flusso complete→play-record (endgame dialog → "Salva partita" → `POST /live-sessions/{id}/complete`) non è raggiungibile dalla topbar G1. Da verificare se spostato in menu/bottom-sheet. |
| U6-12 Lista play-records | Smoke | ✅ | ⏭️ | DOM | Header "Le tue partite" + KPI (20 partite/0 vittorie/2 giochi/0h) + `play-history` (21 righe, `record-card-list-{uuid}`) + CTA "+ Registra partita" (`new-play-record-btn`) + filtri (Tutte/In corso/Completate/Pianificate) + GIOCO/DATA/ESITO/SORT. No errori. |
| U6-13 Creazione play-record | Flow | ⏭️ | ⏭️ | — | Pending — `/play-records/new` (SessionCreateForm) ha causato **CDP freeze ripetuti** in questa sessione (glitch tool, recuperato via navigazione). Da rifare. Coperto anche da U6-28. |
| U6-14 Dettaglio play-record | Smoke | ✅ | ⏭️ | DOM | `/play-records/{id}` (Brass: Birmingham, COOPERATIVA): hero "Partita cooperativa di Brass: Birmingham" + Classifica (1 giocatore Marco R.) + KPI grid (DURATA/PUNTEGGIO TOP/MEDIA/DISTACCO) + CTA "🔗 Condividi"/"📷 Aggiungi foto"/"🕘 Cronologia". No errori. 🟠 Durata = formato TimeSpan grezzo "00:00:00.277". |
| U6-15 Modifica play-record (K5 gate) | Flow | ⏭️ | ⏭️ | — | Pending (coperto da U6-28; `/play-records/new` CDP freeze in questa sessione). |
| U6-16 Statistiche play-records | Smoke | ✅ | ⏭️ | DOM | `/play-records/stats` → `?tab=stats`, `data-testid="stats-page"` + KPI (PARTITE 20/GIOCHI 2/WIN RATE 0%/PREFERITO Azul) + presets range (Tutto/30g/90g/12 mesi) + chart "Giochi più giocati (top 2)". No errori. |
| U6-17 Lista giocatori | Smoke | ✅* | ⏭️ | DOM | `players-library-view` + hero "I tuoi compagni di gioco" + KPI (SESSIONI TOTALI 20/GIOCHI DISTINTI 2). ⚠️ **`*`PASS parziale**: mostra card **GIOCHI** (Azul 18/Brass 2), non card compagni — i play-record di marco sono tutti **solo** (1 giocatore) → nessun co-giocatore. Nessun `/players/{id}` link dalla lista. |
| U6-18 Dettaglio giocatore + tab | Smoke | ✅ | ⏭️ | DOM | `/players/{marcoId}`: `player-detail-view` + header player + "20 sessioni · 0 vittorie · 0% win rate" + KPI (20/0/0%/0 ach) + Classifica #1 + tab (Giochi/Achievement). 🟠 Nome player = **userId grezzo** (id-non-risolto, come card sessioni). |
| U6-19 Giocatore — Achievements | Smoke | ✅ | ⏭️ | DOM | `/players/{id}/achievements`: h1 "Achievements" + back-link + "Badges and milestones earned by…" + badge/empty-state. No errori. (Freeze CDP iniziale = transitorio, recuperato.) |
| U6-20 Giocatore — Games Played | Smoke | ✅ | ⏭️ | DOM | `/players/{id}/games`: h1 "Games Played" + "2 Games" (Azul 18 sessions #1, Brass 2 sessions #2). No errori. |
| U6-21 Giocatore — Sessions | Smoke | ❌ | ⏭️ | DOM | **FAIL/finding**: `/players/{id}/sessions` → h1 "Sessions" ma corpo = **"Schema validation failed: Response validation failed for /api/v1/sessions/history?limit=50"** → mismatch contratto FE↔BE sull'endpoint history. Lista sessioni non renderizzata. |
| U6-22 Giocatore — Statistics | Smoke | ❌ | ⏭️ | DOM | **FAIL/finding**: `/players/{id}/stats` → h1 "Statistics" + "Performance stats for {id}" ma **nessuna KPI card né empty-state** (contenuto vuoto, body ~160 char). Probabile stessa root cause di U6-21 (le stats derivano da `/api/v1/sessions/history`). |
| U6-08 Note sessione (personale localStorage) | Flow | ✅ | ⏭️ | DOM+LS | `/sessions/{id}/notes`: heading "Session Notes" + card "Session Summary" (empty official legittimo) + "My Notes" Textarea (aria "Personal session notes") + Save. Scrivo "HP-TEST-2026-07-12 nota-personale" + "Save Notes" → button "**Saved!**" + `localStorage["meepleai_session_notes_{id}"]` = la nota. |
| U6-29 Nota personale CRUD (persistenza cross-reload) | Flow | ✅ | ⏭️ | LS | Ciclo completo su localStorage (fonte autorevole): **CREATE** reload → textarea/ls = "nota-personale". **EDIT** → "nota-editata" + reload → persiste. **CLEAR** svuota+Save → ls empty/null + reload → nota assente. Tutti e 3 i legs confermati via `meepleai_session_notes_{id}`. (Render textarea finale non probabile per CDP freeze; ls autorevole.) |
| U6-13 Creazione play-record | Flow | ❌ | ⏭️ | DOM+API | **FAIL #BB (data-loss)**: wizard `/play-records/new` 3-step (Gioco Azul via combobox dialog → Quando data+location → Punteggi: aggiungo "HP-TEST Marco" score 42) → "✓ Salva partita" → redirect `/play-records/{id}` + toast "Partita registrata". MA il record persiste **vuoto**: `GET /play-records/{id}` → status **"Planned"**, `players:[]` (0), `location:null` — nonostante il form avesse registrato il giocatore (playerCount 1 + `player-score-HP-TEST Marco` input presente). Il 2xx+toast sono un falso positivo (regola verifica scritture). Probabile bug draft-sync (`BOZZA SALVATA` auto-save promosso senza sync dei player aggiunti dopo). |
| U6-15 Modifica play-record (K5) | Flow | ❌ | ⏭️ | DOM | **FAIL #CC (i18n leak)**: `/play-records/{id}/edit` carica il banner K5 "Modifica limitata" + "Per modificare giocatori o punteggi, elimina e ricrea" + "Cancella partita", MA la form mostra **chiavi i18n grezze** ovunque: `playRecords.edit.step1Label`/`step2Label`/`step3Label`, `playRecords.edit.step1.title`/`.subtitle`/`.searchPlaceholder`, `playRecords.edit.actions.next`. Campi editabili (note/data/luogo) non raggiungibili con label leggibili. |
| U6-28 Ciclo CRUD play-record | Flow | ❌ | ⏭️ | DOM+API | **FAIL** (cluster): CREATE ❌ #BB (record Planned vuoto, vedi U6-13) · EDIT ❌ #CC (i18n leak, vedi U6-15) · DELETE: "Cancella partita" (banner K5) **non apre il confirm dialog** via automazione (`element.click` no-op, nessun dialog, record ancora presente); backend `DELETE /play-records/{id}` → **204** OK (via API, record poi 404). Il ciclo CRUD UI è rotto su tutte e 3 le gambe. |
| U6-04 Dettaglio sessione (summary) | Smoke | ✅ | ⏭️ | DOM | `/sessions/{gameSessionId}` (07fb39ce, status Setup) → redirect a `/sessions/{id}/live` (atteso per stato non-Completed) → shell terminale `session-live-not-found` ("Sessione non trovata"). Nessun errore/crash. Shell terminale legittimo per catalogo ("not-found" ammesso). Degradato da **#X** (GameSession senza LiveGameSession). |
| U6-23 Landing pubblica /join (waitlist) | Smoke | ✅ | ⏭️ | DOM | (giro ospite, logout marco) `/join` pubblica → "ALPHA PRIVATA / Il tuo assistente AI per i giochi da tavolo" + "Entra nella waitlist" + form email + feature cards (Library smart/Agenti AI). Nessun redirect a /login. Route pubblica confermata. |
| U6-24/25/26/27 (guest join via token) | Flow | ⚠️ | ⏭️ | — | **blocked**: richiedono token/codici generati da marco (GN invite, session join-code, guest-join token, play-record share) non recuperabili post-logout. Le route `/join/event`, `/join/session`, `/join/[token]` sono pubbliche (prefix whitelisted) ma senza token valido non esercitabili. Da fare in pass dedicato (marco genera i token → guest li usa). |
| U6-03/09/10 | | ⏭️ | ⏭️ | — | Pending: U6-03 (join code, serve sara), U6-09 (scoreboard, serve completata), U6-10 (join token). |

## U7 — Toolkit & Gamebook

> Eseguito 2026-07-12b (full-stack AI, browser reale, utente marco). ⚠️ 0 toolkit seed (endpoint `recommended` v1 vuoto) + gamebook non seedato (campagne create a runtime). ⚠️ CDP freeze intermittenti su pagine pesanti → **mitigati con tab fresco** (nuovo contesto renderer); gamebook Flow completati (U7-13a..17). U7-04/11/12 rimasti pending.

| ID | Tipo | Locale | Staging | Screenshot | Note/Issue |
|----|------|--------|---------|-----------|------------|
| U7-01 Toolkit hub (landing) | Smoke | ✅ | ⏭️ | DOM | `/toolkit` → placeholder "🛠️ Toolkit in arrivo." + sotto-testo "Qui troverai timer, contatori, mazzi e altri strumenti di gioco." No errori. |
| U7-02 Toolkit standalone play | Flow | ✅ | ⏭️ | DOM | `/toolkit/play` (DEFAULT_TOOLKIT, localStorage): header "Toolkit" + actor input + sezioni **Dadi** (D6/2D6/D20/D4/D8/D10/D12) / **Timer** (01:00, turno 02:00) / **Contatori** (Punti) / **Randomizzatore**. Dopo "🎲 Tira D6" + "+" contatore → **CRONOLOGIA** popolata: "HP-TEST Marco D6 → 4" + "Punti: 1" (con orario). Osservabile azione→cronologia soddisfatto. |
| U7-05 Toolkit templates (griglia) | Smoke | ✅ | ⏭️ | DOM | `/toolkit/templates`: header "Toolkit Templates" + "Browse approved templates" + Select categoria "All" + `templates-grid` + empty-state "No approved templates found." (v1 vuoto legittimo). No errori. |
| U7-06 Templates — filtro categoria | Smoke | ✅ | ⏭️ | DOM | Select categoria presente + empty-state coerente per ogni categoria (lista v1 vuota → il cambio filtro mantiene empty-state legittimo). |
| U7-07 Toolkits community (lista) | Smoke | ✅ | ⏭️ | DOM | `/toolkits`: hero "Catalogo toolkit community" + 3 stat (TOOLKIT 0/INSTALLAZIONI 0/FEATURED 0) + tablist (Tutti/Featured/Nuovi/Top 100) + sort (Popolarità/Rating) + empty-state legittimo (endpoint `recommended?limit=50` v1 vuoto, 0 card). No errori. |
| U7-08 Toolkit detail + tab | Flow | ⚠️ | ⏭️ | DOM | **blocked-env**: `/toolkits` vuoto (endpoint recommended v1) → nessun id toolkit apribile (come da nota 2 catalogo). |
| U7-09 Hub/toolkits → redirect | Smoke | ✅ | ⏭️ | DOM | `/hub/toolkits` → redirect a `/toolkits` (finalUrl confermato) + catalogo renderizzato. No loop. |
| U7-03 Toolkit history | Smoke | ✅ | ⏭️ | DOM | `/toolkit/history` (dopo attesa/reload): header "Session History" + "Review past game sessions" + card Filters (Game/Start Date/End Date/Reset) + empty-state "No sessions found / Start Your First Session". No schema error → **#Z NON impatta toolkit history** (specifico dei player sub-pages). Nota: primo load transitorio-vuoto (dare più tempo alle pagine pesanti). |
| U7-10 Library toolbox → redirect | Smoke | ✅ | ⏭️ | DOM | `/library/{azulId}/toolbox` → redirect a `/library/{azulId}?tab=toolbox` + dettaglio Azul renderizzato. No loop. |
| U7-13a Gamebook resume picker (empty) | Smoke | ✅ | ⏭️ | DOM | (tab fresco risolve i freeze CDP) `/library/{azulId}/play` → `gamebook-resume-empty-first-time` + `data-state="state-01-first-time"` + "📖 Inizia la tua prima campagna" + CTA `gamebook-resume-empty-cta`. No errori. |
| U7-13b Crea campagna (wizard 3 step) | Flow | ✅ | ⏭️ | DOM | CTA → `campaign-setup-drawer` ("Nuova campagna · Azul", stepper Nome/Giocatori/Conferma) → title "HP-TEST-2026-07-12 Campagna A" + preset Gruppo A → Avanti×2 → Conferma (review card) → `campaign-setup-submit` → redirect `/play/625dc808…` (campaignId). Campagna persistita (visibile al ritorno picker). |
| U7-14 Gamebook play shell | Flow | ✅ | ⏭️ | DOM | Su `/play/{campaignId}`: `gamebook-play-shell-header` + h1 "HP-TEST-2026-07-12 Campagna A" + riga "§ —" + `gamebook-paragraph-input` + "Salva" + "Traduci pagina" (`gamebook-open-translate`) + "Apri chat con agente". "Nessun libro in corso" = save gated (no narrative book, atteso nota 4). No errori. |
| U7-15 Gamebook encounter (shell) | Flow | ✅ | ⏭️ | DOM | `/play/{id}/encounter`: EncounterCheatsheetView entry "⚔️ → Encounter / Servono stats e opzioni — fotografa l'Encounter Book" + CTA "📷 Fotografa Encounter". Parse reale = blocked-env AI (foto+LLM), shell entry osservabile. No errori. |
| U7-16 Gamebook translate (shell idle) | Flow | ✅ | ⏭️ | DOM | `/play/{id}/translate`: header "Traduci pagina libro game" + `open-camera-button` "Scatta o scegli foto" + link "Digita il paragrafo manualmente" + alert `translate-viewer-no-narrative-books`. Traduzione reale = blocked-env AI. No errori. |
| U7-17 Ciclo CRUD campagna (crea→rinomina→cancella) | Flow | ✅ | ⏭️ | DOM+API | Create 2 campagne (A `625dc808` + B `15886f89`, via "+ Nuova campagna") → **reload** → `state-03-multi-campaign` + "Le tue campagne attive (2)" + rename/delete per id. **Rename A** (`window.prompt` overridato) → "Campagna A rinominata" (PATCH persistito, verificato API+UI). **Delete B** (`window.confirm` overridato) → DELETE soft-delete (API count 2→1, B assente). **Reload persistenza** → `state-02-single-resume` con solo "Campagna A rinominata", B assente. Ciclo completo + persistenza confermata. 🟠 **Finding minore**: dopo create la query picker **non si invalida** (resta single-resume; serve reload per multi-list). Nota: rename/delete usano dialog nativi `window.prompt`/`window.confirm` → overridati in-page per non bloccare l'estensione. Campagna A HP-TEST da pulire. |
| U7-04 Toolkit stats | Smoke | ✅ | ⏭️ | DOM | `/toolkit/stats` → empty-state "No session data available yet. Play some games to see stats!" (legittimo, 0 sessioni toolkit completate). Minore: header "Session Analytics" assente nello stato vuoto. |
| U7-11/12 | | ⚠️ | ⏭️ | — | **blocked #X**: launcher → sessione live dipende da `POST /live-sessions/{id}/start` → 503. |

## U8 — Profilo & Notifiche

> Eseguito 2026-07-11 (locale, browser reale, utente marco). ⚠️ **Ambiente RICREATO** prima di U8: dopo la diagnosi #R (game-night publish bug), su scelta utente fatto **`down -v` (wipe volumi) + `dev-core` + `seed-sp4`**. Il seed si ferma a step 40-agents (embedding assente in dev-core), quindi library/sessioni di marco NON popolate dal seed → **library ripopolata manualmente via API** con 6 giochi del catalogo per rendere U8 rappresentativo. marco ri-loggato dall'utente. mailpit non presente in dev-core.

| ID | Tipo | Locale | Staging | Screenshot | Note/Issue |
|----|------|--------|---------|-----------|------------|
| U8-02 Profilo overview (stats/quick-actions) | Smoke | ✅ | ⏭️ | DOM | `/profile` tab Panoramica: h1 "Marco R." + email + ruolo user + tab bar (Panoramica/Achievement/Attività/Impostazioni) + card **Library Stats 6 tile** (Giochi **6**/Preferiti 0/Posseduti 0/Wishlist 0/PDF 0/In prestito 0) + card "Ultime partite" (empty "Nessuna partita ancora", legittimo — marco senza sessioni post-reseed) + card "Quick Actions" (Achievements/My Library/Storia) + pulsante "Modifica". Console pulita (reload fresco). |
| U8-03 Modifica nome visualizzato | Flow | ✅ | ⏭️ | DOM | Click "Modifica" → Sheet "Modifica profilo" (input "Il tuo nome" = "Marco R.") → nuovo nome "HP-TEST-2026-07-10 Marco" (native value-setter) → "Salva". Sheet chiuso + header aggiornato + `PUT /api/v1/users/profile` → **200** + `/auth/me` displayName aggiornato. **Persistenza post-reload confermata** (h1 = "HP-TEST-2026-07-10 Marco"). Nome **ripristinato** a "Marco R." via API (cleanup dato HP-TEST). |
| U8-04 Tab Attività (feed) | Flow | ✅ | ⏭️ | DOM | Click tab "Attività" → `?tab=activity` + sottotitolo "Le tue ultime partite, achievement e aggiornamenti" + ActivityFeed empty-state "Nessuna attività ancora" (legittimo). No error boundary. |
| U8-05 Achievements standalone | Smoke | ✅ | ⏭️ | DOM | `/profile/achievements` → redirect `?tab=achievements` (stesso componente) → heading Achievement + "Badge guadagnati e progressi" + 4 filtri (Tutti/Ottenuti/In Corso/Bloccati) + empty-state "Nessun achievement disponibile" (legittimo) + click "Ottenuti" applica filtro. No error boundary. Nota: sottotitolo reale "Badge guadagnati e progressi" (≠ testo catalogo, minore). |
| U8-08 Tab Impostazioni + sezioni | Flow | ✅ | ⏭️ | DOM | Click tab "Impostazioni" → `?tab=settings&section=profile` + SettingsTab (sub-nav: Profile/Security/AI consent/Notifications/Preferences/API keys/Connections). Cambio sezione "Preferences" → `?section=preferences` + pannello diverso (THEME Light/Dark/System + LANGUAGE it/en/fr). |
| U8-11 Badge notifiche (SSE) topbar | Smoke | ✅ | ⏭️ | DOM | NotificationBell in topbar (aria-label "Notifiche", icona bell) + `GET /api/v1/notifications/unread-count` → **200** `{"count":0}` (0 non lette → nessun badge, coerente). Nota: connessione SSE `/notifications/stream` non catturata dal network tool (EventSource) — osservabile secondario; primari OK. |
| U8-06 Notifiche: lista + mark-read | Flow | ✅ | ⏭️ | DOM | `/notifications`: h1 "Notifiche" + 5 pill categoria (Tutte/Sessioni/Agenti/Serate/Sistema) + "Segna tutte come lette" + empty-state "Nessuna notifica per ora" (legittimo, marco senza notifiche). Ramo mark-read (card→Drawer→POST mark-read) **non esercitabile** (0 notifiche) — pass su struttura + empty-state, come da catalogo. |
| U8-07 Notifiche: filtri + mark-all | Flow | ✅ | ⏭️ | DOM | Su `/notifications`: pill categoria interattive + "Segna tutte come lette" **disabilitato** (coerente: 0 non lette → "Nessuna notifica non letta"). Ramo mark-all non esercitabile (0 notifiche). Pass su struttura + azione primaria (disabilitata correttamente). Nota: filtro pill non verificabile a fondo con lista vuota. |
| U8-09 Salva preferenze notifiche | Flow | ❌ | ⏭️ | DOM | **FAIL #T**: pagina OK (h1 "Preferenze notifiche" + categorie Documento pronto/Elaborazione fallita/Retry/… + **14 switch** Email/Push/In-App + "Salva preferenze"). Toggle switch (`emailOnDocumentReady` true→false) + Salva → `PUT /api/v1/notifications/preferences` → **204** MA **non persiste**: GET + post-reload ritornano `true` (originale). Confermato via API payload-completo (204 no-op). Bug BE (persistenza o cache-invalidation). |
| U8-01 Dashboard priority-driven | Smoke | ✅ | ⏭️ | DOM | Hero "Buonasera, Marco R. 👋" + KPI grid (GIOCHI **6**/SESSIONI 0/ORE —/WIN RATE — ; "—"=non esposti dal BE, da catalogo) + 4 sezioni Asse C: **Prossimi** (empty "Nessuna Game Night in arrivo" + CTA) / **"Potresti giocare"** (6 giochi library) / **"Cosa fanno i tuoi amici"** / Attività. **Nessun error boundary** — game-night vuote degradano a empty-state (conferma #S specifico del *detail*, non della dashboard). |
| U8-10 Storico versioni RuleSpec | Smoke | ✅ | ⏭️ | DOM | `/versions?gameId={7wonders}`: heading "Storico Versioni RuleSpec" + "Versioni (0)" + "Confronta Versioni" + toggle List/Timeline View + **pulsante "Ripristina" ASSENTE** (role-gated admin/editor; marco=User → corretto). No error boundary. 🟠 Nota: `GET /api/v1/games/{id}/rulespec/history` → **403** per User (history role-gated) → messaggio grezzo inline "API … failed with status 403" in pagina (UX minore; degrada a "Versioni (0)", no crash). |
| U8-13 Pagina offline PWA | Smoke | ⚠️ | ⏭️ | — | **blocked-env**: `/offline` fa `history.back()` automaticamente se `isOnline=true`; il browser (CDP) è online → redirect immediato indietro, stato offline non osservabile senza DevTools→Network→Offline (non producibile via automazione). Come da nota catalogo A-FINAL. |

## A1 — Agenti AI

> **Smoke sweep recon 2026-07-12b** (admin loggato): la landing **`/admin/agents/definitions`** carica OK — "Agent Definitions / Manage AI agent templates for the AI Lab" + "Create Agent" + Search/Status filtri + tabella (Nome/Descrizione/Modello/Stato/Creato) con agenti seed (7 Wonders Duel, Strategy Builder). Nav admin espone anche `/admin/agents/inspector` (RAG Inspector), `/admin/agents/usage`, `/admin/rag-quality`. ⚠️ Route reali diverse dal catalogo A1 → **deep pass da ri-mappare** (scenari specifici non ancora eseguiti, non contati).

| ID | Tipo | Locale | Staging | Screenshot | Note/Issue |
|----|------|--------|---------|-----------|------------|
| _(deep pass da ri-mappare alle route reali; landing `/admin/agents/definitions` smoke-OK)_ | | ⏭️ | ⏭️ | | |

## A2 — Knowledge Base admin

> **Smoke sweep recon 2026-07-12b** (admin loggato): la landing **`/admin/knowledge-base`** carica OK — "Knowledge Base / Admin · KB · Explorer" + "+ Upload PDF" + tab (Explorer / Vector Collections / Processing Queue / RAG Pipeline / Embedding / Feedback / Settings / Snapshots). Nav espone anche `/admin/ai` (AI/RAG) + `/admin/knowledge-base/mechanic-extractor/analyses`. ⚠️ Deep pass da ri-mappare.

| ID | Tipo | Locale | Staging | Screenshot | Note/Issue |
|----|------|--------|---------|-----------|------------|
| _(deep pass da ri-mappare; landing `/admin/knowledge-base` smoke-OK)_ | | ⏭️ | ⏭️ | | |

## A3 — Catalogo condiviso

> Eseguito 2026-07-12b (full-stack AI, browser reale, **admin@meepleai.app** superadmin, login fatto dall'utente). ⚠️ **Admin ristrutturato**: la landing `/admin/shared-games` redirige a **`/admin/content?tab=shared`** (nuovo hub "Gestione Contenuti" tab Shared Games/KB); molte route del catalogo A cambiate (nav reale: `/admin/overview`, `/admin/users`, `/admin/content`, `/admin/ai`, `/admin/knowledge-base`, `/admin/config`, `/admin/monitor`, `/admin/agents/*`, `/admin/catalog-ingestion`, `/admin/providers`, `/admin/analytics`, `/admin/business`, `/admin/rag-quality`). Il catalogo A1-A5 va **ri-mappato** alle route reali (deep pass rinviato). Alcune sub-route (`/admin/shared-games/all`, `/admin/shared-games/categories`) esistono ancora.

| ID | Tipo | Locale | Staging | Screenshot | Note/Issue |
|----|------|--------|---------|-----------|------------|
| A3-01 Lista catalogo (All Games) | Smoke | ✅ | ⏭️ | DOM | `/admin/shared-games` → redirect `/admin/content?tab=shared` ("Gestione Contenuti"): tab Shared Games/Knowledge Base + filtri (Cerca/Categoria/Stato/Giocatori) + stats (Totale 160/Pubblicati 160/Bozze 0) + griglia card giochi con badge + quick-action ✏️📦🗑️. No errori. Route ristrutturata vs catalogo. |
| A3-09 Categorie giochi | Smoke | ✅ | ⏭️ | DOM | `/admin/shared-games/categories` (route esiste): header "Game Categories / Organize and manage game categories" + "Add Category" + tabella (CATEGORY NAME/GAME COUNT/COLOR/ACTIONS) con righe (Abstract 0 games, …). No errori. |
| A3-19 Ciclo CRUD categoria | Flow | ❌ | ⏭️ | DOM+API | **CREATE ✅** ("Add Category" dialog Name/Emoji/Color → "HP-TEST-2026-07-12 Cat" `e718416b` creata, UI+API confermano). **EDIT ❌ #FF**: click matita "Edit" apre un dialog il cui submit è "Add category" (semantica **create/POST**) → invece di aggiornare crea un **DUPLICATO** (`69ee46a8` "Cat Edit"); l'originale resta invariato (API 2 categorie HP-TEST). **DELETE**: il delete-confirm ha dialog-stacking (non completato pulito via UI); backend `DELETE /admin/categories/{id}` → **204** OK (via API, cleanup 2→0). Ciclo CRUD rotto su EDIT. |
| A3-02 Crea shared game (Draft) | Flow | ❌ | ⏭️ | DOM+API | **FAIL #HH**: `/admin/shared-games/new` (form BGG-search + title/desc/year/time/min-maxPlayers/minAge) compilato → "Create Game" → il BE **crea** il gioco (verificato API, id `1f7f5446`, Draft) MA la **risposta POST fallisce schema-validation FE** ("Schema validation failed: Response validation fail...") → **nessun redirect** al detail + errore mostrato → l'admin crede fallito e ritenta (2 duplicati creati). |
| A3-03 Dettaglio shared game (tabs) | Smoke | ❌ | ⏭️ | DOM+API | **FAIL #HH**: `/admin/shared-games/{id}` → **"Failed to load game details."** per OGNI gioco (seed Nanolith Published incluso). `GET /api/v1/admin/shared-games/{id}` → **405 Method Not Allowed** → il detail non carica mai (tabs Details/Documents/Agent irraggiungibili). |
| A3-18 Ciclo CRUD shared game | Flow | ❌ | ⏭️ | API | **FAIL #HH**: CREATE ❌ (schema-fail response, BE crea) · DETAIL ❌ (405) · DELETE: `DELETE /admin/shared-games/{id}` → **202 Accepted** ma il gioco **resta Draft nella lista** (no-op / async non riflesso). Ciclo CRUD admin shared-game rotto su tutte e 3 le gambe. ⚠️ 2 giochi HP-TEST (`1f7f5446`+`3b31290d`) **non eliminati** (202 no-effect) → cleanup manuale pendente. |
| A3-08 Seeding dashboard | Smoke | ✅ | ⏭️ | DOM | `/admin/shared-games/seeding` (route esiste): "Seeding & Enrichment / Gestisci arricchimento giochi e traccia progresso seeding" + "Games (162)" + filtro "All Statuses" + "Enrich Selected" + "Download Excel" + tabella (Title/ID Ext./Data Status/…). No errori. |
| A3-16 Catalog ingestion | Smoke | ✅ | ⏭️ | DOM | `/admin/catalog-ingestion`: "Catalog ingestion / Admin · Catalog · BoardGameGeek sync" + "Export catalog" + "🔄 BGG Catalog Sync / SETUP / Cron ogni 6h". No errori. |
| A3-17 Catalog seed queue | Smoke | ✅ | ⏭️ | DOM | `/admin/catalog/seed-queue`: "Catalog seed queue / Seed pipeline (Wikidata + BGG)" + "Admin-only fetch + approve pipeline" + PENDING + fetch controls. Feature flag ON (no 503). No errori. |
| A3-04/05/06/07/10/11/12/13/14/15/20 | | ⏭️ | ⏭️ | — | Pending (deep pass): publish/link-agent (dipende dal detail rotto #HH), import/wizard PDF (AI), rag-setup, KB per-gioco, games wizard, phases CRUD (A3-13/20), agent-test, processing. |

## A4 — Config & Sistema

> **Smoke sweep recon 2026-07-12b** (admin loggato): landing OK — **`/admin/config`** ("Configurazione" + tab General/Limits/Feature Flags/Rate Limits/Banner + "Registration Mode") e **`/admin/providers`** ("LLM Providers & Circuit Breakers" + stato token/quota/fallback/circuit-breaker). Nav espone anche `/admin/business` (Budget & Cost), `/admin/staging-access`, `/admin/content/email-templates`. ⚠️ Deep pass (mutazioni: toggle registration/flags) da fare — probabile impatto pattern #HH schema-validation.

| ID | Tipo | Locale | Staging | Screenshot | Note/Issue |
|----|------|--------|---------|-----------|------------|
| _(deep pass da ri-mappare; landing `/admin/config` smoke-OK)_ | | ⏭️ | ⏭️ | | |

## A5 — Monitoraggio & Utenti

> **Smoke sweep recon 2026-07-12b** (admin loggato): landing OK — **`/admin/monitor`** ("Monitor · Alerts" + tab Alerts/Metrics/Infrastructure/Command Center/Containers/Logs/Grafana/Events), **`/admin/users`** ("Utenti · 8 utenti" + filtri ruolo + "Invita Utente" + tabella), **`/admin/overview`** ("Overview sistema" + KPI Giochi 162/Documenti 0/Utenti 8 +75%), **`/admin/analytics`** ("Analitiche" + tab Overview/AI Usage/Audit Log/Reports/API Keys). Nav espone anche `/admin/overview/{activity,system}` + `/admin/users/{invitations,roles,access-requests}`. **Tutte le landing read-only caricano OK** — coerente col pattern "read-only ok / mutazioni rotte" (#FF/#GG/#HH). ⚠️ Deep pass mutazioni (invite user, roles, access-request approve) da fare.

| ID | Tipo | Locale | Staging | Screenshot | Note/Issue |
|----|------|--------|---------|-----------|------------|
| _(deep pass da ri-mappare; landing `/admin/monitor` + `/admin/users` smoke-OK)_ | | ⏭️ | ⏭️ | | |
