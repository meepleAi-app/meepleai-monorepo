# Next steps — #2989 mobile GameNight + follow-up (2026-07-15)

Prompt riprendibili per continuare il lavoro in sessioni Claude Code fresche. Ognuno è auto-contenuto (una sessione senza contesto può ripartire). Contesto: sessione 2026-07-15 ha consegnato SP8 demo (#1890, merged), SP9 design (3 mockup verified + handoff), Screen A dashboard mobile completo end-to-end (FE + backend CQRS), Screen B index già implementato + fix mobile bottom-nav. Restano Screen C, il debito #2988, la wave #3.

Branch di lavoro #2989: `feature/issue-2989-mobile-gamenight-social`.
Piano Screen A: [`2026-07-15-issue-2989-sp9-dashboard-mobile.md`](./2026-07-15-issue-2989-sp9-dashboard-mobile.md).

---

## 1. Screen C — detail-RSVP mobile (completa #2989) — ✅ DONE (2026-07-18)

Shipped su `feature/issue-2989-screen-c-detail-rsvp-mobile` (branch originale auto-cancellato al merge #3008). Gap 2 (44px touch-target, `d8e51932f`) + Gap 1+3 (sticky RSVP/host bar, `d306d5de6`) + Gap 4 (pb bottom-nav-safe, `d306d5de6`). Gap 5 (offline-disabled sticky) = follow-up minore. Dettaglio nella § "Execution status — Screen C" del piano Screen A. Prompt originale sotto per riferimento.

```
Riprendi l'implementazione FE di #2989, Screen C: la route /game-nights/[id] (detail + RSVP) in mobile-first 375px.

Branch: feature/issue-2989-mobile-gamenight-social (già esistente, checkout — NON crearne uno nuovo).
Piano di riferimento (pattern già usato per Screen A/B): docs/superpowers/plans/2026-07-15-issue-2989-sp9-dashboard-mobile.md
Mockup target: claude-design-handoff/2026-07-15-sp9-gamenight-mobile/sp9-game-night-detail-rsvp-mobile.dc.html
Brief: admin-mockups/briefs/SP9-mobile-gamenight-social.md § C.

Contesto: i componenti v2 ESISTONO GIÀ e sono testati (come per Screen B, niente greenfield): GameNightDetailView (app/(authenticated)/game-nights/[id]/_components/), GameNightDetailHero, GameNightRsvpActionBar (3-button, ≥44px), GameNightRsvpRow (roster) in components/features/game-night-detail/. Route: app/(authenticated)/game-nights/[id]/page.tsx.

Fai così:
1. Discovery diretta (Read/Grep, NO subagent se sei ancora sotto session limit): leggi GameNightDetailView + i componenti, confronta col mockup C. Identifica i gap MOBILE-specifici reali: (a) RSVP bar sticky-bottom (position sticky/fixed bottom, ≥44px, safe-area sopra MobileBottomBar); (b) hero compatto 375px; (c) i 6 status branch (Draft-host/Published-host/Published-invited-pending/Published-invited-answered/InProgress/Completed) resi bene su mobile; (d) CTA host "Invia inviti".
2. Per ogni gap concreto: ciclo TDD — scrivi test (vitest, pattern GameNightRsvpRow.test.tsx), `cd apps/web && pnpm exec vitest run <path>`, implementa, ri-run verde, `pnpm exec eslint <file>`, commit. Return type SENZA `: JSX.Element` (usa inferenza — il pre-commit typecheck fallisce su namespace JSX globale).
3. Touch-target ≥44px hard sulle CTA RSVP (regressione SP8 B-02 da non ripetere). Zero hex/scrim hardcoded.
4. Aggiorna la § Execution status del piano + commenta #2989. NON aprire PR senza chiedermi.

Nota: se Screen C è già mobile-ready come Screen B, applica solo i fix mobile-safe concreti (sticky bar, bottom-nav padding) e documentalo — non inventare greenfield.
```

---

## 2. Debito #2988 — build-bundle sp7/sp8 rotti

```
Fixa il debito emerso in #2989: il commit #2988 (90f731b4f "DS-17-16 cleanup") ha migrato i page-mock a Storybook e RIMOSSO molti .html/.jsx da admin-mockups/design_files/. Lo script scripts/build-claude-design-bundle.sh referenzia file ora inesistenti → `build-claude-design-bundle.sh sp7` e `sp8` (e quindi `all`) falliscono con `cp: cannot stat`.

File rotti noti:
- SP8_MOCKUPS include sp4-library-mobile.html + sp4-library-mobile.jsx → RIMOSSI da #2988.
- SP7_MOCKUPS include sp7-game-night-new/detail-rsvp/live/summary/notifications-* → verifica quali esistono ancora (grep in design_files).

Branch: parti da main-dev pulito, crea feature/issue-2988-fix-bundle-script (verifica `git branch --show-current` = main-dev + tree pulito prima).

Fai così:
1. Per ogni file negli array SP6_MOCKUPS/SP7_MOCKUPS/SP8_MOCKUPS: verifica se esiste in admin-mockups/design_files/ (script bash con test -f). Elenca i mancanti.
2. Decidi la strategia e applicala: (a) rimuovere dagli array i file migrati a Storybook; oppure (b) recuperarli via `git show 56668f4b3:admin-mockups/design_files/<file>` in una dir; oppure (c) se un intero target è obsoleto, rimuoverlo con un commento. Preferisci (a) — allinea gli array a ciò che esiste, aggiungi un commento "post-#2988 Storybook migration".
3. Verifica: `bash scripts/build-claude-design-bundle.sh sp6 && ... sp7 && ... sp8 && ... all` — tutti devono uscire 0 e produrre bundle. Il mirror asset in mockups/ (fix #1890) deve restare.
4. Commit + push, apri PR verso main-dev. Chiudi il debito citando #2989 dove fu tracciato.
```

---

## 3. Wave #3 — design mobile session-live (gap headline)

```
Attacca la wave mobile del gap HEADLINE di #1890/#2989: il flow session-live immersivo non ha parità mobile. Screen A/B/C (#2989) coprono il wrapper SOCIAL; manca la session-live (/game-nights/[id]/live + session editor/scoring), dove vivono le invarianti #10 max-1-live, #11 3-timestamp, #13 draft-warning, #14 ora-derivata, live-immersive.

È una wave di DESIGN (come SP9 fu per il wrapper social), poi implementazione. Riusa il pattern consolidato:
- Workflow: docs/for-developers/workflows/claude-design-demo-prompts.md (system-prompt SP9 come modello per un nuovo "SP10 mobile session-live")
- Bundle: scripts/build-claude-design-bundle.sh (aggiungi un target sp10 con i reference esistenti — sp7-game-night-transition, sp4-session-*-live.html/jsx, primitive-nav-bottom-mobile, mobile-app.jsx)
- Domain model: docs/for-developers/specs/2026-06-04-gamenight-session-domain-model.md (invarianti #10-#14)
- Gap report SP8: docs/for-developers/audits/2026-07-15-claude-design-gap-report-mobile.md (§4 gap headline)
- Brief modello: admin-mockups/briefs/SP9-mobile-gamenight-social.md

Fai così:
1. Prima brainstorming/scoping: quali schermi session-live mobile (live play, scoring polimorfico, "+ Nuova session" modale, pausa live, max-1-live guard). Chiedimi conferma scope prima di scrivere il brief.
2. Scrivi il brief SP10-mobile-session-live.md (stessa struttura di SP9: scope, componenti v2 esistenti da riusare — cerca useLiveSessionStore, SessionLiveView, ScoringPanelRenderer, PolymorphicScoreEditor — vincoli mobile SP8, invarianti da rendere [INV-n], DoD).
3. Verifica adversariale del brief (workflow, se fuori session limit).
4. System-prompt SP10 (GENERAZIONE, come SP9) + bundle sp10. Poi io faccio la sessione Claude Design e ti giro l'output turno per turno.
5. Valida i mockup generati (workflow), integra nell'handoff, poi piano+TDD per l'implementazione.

Apri una issue di tracking dedicata (follow-up di #2989) prima di iniziare.
```
