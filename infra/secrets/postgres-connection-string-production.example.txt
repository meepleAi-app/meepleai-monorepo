# Production PostgreSQL Connection String Template
# Replace placeholders with actual production values

# Format: Host=HOST;Database=DB;Username=USER;Password=PASS;[OPTIONS]
# SSL/TLS Configuration (Required for Production):
#   - SSL Mode=Require: Forces SSL/TLS connection
#   - Trust Server Certificate=false: Validates server certificate (recommended)
# Connection Pooling (Optimized for Production):
#   - Pooling=true: Enable connection pooling
#   - Minimum Pool Size=5: Pre-allocate 5 connections
#   - Maximum Pool Size=50: Limit to 50 concurrent connections
# Timeouts:
#   - Command Timeout=30: SQL command execution timeout (seconds)
#   - Timeout=15: Connection establishment timeout (seconds)

Host=YOUR_POSTGRES_HOST;Database=meepleai;Username=YOUR_POSTGRES_USER;Password=YOUR_POSTGRES_PASSWORD;Pooling=true;Minimum Pool Size=5;Maximum Pool Size=50;SSL Mode=Require;Trust Server Certificate=false;Command Timeout=30;Timeout=15

# Alternative: Set individual POSTGRES_* environment variables
# The API will automatically construct the connection string via SecretsHelper
# POSTGRES_HOST=your-production-host.com
# POSTGRES_PORT=5432
# POSTGRES_DB=meepleai
# POSTGRES_USER=your-production-user
# POSTGRES_PASSWORD=your-secure-password

# Production Deployment Notes:
# 1. Use managed PostgreSQL (AWS RDS, Azure Database, Google Cloud SQL) for SSL/TLS
# 2. Store password in Docker Secrets, Kubernetes Secrets, or vault
# 3. Enable SSL Mode=Require for encrypted connections
# 4. Adjust pool sizes based on application load and database limits
# 5. Monitor connection pool usage in production (Grafana dashboard)
# 6. Set Trust Server Certificate=false to validate SSL certificates
