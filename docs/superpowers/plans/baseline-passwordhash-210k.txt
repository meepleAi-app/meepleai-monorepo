PasswordHash Baseline — PBKDF2-SHA256 @ 210k iterations
========================================================
Date captured: 2026-05-07
Captured by: Task 2 implementation (auth flow security fixes)
Hardware/env: deferred — see "Status" below
Source of truth: apps/api/src/Api/BoundedContexts/Authentication/Domain/ValueObjects/PasswordHash.cs

Method
------
- Algorithm: PBKDF2 with SHA-256
- Iterations: 210,000 (OWASP 2023 recommendation)
- Salt: 16 bytes (RandomNumberGenerator)
- Output: 32 bytes
- Versioned format: "v1.<iterations>.<salt-base64>.<hash-base64>"

Benchmark sources
-----------------
- BenchmarkDotNet harness:
    apps/api/tests/Api.Tests/Benchmarks/PasswordHashBenchmark.cs
    Methods: Create_210k(), Verify_210k()
    [MemoryDiagnoser] enabled.

- Lightweight Stopwatch harness (test-only, skipped by default):
    apps/api/tests/Api.Tests/Benchmarks/PasswordHashBaselineTests.cs

Status
------
DEFERRED: full BenchmarkDotNet capture not executed in this session.

Reason: BenchmarkDotNet requires a Release-built console runner (BenchmarkRunner.Run<T>())
to produce statistically meaningful results. The Api.Tests project is configured as a
test library (no <OutputType>Exe</OutputType>) and adding a separate runner project was
deemed out of scope for Task 2.

Action items (future):
1. Run BenchmarkDotNet locally:
   - Create apps/api/tests/Api.Tests.Benchmarks/Api.Tests.Benchmarks.csproj (OutputType=Exe)
   - Reference Api.Tests.csproj or replicate PasswordHashBenchmark.cs there.
   - dotnet run -c Release --project apps/api/tests/Api.Tests.Benchmarks
   - Capture: Mean, StdDev, Allocated for both Create_210k and Verify_210k.
   - Replace this document with the BenchmarkDotNet output.

2. Re-run after I7 (PBKDF2 → Argon2id migration) to validate the iteration-count change
   does not regress login latency under the 250ms p99 SLO.

Expected order of magnitude (literature, modern x64):
   - Create:  60-150ms per op
   - Verify:  60-150ms per op
   - Allocations: ~300-500 bytes per op (transient)

If the post-I7 measurement falls outside that band, investigate before merging.
