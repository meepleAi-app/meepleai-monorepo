# Redis configuration for MeepleAI
# Static settings — password injected at startup via --requirepass
# See docker-compose.yml redis service for usage

appendonly yes
maxmemory 768mb
maxmemory-policy allkeys-lru
