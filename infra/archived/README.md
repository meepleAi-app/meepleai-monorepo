# Archived Infrastructure Files

Files moved here are kept as reference but are not part of the active infrastructure stack.

| File | Original Purpose | Reason Archived |
|------|-----------------|-----------------|
| `compose.traefik-examples.yml` | Traefik label examples for all services | Superseded by `compose.meepleai.yml` with real production config |
| `compose.test-legacy.yml` | Test/CI environment overrides | Duplicate of `compose.test.yml`; kept for reference |

For active compose files, see the parent `infra/` directory.
