'use client';

/**
 * AiTopActions — placeholder for the right-side actions slot on the AI
 * hub band. Today empty (parity with #1722 PR 1/4 scope). Future
 * candidates: "New agent", "Refresh requests", quick links to RAG
 * pipeline tooling. Keep this thin until a real action emerges.
 */
export function AiTopActions() {
  return null;
}
