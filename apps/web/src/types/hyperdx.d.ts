/**
 * Ambient type stub for @hyperdx/browser
 *
 * The package is listed in package.json but is not installed in dev/CI.
 * Runtime usage in hyperdx.ts is fully dynamic (any-typed), so this stub
 * satisfies the TypeScript compiler without affecting runtime behaviour.
 */

declare module '@hyperdx/browser' {
  const HyperDX: any;
  export default HyperDX;
}
