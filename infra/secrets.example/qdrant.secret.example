# Qdrant Vector Database — REMOVED (Issue #4861)
# Vector search now uses pgvector (PostgreSQL extension).
# This file is a tombstone — qdrant.secret is no longer required.
# pgvector runs inside the PostgreSQL container — no separate credentials needed.
