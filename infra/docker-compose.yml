services:
  postgres:
    image: postgres:16.4-alpine3.20
    container_name: meepleai-postgres
    restart: unless-stopped
    profiles: [minimal, dev, observability, ai, automation, full]
    environment:
      # Issue #2152: Changed default from 'meeple' to 'postgres' for standard PostgreSQL compatibility
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD_FILE: /run/secrets/postgres-password
      POSTGRES_DB: ${POSTGRES_DB:-meepleai}
      # Performance tuning aligned with resource limits
      POSTGRES_SHARED_BUFFERS: 512MB
      POSTGRES_EFFECTIVE_CACHE_SIZE: 1536MB
      POSTGRES_WORK_MEM: 16MB
      POSTGRES_MAINTENANCE_WORK_MEM: 256MB
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init/postgres-init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    secrets:
      - postgres-password
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 10
    networks:
      - meepleai
    shm_size: 1g
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '1.0'
          memory: 1G

  qdrant:
    image: qdrant/qdrant:v1.12.4
    container_name: meepleai-qdrant
    restart: unless-stopped
    profiles: [minimal, dev, observability, ai, automation, full]
    ports:
      - "127.0.0.1:6333:6333"  # HTTP REST API
      - "127.0.0.1:6334:6334"  # gRPC API
    volumes:
      - qdrantdata:/qdrant/storage
    healthcheck:
      test: ["CMD-SHELL", "kill -0 1 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    networks:
      - meepleai
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 4G
        reservations:
          cpus: '1.0'
          memory: 2G

  redis:
    image: redis:7.4.1-alpine3.20
    container_name: meepleai-redis
    restart: unless-stopped
    profiles: [minimal, dev, observability, ai, automation, full]
    command: ["sh","-c","redis-server --appendonly yes --requirepass $(cat /run/secrets/redis-password) --maxmemory 768mb --maxmemory-policy allkeys-lru"]
    ports:
      - "127.0.0.1:6379:6379"
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a $(cat /run/secrets/redis-password) ping | grep PONG"]
      interval: 10s
      timeout: 3s
      retries: 5
    networks:
      - meepleai
    secrets:
      - redis-password
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 512M

  # ISSUE-922: Email testing service for development (Mailpit - modern Mailhog alternative)
  mailpit:
    image: axllent/mailpit:v1.22
    container_name: mailpit
    restart: unless-stopped
    profiles: [dev, observability, full]
    ports:
      - "127.0.0.1:1025:1025"  # SMTP server
      - "127.0.0.1:8025:8025"  # Web UI + REST API
    environment:
      MP_MAX_MESSAGES: 500
      MP_SMTP_AUTH_ACCEPT_ANY: 1
      MP_SMTP_AUTH_ALLOW_INSECURE: 1
      MP_DATABASE: /data/mailpit.db
      MP_DATA_DIR: /data
    volumes:
      - mailpit-data:/data
    healthcheck:
      # Comprehensive health check: HTTP API + SMTP port
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:8025/api/v1/messages && nc -z localhost 1025"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s
    networks:
      - meepleai
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 128M
        reservations:
          cpus: '0.25'
          memory: 64M

  ollama:
    image: ollama/ollama:0.3.14
    container_name: meepleai-ollama
    restart: unless-stopped
    profiles: [ai, full]
    environment:
      # Memory management for CPU-only mode
      OLLAMA_MAX_LOADED_MODELS: 3
      OLLAMA_KEEP_ALIVE: 5m
      # Uncomment if using GPU
      # OLLAMA_GPU_MEMORY_FRACTION: 0.80
    ports:
      - "11434:11434"
    volumes:
      - ollamadata:/root/.ollama
    healthcheck:
      test: ["CMD-SHELL", "ollama list || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
      - meepleai
    deploy:
      resources:
        limits:
          cpus: '4.0'
          memory: 8G
        reservations:
          cpus: '2.0'
          memory: 4G
    # Uncomment if you have NVIDIA GPU support
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]

  ollama-pull:
    image: curlimages/curl:8.12.1
    container_name: ollama-pull
    profiles: [ai, full]
    depends_on:
      ollama:
        condition: service_healthy
    restart: "no"
    command: >
      sh -c '
        echo "Pulling nomic-embed-text model..." &&
        curl -X POST http://ollama:11434/api/pull -d "{\"name\":\"nomic-embed-text\"}" &&
        echo "Model pull initiated!"
      '
    networks:
      - meepleai
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
        reservations:
          cpus: '0.5'
          memory: 256M

  # AI-09: Local multilingual embedding service
  embedding-service:
    build:
      context: ../apps/embedding-service
      dockerfile: ./Dockerfile
    container_name: meepleai-embedding
    restart: unless-stopped
    profiles: [ai, full]
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - meepleai
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 4G
        reservations:
          cpus: '1.0'
          memory: 2G
    # Uncomment if you have NVIDIA GPU support for faster embeddings
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]

  # BGAI-001-v2: Unstructured PDF extraction service (Stage 1)
  unstructured-service:
    build:
      context: ../apps/unstructured-service
      dockerfile: ./Dockerfile
    container_name: meepleai-unstructured
    restart: unless-stopped
    profiles: [ai, full]
    ports:
      - "8001:8001"
    environment:
      - LOG_LEVEL=INFO
      - MAX_FILE_SIZE=52428800  # 50MB
      - TIMEOUT=30
      - UNSTRUCTURED_STRATEGY=fast
      - LANGUAGE=ita
      - CHUNK_MAX_CHARACTERS=2000
      - CHUNK_OVERLAP=200
      - QUALITY_THRESHOLD=0.80
      - MIN_CHARS_PER_PAGE=500
      - ENABLE_METRICS=true
      - ENABLE_STRUCTURED_LOGGING=true
    volumes:
      - unstructured-temp:/tmp/pdf-extraction
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    networks:
      - meepleai
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '1.0'
          memory: 1G

  # BGAI-005: SmolDocling VLM service (Stage 2 - complex layouts fallback)
  smoldocling-service:
    build:
      context: ../apps/smoldocling-service
      dockerfile: ./Dockerfile
    container_name: meepleai-smoldocling
    restart: unless-stopped
    profiles: [ai, full]
    ports:
      - "8002:8002"
    environment:
      - LOG_LEVEL=INFO
      - MAX_FILE_SIZE=52428800  # 50MB
      - MAX_PAGES_PER_REQUEST=20
      - TIMEOUT=60
      - DEVICE=cpu  # Use cpu for development (set to cuda in production with GPU)
      - MODEL_NAME=docling-project/SmolDocling-256M-preview
      - MAX_NEW_TOKENS=2048
      - TORCH_DTYPE=bfloat16
      - IMAGE_DPI=300
      - IMAGE_FORMAT=PNG
      - QUALITY_THRESHOLD=0.70
      - MIN_CHARS_PER_PAGE=300
      - ENABLE_METRICS=true
      - ENABLE_STRUCTURED_LOGGING=true
      - ENABLE_MODEL_WARMUP=true  # Enable warmup so health checks pass
    volumes:
      - smoldocling-temp:/tmp/pdf-processing
      - smoldocling-models:/root/.cache/huggingface
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8002/health"]
      interval: 60s
      timeout: 15s
      retries: 3
      start_period: 120s  # Long startup for model download
    networks:
      - meepleai
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 4G
        reservations:
          cpus: '1.0'
          memory: 2G
    # Uncomment for GPU support (requires NVIDIA Docker runtime)
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]

  # ADR-016 Phase 4: Cross-encoder reranking service for RAG pipeline
  reranker-service:
    build:
      context: ../apps/reranker-service
      dockerfile: ./Dockerfile
    container_name: meepleai-reranker
    restart: unless-stopped
    profiles: [ai, full]
    ports:
      - "127.0.0.1:8003:8003"  # Bind to localhost only for security
    environment:
      - MODEL_NAME=BAAI/bge-reranker-v2-m3
      - BATCH_SIZE=32
      - LOG_LEVEL=INFO
      - ENABLE_MODEL_WARMUP=true
    volumes:
      - reranker-models:/home/reranker/.cache/huggingface
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8003/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s  # Long startup for model download
    networks:
      - meepleai
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '1.0'
          memory: 1G
    # Uncomment for GPU support (requires NVIDIA Docker runtime)
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]

  # OPS-02: OpenTelemetry Observability Stack
  # Seq and Jaeger removed - replaced by HyperDX unified observability (see docker-compose.hyperdx.yml)
  # Migration: Issue #1564 - HyperDX provides logs, traces, and session replay in one platform
  # HyperDX UI: http://localhost:8180
  # OTLP endpoints: gRPC :14317, HTTP :14318

  prometheus:
    image: prom/prometheus:v3.7.0
    container_name: meepleai-prometheus
    restart: unless-stopped
    profiles: [dev, observability, full]
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=30d'
      - '--storage.tsdb.retention.size=5GB'
      - '--web.enable-lifecycle'
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./prometheus-rules.yml:/etc/prometheus/prometheus-rules.yml:ro
      - prometheusdata:/prometheus
    ports:
      - "127.0.0.1:9090:9090"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:9090/-/healthy"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - meepleai
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 1G

  # OPS-05: Error Monitoring & Alerting
  alertmanager:
    image: prom/alertmanager:v0.27.0
    container_name: meepleai-alertmanager
    restart: unless-stopped
    profiles: [observability, full]
    env_file:
      - ./env/alertmanager.env
    secrets:
      - gmail-app-password
    volumes:
      - ./scripts/load-secrets-env.sh:/scripts/load-secrets-env.sh:ro
      - ./alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
      - alertmanagerdata:/alertmanager
    entrypoint: ["/scripts/load-secrets-env.sh"]
    command:
      - '/bin/alertmanager'
      - '--config.file=/etc/alertmanager/alertmanager.yml'
      - '--storage.path=/alertmanager'
      - '--web.external-url=http://localhost:9093'
    ports:
      - "127.0.0.1:9093:9093"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:9093/-/healthy"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - meepleai
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M

  # NOTE: Grafana OnCall is commented out for now as it requires manual setup
  # To add on-call management, follow the setup guide at:
  # https://grafana.com/docs/oncall/latest/set-up/open-source/
  # You can use Alertmanager directly with Slack/Email notifications in the meantime
  #
  # grafana-oncall:
  #   image: grafana/oncall:latest
  #   restart: unless-stopped
  #   environment:
  #     BASE_URL: http://localhost:8082
  #     SECRET_KEY: ${ONCALL_SECRET_KEY:-changeme-generate-secure-secret}
  #     DATABASE_TYPE: sqlite3
  #   volumes:
  #     - oncalldata:/var/lib/oncall
  #   ports:
  #     - "8082:8080"
  #   depends_on:
  #     - redis
  #     - alertmanager
  #   networks:
  #     - meepleai

  # P3-705: cAdvisor for container metrics
  cadvisor:
    image: gcr.io/cadvisor/cadvisor:v0.49.1
    container_name: meepleai-cadvisor
    restart: unless-stopped
    profiles: [observability, full]
    privileged: true
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /dev/disk/:/dev/disk:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro  # Required for Docker API access
    ports:
      - "127.0.0.1:8082:8080"
    networks:
      - meepleai
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M

  # P3-705: node-exporter for host metrics
  node-exporter:
    image: prom/node-exporter:v1.8.2
    container_name: meepleai-node-exporter
    restart: unless-stopped
    profiles: [observability, full]
    command:
      - '--path.rootfs=/host'
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
      - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)'
    volumes:
      - /:/host:ro
    ports:
      - "127.0.0.1:9100:9100"
    networks:
      - meepleai
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          cpus: '0.25'
          memory: 128M

  grafana:
    image: grafana/grafana:11.4.0
    container_name: meepleai-grafana
    restart: unless-stopped
    profiles: [dev, observability, full]
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD__FILE: /run/secrets/grafana-admin-password
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_SERVER_ROOT_URL: "http://localhost:3001"
      # Issue #901: Enable iframe embedding for Infrastructure page
      GF_SECURITY_ALLOW_EMBEDDING: "true"
      GF_AUTH_ANONYMOUS_ENABLED: "true"
      GF_AUTH_ANONYMOUS_ORG_ROLE: "Viewer"
    volumes:
      - grafanadata:/var/lib/grafana
      - ./grafana-datasources.yml:/etc/grafana/provisioning/datasources/datasources.yml:ro
      - ./grafana-dashboards.yml:/etc/grafana/provisioning/dashboards/dashboards.yml:ro
      - ./dashboards:/var/lib/grafana/dashboards:ro
    secrets:
      - grafana-admin-password
    ports:
      - "127.0.0.1:3001:3000"
    depends_on:
      - prometheus
      # jaeger dependency removed - replaced by HyperDX (Issue #1564)
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
      - meepleai
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 512M

  n8n:
    image: n8nio/n8n:1.114.4
    container_name: meepleai-n8n
    restart: unless-stopped
    profiles: [automation, full]
    environment:
      # Node.js heap size tuning (75% of container limit minus overhead)
      NODE_OPTIONS: "--max-old-space-size=512"
    env_file:
      - ./env/n8n.env.dev
    secrets:
      - postgres-password
      - n8n-encryption-key
      - n8n-basic-auth-password
    volumes:
      - ./scripts/load-secrets-env.sh:/scripts/load-secrets-env.sh:ro
    entrypoint: ["/scripts/load-secrets-env.sh"]
    command: ["n8n"]
    ports:
      - "127.0.0.1:5678:5678"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - meepleai
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 512M

  api:
    build:
      context: ../apps/api
      dockerfile: ./src/Api/Dockerfile
    container_name: meepleai-api
    restart: unless-stopped
    profiles: [minimal, dev, observability, ai, automation, full]
    env_file:
      - ./env/api.env.dev
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
      # Postgres/Redis connection details come from api.env.dev and secrets
      QDRANT_URL: http://qdrant:6333
      OLLAMA_URL: http://ollama:11434
      # AI-09: Use external embedding service to avoid OOM in API container
      EMBEDDING_PROVIDER: external
      Embedding__Provider: External
      EMBEDDING_MODEL: intfloat/multilingual-e5-large
      EMBEDDING_DIMENSIONS: 1024
      # Override nested config from appsettings.json (ASP.NET Core uses __ for nesting)
      Embedding__Dimensions: 1024
      # AI-09: External embedding service configuration (Python service)
      LOCAL_EMBEDDING_URL: http://embedding-service:8000
      Embedding__LocalServiceUrl: http://embedding-service:8000
      EMBEDDING_FALLBACK_ENABLED: "false"
      Embedding__FallbackEnabled: "false"
      # ADR-016 Phase 4: Reranker service configuration
      RERANKER_SERVICE_URL: http://reranker-service:8003
      # AI-13: BoardGameGeek API token (REQUIRED as of Jan 2026 - register at https://boardgamegeek.com/using_the_xml_api)
      # Format: Bgg__ApiToken uses ASP.NET Core __ notation for nested config
      BGG_API_TOKEN_FILE: /run/secrets/bgg-api-token
      # SEC-708: OpenRouter API key from secret file
      OPENROUTER_API_KEY_FILE: /run/secrets/openrouter-api-key
      # Bootstrap: Initial admin user configuration (email and display name from api.env.dev, password from secret)
      # Note: INITIAL_ADMIN_EMAIL is defined in ./env/api.env.dev
      INITIAL_ADMIN_PASSWORD_FILE: /run/secrets/initial-admin-password
      # SEC-2459: JWT signing secret from secret file
      JWT_SECRET_FILE: /run/secrets/jwt-secret
      # OAuth provider credentials from secret files
      GOOGLE_OAUTH_CLIENT_ID_FILE: /run/secrets/google-oauth-client-id
      GOOGLE_OAUTH_CLIENT_SECRET_FILE: /run/secrets/google-oauth-client-secret
      DISCORD_OAUTH_CLIENT_ID_FILE: /run/secrets/discord-oauth-client-id
      DISCORD_OAUTH_CLIENT_SECRET_FILE: /run/secrets/discord-oauth-client-secret
      GITHUB_OAUTH_CLIENT_ID_FILE: /run/secrets/github-oauth-client-id
      GITHUB_OAUTH_CLIENT_SECRET_FILE: /run/secrets/github-oauth-client-secret
    secrets:
      - postgres-password
      - redis-password
      - jwt-secret
      - openrouter-api-key
      - bgg-api-token
      - initial-admin-password
      - google-oauth-client-id
      - google-oauth-client-secret
      - discord-oauth-client-id
      - discord-oauth-client-secret
      - github-oauth-client-id
      - github-oauth-client-secret
    volumes:
      - ./scripts/load-secrets-env.sh:/scripts/load-secrets-env.sh:ro
      - pdf-uploads:/app/pdf_uploads  # Persistent PDF storage
      # ISSUE-2510: Secret management system - mount secrets directory
      - ./secrets:/app/infra/secrets:ro  # Read-only mount for .secret files
    entrypoint: ["/scripts/load-secrets-env.sh"]
    command: ["dotnet", "Api.dll"]
    # Direct port binding for development (Next.js proxy handles routing)
    ports:
      - "127.0.0.1:8080:8080"
    # Traefik labels (ready for production Linux deployment)
    # Commented out for Windows development - use Next.js proxy instead
    # labels:
    #   - "traefik.enable=true"
    #   - "traefik.http.routers.meepleai-api.rule=Host(`localhost`) && PathPrefix(`/api`)"
    #   - "traefik.http.routers.meepleai-api.entrypoints=web"
    #   - "traefik.http.routers.meepleai-api.priority=10"
    #   - "traefik.http.services.meepleai-api.loadbalancer.server.port=8080"
    #   - "traefik.http.routers.meepleai-api.middlewares=rate-limit-api@file,security-headers@file"
    depends_on:
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_started
      redis:
        condition: service_healthy
      # ollama dependency removed - optional service for local embeddings
      # API works without it (uses OpenRouter for LLM, embedding-service for embeddings)
    healthcheck:
      test: ["CMD-SHELL", "curl --fail http://localhost:8080/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s
    networks:
      - meepleai
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 4G  # Sufficient with batch processing (20 chunks/batch = ~500MB peak)
        reservations:
          cpus: '1.0'
          memory: 2G

  web:
    build:
      context: ../apps/web
      dockerfile: ./Dockerfile
    container_name: meepleai-web
    restart: unless-stopped
    profiles: [minimal, dev, observability, ai, automation, full]
    env_file:
      - ./env/web.env.dev
    # Direct port binding for development (Next.js proxy handles routing)
    ports:
      - "127.0.0.1:3000:3000"
    # Traefik labels (ready for production Linux deployment)
    # Commented out for Windows development - use Next.js proxy instead
    # labels:
    #   - "traefik.enable=true"
    #   - "traefik.http.routers.meepleai-web.rule=Host(`localhost`)"
    #   - "traefik.http.routers.meepleai-web.entrypoints=web"
    #   - "traefik.http.routers.meepleai-web.priority=1"
    #   - "traefik.http.services.meepleai-web.loadbalancer.server.port=3000"
    #   - "traefik.http.routers.meepleai-web.middlewares=security-headers@file"
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:3000/ || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 12
      start_period: 60s
    networks:
      - meepleai
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 512M

volumes:
  pgdata:
  qdrantdata:
  # seqdata removed - replaced by HyperDX (Issue #1564)
  ollamadata:
  # ISSUE-922: Mailpit email testing storage
  mailpit-data:
  # OPS-02: OpenTelemetry volumes
  # jaegerdata removed - using ephemeral storage for development
  # Seq and Jaeger replaced by HyperDX unified observability
  prometheusdata:
  grafanadata:
  # OPS-05: Error Monitoring & Alerting volumes
  alertmanagerdata:
  # BGAI-001-v2: Unstructured PDF extraction temporary storage
  unstructured-temp:
  # BGAI-005: SmolDocling VLM service storage
  smoldocling-temp:
  smoldocling-models:  # Hugging Face model cache
  # ADR-016 Phase 4: Reranker service model cache
  reranker-models:
  # PDF Uploads: Persistent storage for uploaded PDFs
  pdf-uploads:

networks:
  meepleai:
    driver: bridge

# SEC-708: Docker Secrets for sensitive credentials
# Secret files are stored in ./secrets/ directory (gitignored)
# Initialize secrets with: tools/secrets/init-secrets.sh
#
# ISSUE-2510: New secret management system (alternative to Docker secrets)
# - Copy templates: cp -r infra/secrets.example/* infra/secrets/
# - Edit files: Remove .example extension and fill in actual values
# - Format: KEY=VALUE (one per line, comments with #)
# - Validation: Critical/Important/Optional levels enforced at startup
secrets:
  postgres-password:
    file: ./secrets/postgres-password.txt
  redis-password:
    file: ./secrets/redis-password.txt
  jwt-secret:
    file: ./secrets/jwt-secret.txt
  openrouter-api-key:
    file: ./secrets/openrouter-api-key.txt
  bgg-api-token:
    file: ./secrets/bgg-api-token.txt
  n8n-encryption-key:
    file: ./secrets/n8n-encryption-key.txt
  n8n-basic-auth-password:
    file: ./secrets/n8n-basic-auth-password.txt
  gmail-app-password:
    file: ./secrets/gmail-app-password.txt
  grafana-admin-password:
    file: ./secrets/grafana-admin-password.txt
  initial-admin-password:
    file: ./secrets/initial-admin-password.txt
  # OAuth provider secrets for external authentication
  google-oauth-client-id:
    file: ./secrets/google-oauth-client-id.txt
  google-oauth-client-secret:
    file: ./secrets/google-oauth-client-secret.txt
  discord-oauth-client-id:
    file: ./secrets/discord-oauth-client-id.txt
  discord-oauth-client-secret:
    file: ./secrets/discord-oauth-client-secret.txt
  github-oauth-client-id:
    file: ./secrets/github-oauth-client-id.txt
  github-oauth-client-secret:
    file: ./secrets/github-oauth-client-secret.txt