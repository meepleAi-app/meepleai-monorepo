# Database Sync CLI Configuration
# API base URL (local development)
API_URL=http://localhost:8080/api/v1/admin/database-sync

# SuperAdmin JWT token — source from secrets
# Generate by logging in as SuperAdmin and extracting the token
MEEPLEAI_ADMIN_TOKEN=
